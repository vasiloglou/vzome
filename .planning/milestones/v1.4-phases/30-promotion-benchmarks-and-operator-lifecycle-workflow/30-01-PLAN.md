---
phase: 30-promotion-benchmarks-and-operator-lifecycle-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/serving_benchmark.py
  - materials-discovery/tests/test_llm_serving_benchmark_schema.py
  - materials-discovery/tests/test_llm_serving_benchmark_core.py
autonomous: true
requirements: [LLM-25, OPS-14]
---

<objective>
Give lifecycle benchmarks explicit target roles so recommendation output can
recommend promote/keep/rollback behavior from structured intent.
</objective>

<tasks>

<task>
  <name>Task 1: Add typed lifecycle benchmark roles</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_llm_serving_benchmark_schema.py
  </files>
  <action>
Add an additive benchmark-target role field for lifecycle workflows that can
distinguish the promoted default target, a candidate checkpoint target, and the
baseline rollback lane without relying on target IDs.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_schema.py -x -v</automated>
  </verify>
</task>

<task>
  <name>Task 2: Emit benchmark-backed lifecycle recommendations</name>
  <files>
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/tests/test_llm_serving_benchmark_core.py
  </files>
  <action>
Use the new structured lifecycle benchmark roles to emit clear summary lines
for promoting a candidate checkpoint, keeping the current promoted checkpoint,
and retaining the baseline local rollback path.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_core.py -x -v</automated>
  </verify>
</task>

</tasks>
