---
phase: 30-promotion-benchmarks-and-operator-lifecycle-workflow
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - materials-discovery/configs/systems/al_cu_fe_llm_adapted_candidate.yaml
  - materials-discovery/configs/llm/al_cu_fe_checkpoint_lifecycle_benchmark.yaml
  - materials-discovery/tests/test_real_mode_pipeline.py
autonomous: true
requirements: [LLM-25, OPS-14]
---

<objective>
Commit and prove the three-way lifecycle benchmark workflow offline.
</objective>

<tasks>

<task>
  <name>Task 1: Commit candidate and lifecycle benchmark example configs</name>
  <files>
    materials-discovery/configs/systems/al_cu_fe_llm_adapted_candidate.yaml,
    materials-discovery/configs/llm/al_cu_fe_checkpoint_lifecycle_benchmark.yaml
  </files>
  <action>
Add a committed explicit-pin candidate config for `Al-Cu-Fe` and a shared
benchmark spec that compares baseline local, promoted default, and candidate
checkpoint generation on one acceptance-pack context.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py -x -v</automated>
  </verify>
</task>

<task>
  <name>Task 2: Prove the lifecycle benchmark workflow offline</name>
  <files>
    materials-discovery/tests/test_real_mode_pipeline.py
  </files>
  <action>
Extend the real-mode offline benchmark proof so it stages a promoted
checkpoint, a candidate checkpoint in the same family, and the baseline local
lane, then verifies the benchmark summary recommends promotion or retention
from structured target roles.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py -x -v</automated>
  </verify>
</task>

</tasks>
