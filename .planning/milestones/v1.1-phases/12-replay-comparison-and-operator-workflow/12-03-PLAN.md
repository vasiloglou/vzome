---
phase: 12-replay-comparison-and-operator-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - materials-discovery/RUNBOOK.md
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/tests/test_real_mode_pipeline.py
  - materials-discovery/tests/test_llm_campaign_lineage.py
autonomous: true
requirements: [LLM-09, LLM-11, OPS-07]

must_haves:
  truths:
    - "Phase 12 makes the closed-loop path usable by operators, not just technically available."
    - "Replay and compare stay additive to the existing suggest/approve/launch workflow."
    - "Docs must explain safe defaults and strict replay clearly enough that operators do not infer hidden override behavior."
    - "Regression coverage must protect the full workflow boundary from dry-run suggestion through replay and compare."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/RUNBOOK.md"
      provides: "End-to-end operator workflow for suggest, approve, launch, replay, compare, and interpretation"
    - path: "materials-discovery/developers-docs/llm-integration.md"
      provides: "Deep-dive architecture and artifact references for replay and compare"
    - path: "materials-discovery/developers-docs/pipeline-stages.md"
      provides: "CLI contract documentation for llm-replay and llm-compare"
    - path: "materials-discovery/tests/test_real_mode_pipeline.py"
      provides: "Offline workflow regression for the full closed-loop operator path"
  key_links:
    - from: "materials-discovery/RUNBOOK.md"
      to: "materials-discovery/developers-docs/pipeline-stages.md"
      via: "the runbook must align exactly with the CLI contract surface"
      pattern: "llm-suggest|llm-approve|llm-launch|llm-replay|llm-compare"
    - from: "materials-discovery/tests/test_real_mode_pipeline.py"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "the end-to-end regression should drive the actual commands, not internal helpers only"
      pattern: "llm-replay|llm-compare"
    - from: "materials-discovery/tests/test_llm_campaign_lineage.py"
      to: "materials-discovery/src/materials_discovery/llm/generate.py"
      via: "replayed outputs must keep additive llm_campaign lineage without breaking current launch lineage"
      pattern: "replay_of_launch_id|replay_of_launch_summary_path"
---

<objective>
Prove the Phase 12 operator workflow end to end and document it clearly.

Purpose: finish the milestone by making replay and comparison not only
implemented, but safe and understandable for day-to-day operator use.

Output: updated runbook and developer docs plus offline regression coverage for
the full suggest -> approve -> launch -> replay -> compare path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-CONTEXT.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-VALIDATION.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-01-PLAN.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-02-PLAN.md

@materials-discovery/RUNBOOK.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/tests/test_real_mode_pipeline.py
@materials-discovery/tests/test_llm_campaign_lineage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add offline regression coverage for the full replay/compare operator path</name>
  <files>
    materials-discovery/tests/test_real_mode_pipeline.py,
    materials-discovery/tests/test_llm_campaign_lineage.py
  </files>
  <read_first>
    materials-discovery/tests/test_real_mode_pipeline.py,
    materials-discovery/tests/test_llm_campaign_lineage.py,
    materials-discovery/tests/test_llm_approve_cli.py,
    materials-discovery/tests/test_llm_launch_cli.py,
    materials-discovery/tests/test_llm_suggest_cli.py,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the offline workflow can produce a dry-run proposal, approve it, launch it, replay that launch, and compare the replayed launch without live providers or Java
    - Test 2: replayed launches preserve additive `llm_campaign` lineage with `replay_of_launch_id`
    - Test 3: compare writes `outcome_snapshot.json` and the comparison artifact for the targeted launch
    - Test 4: the workflow still treats approval and launch as separate steps
    - Test 5: missing downstream metrics in the compare step are explicit and do not break the workflow
  </behavior>
  <action>
Add one offline end-to-end regression path across:
- `materials-discovery/tests/test_real_mode_pipeline.py`
- `materials-discovery/tests/test_llm_campaign_lineage.py`

Concrete regression flow:
1. Build or reuse a synthetic acceptance-pack fixture.
2. Run `mdisc llm-suggest --acceptance-pack ...`.
3. Run `mdisc llm-approve --proposal ... --decision approved --operator ... --config ...`.
4. Run `mdisc llm-launch --campaign-spec ...`.
5. Capture the first launch summary path.
6. Run `mdisc llm-replay --launch-summary ...`.
7. Capture the replay launch summary path.
8. Run `mdisc llm-compare --launch-summary <replay_launch_summary_path>`.
9. Assert the comparison and outcome-snapshot artifacts exist and that replay
   lineage is present in the replayed run outputs.

Use the existing mock LLM config and committed fixtures only. Do not add live
provider calls or Java-dependent assets.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py -k "llm_replay or llm_compare or campaign" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/tests/test_real_mode_pipeline.py contains a Phase 12 replay/compare workflow regression
    - materials-discovery/tests/test_llm_campaign_lineage.py asserts replay lineage fields
    - the Phase 12 E2E regression command exits 0
  </acceptance_criteria>
  <done>The repo has an offline proof that the full closed-loop operator workflow now extends through replay and compare.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update the runbook and developer docs for the full closed-loop operator workflow</name>
  <files>
    materials-discovery/RUNBOOK.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md
  </files>
  <read_first>
    materials-discovery/RUNBOOK.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/src/materials_discovery/cli.py,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: docs explain `llm-replay` as strict launch-artifact replay with no behavioral overrides
    - Test 2: docs explain `llm-compare` baselines: acceptance pack always, prior launch when available
    - Test 3: the runbook shows the exact command sequence for suggest, approve, launch, replay, and compare
    - Test 4: docs explain where comparison artifacts and outcome snapshots live on disk
    - Test 5: docs explain safe defaults and missing-metric behavior clearly enough for operators to interpret partial comparisons
  </behavior>
  <action>
Update the operator-facing docs in additive sections only.

In `materials-discovery/RUNBOOK.md`, add a new closed-loop workflow section that
shows the exact command sequence:
- `mdisc llm-suggest --acceptance-pack ...`
- `mdisc llm-approve --proposal ... --decision approved --operator ... --config ...`
- `mdisc llm-launch --campaign-spec ...`
- `mdisc llm-replay --launch-summary ...`
- `mdisc llm-compare --launch-summary ...`

Document the on-disk audit trail:
- `campaign_spec.json`
- `launches/{launch_id}/resolved_launch.json`
- `launches/{launch_id}/launch_summary.json`
- `launches/{launch_id}/outcome_snapshot.json`
- `comparisons/{comparison_id}.json`

In `materials-discovery/developers-docs/llm-integration.md`, extend the Phase
11/12 sections to explain:
- why replay uses the full launch bundle
- why replay records current-vs-source config hash drift but does not expose
  override knobs
- why comparison writes immutable outcome snapshots

In `materials-discovery/developers-docs/pipeline-stages.md`, add CLI contract
sections for:
- `mdisc llm-replay`
- `mdisc llm-compare`

The docs must explicitly say:
- replay is strict in Phase 12
- compare always uses the acceptance-pack baseline
- prior-launch comparison is included when available
- missing downstream metrics are reported explicitly, not treated as zero
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py -k "llm_replay or llm_compare or campaign" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/RUNBOOK.md contains "llm-replay"
    - materials-discovery/RUNBOOK.md contains "llm-compare"
    - materials-discovery/developers-docs/llm-integration.md contains "Phase 12"
    - materials-discovery/developers-docs/pipeline-stages.md contains "mdisc llm-replay"
    - materials-discovery/developers-docs/pipeline-stages.md contains "mdisc llm-compare"
  </acceptance_criteria>
  <done>Operators and future phases now have a clear, accurate, end-to-end replay/compare workflow reference.</done>
</task>

</tasks>

