---
phase: 11-closed-loop-campaign-execution-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - materials-discovery/src/materials_discovery/common/manifest.py
  - materials-discovery/src/materials_discovery/common/pipeline_manifest.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/tests/test_llm_campaign_lineage.py
  - materials-discovery/tests/test_report.py
  - materials-discovery/tests/test_real_mode_pipeline.py
autonomous: true
requirements: [LLM-10, OPS-06]

must_haves:
  truths:
    - "Phase 11 does not auto-run the downstream pipeline, but campaign lineage still has to survive when operators run the standard stage commands afterward."
    - "Standard CandidateRecord JSONL and stage manifests remain the integration surface for downstream steps."
    - "Campaign lineage must be additive: no benchmark-context or source-registry lineage from earlier phases should be broken."
    - "The campaign launch wrapper is only complete when later stage manifests can still be traced back to campaign spec, approval, and acceptance-pack lineage."
    - "Docs should explain llm-launch and downstream continuation, but the full operator runbook remains a later phase."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/common/manifest.py"
      provides: "Shared manifest support for additive source lineage on launched LLM runs"
    - path: "materials-discovery/src/materials_discovery/common/pipeline_manifest.py"
      provides: "Pipeline-manifest lineage support for campaign-launched runs"
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Downstream stage manifest propagation and campaign-aware report/pipeline outputs"
    - path: "materials-discovery/tests/test_llm_campaign_lineage.py"
      provides: "Focused propagation coverage for stage manifests after llm-launch"
    - path: "materials-discovery/tests/test_real_mode_pipeline.py"
      provides: "E2E compatibility proof for launched candidates continuing through existing stages"
  key_links:
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/common/manifest.py"
      via: "screen, hifi-validate, hifi-rank, active-learn, and report must pass campaign lineage into build_manifest"
      pattern: "build_manifest|source_lineage"
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/common/pipeline_manifest.py"
      via: "the final pipeline manifest should preserve the same additive campaign lineage"
      pattern: "build_pipeline_manifest"
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "downstream stage manifests stay standard ArtifactManifest objects with additive source_lineage only"
      pattern: "ArtifactManifest|CandidateRecord|source_lineage"
---

<objective>
Propagate campaign lineage through downstream artifacts and prove that launched
candidates continue through the existing pipeline without format breaks.

Purpose: satisfy the Phase 11 lineage and compatibility requirements after the
launch command exists, while keeping the rest of the pipeline operator-driven
and standard-artifact-based.

Output: additive downstream manifest propagation, campaign-aware pipeline
manifest support, focused lineage tests, and doc updates for the new launch +
manual continuation flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-closed-loop-campaign-execution-bridge/11-CONTEXT.md
@.planning/phases/11-closed-loop-campaign-execution-bridge/11-RESEARCH.md
@.planning/phases/11-closed-loop-campaign-execution-bridge/11-VALIDATION.md
@.planning/phases/11-closed-loop-campaign-execution-bridge/11-01-PLAN.md
@.planning/phases/11-closed-loop-campaign-execution-bridge/11-02-PLAN.md

@materials-discovery/src/materials_discovery/common/manifest.py
@materials-discovery/src/materials_discovery/common/pipeline_manifest.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/generate.py
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate campaign lineage into downstream manifests and pipeline artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/common/manifest.py,
    materials-discovery/src/materials_discovery/common/pipeline_manifest.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_campaign_lineage.py,
    materials-discovery/tests/test_report.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/manifest.py,
    materials-discovery/src/materials_discovery/common/pipeline_manifest.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/tests/test_report.py,
    .planning/phases/11-closed-loop-campaign-execution-bridge/11-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: downstream `screen` manifests can carry campaign lineage when the input candidates were produced by llm-launch
    - Test 2: `hifi-validate`, `hifi-rank`, `active-learn`, and `report` manifests preserve the same additive campaign lineage rather than dropping it
    - Test 3: the final pipeline manifest can also carry campaign lineage
    - Test 4: report or report-adjacent payloads remain valid and do not lose existing benchmark-context behavior
    - Test 5: non-campaign runs still produce valid manifests with `source_lineage=None`
    - Test 6: downstream helpers do not duplicate the same `llm_campaign` lineage payload when later stages re-read launched candidates
  </behavior>
  <action>
Add a shared downstream-lineage propagation path.

In `materials-discovery/src/materials_discovery/cli.py`, create a helper that
can derive campaign lineage from the current system's standard artifacts:
- prefer campaign-aware data from the candidates file's `provenance.llm_campaign`
- fall back to the current llm-generate manifest's `source_lineage` if needed
- normalize the result so repeated downstream stages do not wrap or duplicate the same `llm_campaign` payload
- return `None` when no campaign lineage is present

Use that helper in the downstream commands:
- `screen`
- `hifi-validate`
- `hifi-rank`
- `active-learn`
- `report`

For each command above, pass the additive lineage into `build_manifest(...)` via
`source_lineage=...` without removing any existing benchmark-context handling.

Update `materials-discovery/src/materials_discovery/common/pipeline_manifest.py`
so `build_pipeline_manifest(...)` also accepts `source_lineage: dict[str, object] | None = None`
and writes it into the returned `ArtifactManifest`.

Use the same campaign lineage when creating the pipeline manifest in `report`.

Create `materials-discovery/tests/test_llm_campaign_lineage.py` with focused
coverage for manifest propagation and update `materials-discovery/tests/test_report.py`
only where needed to keep report payloads/manifests valid.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_campaign_lineage.py tests/test_report.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/common/pipeline_manifest.py accepts source_lineage
    - materials-discovery/src/materials_discovery/cli.py passes source_lineage into downstream build_manifest calls
    - downstream lineage propagation avoids duplicating the same llm_campaign payload across stages
    - launched candidate lineage survives into downstream manifest JSON
    - the lineage/report regression command exits 0
  </acceptance_criteria>
  <done>Campaign lineage now survives beyond launch and is visible in downstream stage and pipeline manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove end-to-end compatibility for launched campaigns and document the flow</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_real_mode_pipeline.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_real_mode_pipeline.py,
    .planning/phases/11-closed-loop-campaign-execution-bridge/11-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: an approved campaign spec can launch through llm-launch and produce standard candidates JSONL
    - Test 2: those candidates can continue through at least `screen`, and the resulting manifests still preserve campaign lineage
    - Test 3: docs explain that Phase 11 adds `llm-launch`, keeps `llm-approve` separate, and still expects manual downstream continuation
    - Test 4: docs explain the failure posture: partial artifacts are preserved and Phase 11 has no resume path
  </behavior>
  <action>
Add an offline/mock end-to-end regression in
`materials-discovery/tests/test_real_mode_pipeline.py` for the campaign-launch lane.

Concrete regression flow:
- build or reuse an approved campaign spec fixture for `al_cu_fe_llm_mock.yaml`
- run `mdisc llm-launch --campaign-spec ...`
- assert that the standard candidates file exists and contains standard `CandidateRecord` rows
- run `mdisc screen --config ...`
- assert that both the llm-launch wrapper artifacts and the screen manifest exist
- assert that the screen manifest still carries the campaign lineage

Do not depend on live provider calls or Java.
Use the existing mock LLM path and committed system configs.

Update docs in additive sections only:
- `materials-discovery/developers-docs/llm-integration.md`
- `materials-discovery/developers-docs/pipeline-stages.md`

Document:
- `mdisc llm-launch --campaign-spec ...`
- the launch wrapper artifact paths under `data/llm_campaigns/{campaign_id}/launches/{launch_id}/`
- the fact that the launched run still writes standard candidates, manifests, and llm run artifacts
- the fact that later stages remain manual/operator-driven in Phase 11
- the failure posture: partial artifacts preserved, no resume yet
- a short `Lineage Audit` explanation showing how to trace a downstream manifest or report entry back to `campaign_spec.json`, the approval artifact, and the launch wrapper directory on disk
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py -k "campaign or llm_launch" -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/tests/test_real_mode_pipeline.py contains a Phase 11 llm-launch compatibility regression
    - developers-docs/llm-integration.md documents llm-launch and its artifact layout
    - developers-docs/pipeline-stages.md documents manual downstream continuation after launch
    - docs include a Lineage Audit explanation for tracing downstream artifacts back to the campaign spec
    - the llm-launch E2E regression command exits 0
  </acceptance_criteria>
  <done>The Phase 11 launch bridge is proven end to end and documented clearly enough for operators and later phases.</done>
</task>

</tasks>
