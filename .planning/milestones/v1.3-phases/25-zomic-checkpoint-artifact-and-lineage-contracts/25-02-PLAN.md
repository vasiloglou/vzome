# Phase 25 Plan 02

## Goal

Make checkpoint registration operable through the CLI and reusable through one
shared lane resolver.

## Tasks

1. Add `llm/checkpoints.py` for registration and lane-resolution logic.
2. Expose `mdisc llm-register-checkpoint --spec ...`.
3. Preserve backward compatibility for older lanes that use `checkpoint_id`
   only as metadata.
