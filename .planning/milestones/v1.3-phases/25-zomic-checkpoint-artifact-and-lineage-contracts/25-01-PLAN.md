# Phase 25 Plan 01

## Goal

Add typed checkpoint registration and lineage models plus deterministic storage
paths for adapted checkpoints.

## Tasks

1. Extend the LLM schema with checkpoint registration and lineage models.
2. Add checkpoint storage helpers under `data/llm_checkpoints/`.
3. Lock the contract with focused registry tests before integration work.
