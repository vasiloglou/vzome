---
phase: 03-reference-phase-integration-with-current-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
requirements:
  - DATA-05
  - PIPE-01
  - OPS-03
files_modified:
  - materials-discovery/tests/test_real_mode_pipeline.py
  - materials-discovery/tests/test_hull_proxy.py
  - materials-discovery/tests/test_report.py
  - materials-discovery/tests/test_ingest_source_registry.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - Phase 3 is not complete if source-backed ingest works in isolation but the downstream no-DFT pipeline breaks.
    - All Phase 3 regression coverage must stay offline and deterministic.
    - The new bridge must keep the no-DFT boundary explicit by stopping at staged/projection work during ingest.
    - Every execution task that changes `materials-discovery/` must update `materials-discovery/Progress.md` per `AGENTS.md`.
  artifacts:
    - source-backed real-mode pipeline regression coverage
    - targeted downstream compatibility tests for reference-phase consumers
    - proof that the bridge path stays offline and no-DFT during ingest
  key_links:
    - .planning/phases/03-reference-phase-integration-with-current-pipeline/03-CONTEXT.md
    - .planning/phases/03-reference-phase-integration-with-current-pipeline/03-RESEARCH.md
    - .planning/phases/03-reference-phase-integration-with-current-pipeline/03-VALIDATION.md
    - materials-discovery/tests/test_real_mode_pipeline.py
    - materials-discovery/tests/test_hull_proxy.py
    - materials-discovery/tests/test_report.py
    - materials-discovery/src/materials_discovery/hifi_digital/hull_proxy.py
    - materials-discovery/src/materials_discovery/hifi_digital/xrd_validate.py
---

# Phase 3 Plan 03: Prove Pipeline Compatibility And No-DFT Guardrails

## Objective

- **What:** Add the regression coverage that proves the new source-backed ingest
  path keeps the current discovery pipeline green and preserves the no-DFT
  boundary.
- **Why:** `PIPE-01` is about the whole discovery workflow, not only ingest.
- **Output:** End-to-end bridge-path pipeline tests and downstream
  compatibility checks that fail loudly if richer reference-phase data breaks
  the existing pipeline.

## Context

@.planning/phases/03-reference-phase-integration-with-current-pipeline/03-CONTEXT.md
@.planning/phases/03-reference-phase-integration-with-current-pipeline/03-RESEARCH.md
@.planning/phases/03-reference-phase-integration-with-current-pipeline/03-VALIDATION.md
@materials-discovery/tests/test_real_mode_pipeline.py
@materials-discovery/tests/test_hull_proxy.py
@materials-discovery/tests/test_report.py
@materials-discovery/src/materials_discovery/hifi_digital/hull_proxy.py
@materials-discovery/src/materials_discovery/hifi_digital/xrd_validate.py

## Tasks

<task type="code">
  <name>Add a source-backed real-mode pipeline smoke test</name>
  <files>materials-discovery/tests/test_real_mode_pipeline.py, materials-discovery/tests/test_ingest_source_registry.py</files>
  <read_first>
    materials-discovery/tests/test_real_mode_pipeline.py
    materials-discovery/tests/test_ingest_source_registry.py
    materials-discovery/src/materials_discovery/cli.py
    materials-discovery/src/materials_discovery/data_sources/runtime.py
    materials-discovery/src/materials_discovery/data_sources/projection.py
  </read_first>
  <action>Extend the real-mode pipeline regression so at least one offline config path is built dynamically inside the test with `tmp_path` and uses `backend.ingest_adapter: source_registry_v1` plus a thin local snapshot fixture or inline payload instead of the legacy HYPOD-X backend. Do not commit a new permanent YAML config just for this test. Mark the bridge-backed path as `@pytest.mark.integration` consistent with the existing real-mode pipeline tests. The test should run `ingest -> generate -> screen -> hifi-validate -> hifi-rank -> active-learn -> report`, assert the usual manifests/calibration artifacts exist, and confirm the ingest manifest includes source lineage references. Keep the setup fully local and deterministic.</action>
  <acceptance_criteria>
    - `materials-discovery/tests/test_real_mode_pipeline.py` includes a source-registry-backed real-mode flow.
    - The source-registry-backed flow builds its config dynamically in-test rather than depending on a newly committed system YAML.
    - The source-backed flow runs from ingest through report without network access.
    - The source-backed ingest manifest asserts on source lineage fields in addition to the normal stage metadata.
  </acceptance_criteria>
  <verify>Run `cd materials-discovery && uv run pytest tests/test_real_mode_pipeline.py` and confirm the bridge-backed pipeline path passes.</verify>
  <done>Phase 3 has end-to-end proof that richer ingested reference-phase data does not break the current no-DFT workflow.</done>
</task>

<task type="code">
  <name>Add targeted downstream compatibility coverage for projected reference phases</name>
  <files>materials-discovery/tests/test_hull_proxy.py, materials-discovery/tests/test_report.py</files>
  <read_first>
    materials-discovery/tests/test_hull_proxy.py
    materials-discovery/tests/test_report.py
    materials-discovery/src/materials_discovery/hifi_digital/hull_proxy.py
    materials-discovery/src/materials_discovery/hifi_digital/xrd_validate.py
    materials-discovery/src/materials_discovery/data_sources/projection.py
  </read_first>
  <action>Extend the downstream tests so projected reference-phase outputs with additive metadata are exercised directly. In `test_hull_proxy.py`, generate the processed JSONL inline by calling the Plan 01 projection helper on thin canonical source records rather than committing a separate processed fixture file, then confirm the hull proxy still chooses reference phases correctly when rows contain enriched metadata. In `test_report.py`, keep the report pipeline green after a source-backed ingest or equivalent projected processed input setup. Use the tests to guard against accidental assumptions that processed rows only ever come from the legacy HYPOD-X path.</action>
  <acceptance_criteria>
    - `materials-discovery/tests/test_hull_proxy.py` includes at least one assertion path that exercises projected/enriched reference-phase rows.
    - `materials-discovery/tests/test_hull_proxy.py` generates the projected processed input inline rather than relying on a newly committed processed fixture file.
    - `materials-discovery/tests/test_report.py` stays green after a source-backed or projected-reference ingest setup.
    - `cd materials-discovery && uv run pytest tests/test_hull_proxy.py tests/test_report.py` exits `0`.
  </acceptance_criteria>
  <verify>Run the focused pytest command above and then re-run `cd materials-discovery && uv run pytest tests/test_real_mode_pipeline.py` to ensure downstream and end-to-end expectations agree.</verify>
  <done>The downstream reference-phase consumers are explicitly protected against richer processed rows coming from the new bridge path.</done>
</task>

<task type="code">
  <name>Lock the no-DFT boundary and offline bridge behavior in tests</name>
  <files>materials-discovery/tests/test_ingest_source_registry.py, materials-discovery/tests/test_real_mode_pipeline.py</files>
  <read_first>
    materials-discovery/tests/test_ingest_source_registry.py
    materials-discovery/tests/test_real_mode_pipeline.py
    materials-discovery/src/materials_discovery/cli.py
    .planning/phases/03-reference-phase-integration-with-current-pipeline/03-CONTEXT.md
    .planning/phases/03-reference-phase-integration-with-current-pipeline/03-RESEARCH.md
  </read_first>
  <action>Add assertions that the source-backed ingest path completes from local fixtures or inline payloads only and does not invoke committee/phonon/md/xrd adapters during ingest. The test can do this through monkeypatching, sentinel flags, or import guards around the downstream adapters so the bridge path will fail if it accidentally crosses the no-DFT boundary. Keep these checks lightweight and deterministic, and keep `tests/test_ingest.py` in the final verification sweep as the baseline determinism anchor for the legacy processed ingest path.</action>
  <acceptance_criteria>
    - The bridge test suite contains an explicit no-DFT guard, not only an implicit assumption.
    - The bridge path is verified against local payloads or inline responses rather than live provider calls.
    - `cd materials-discovery && uv run pytest tests/test_ingest_source_registry.py tests/test_real_mode_pipeline.py tests/test_ingest.py` exits `0`.
  </acceptance_criteria>
  <verify>Run the focused pytest command above and confirm the bridge path remains offline and purely ingest/projection-oriented while legacy ingest determinism still passes.</verify>
  <done>Phase 3 has regression protection for the program’s required no-DFT ingest boundary.</done>
</task>

## Verification

Verify this plan by checking:

1. There is at least one end-to-end real-mode pipeline test using the new
   source-registry bridge.
2. Downstream reference-phase consumers are tested against projected/enriched
   processed rows.
3. The bridge path is explicitly guarded against accidental DFT/high-fidelity
   work during ingest.
4. Legacy ingest determinism still passes in the final Phase 3 verification sweep.

## Success Criteria

- [ ] Source-backed real-mode pipeline smoke coverage exists and passes.
- [ ] Downstream reference-phase consumers are protected against richer processed rows.
- [ ] The source bridge is verified to stay offline and no-DFT during ingest.
- [ ] Phase 3 verification now covers the whole pipeline impact, not only ingest success.
