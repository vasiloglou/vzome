---
phase: 06-zomic-training-corpus-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/manifests.py
  - materials-discovery/configs/llm/corpus_v1.yaml
  - materials-discovery/tests/test_llm_corpus_schema.py
  - materials-discovery/tests/test_llm_corpus_storage.py
  - materials-discovery/tests/test_llm_corpus_manifest.py
autonomous: true
requirements: [LLM-01]

must_haves:
  truths:
    - "Phase 6 introduces an additive llm corpus package under src/materials_discovery/llm rather than overloading existing generator or data_sources contracts"
    - "A dedicated corpus build config exists and can describe build_id, enabled source families, systems, and QA thresholds"
    - "Corpus artifacts live under data/llm_corpus/{build_id}/ with deterministic paths for syntax, materials, rejects, QA, inventory, and manifest outputs"
    - "Corpus contracts carry explicit fidelity and release-tier metadata, including a neutral pending state before QA promotes examples into gold, silver, or reject tiers"
    - "Manifest and lineage paths remain workspace-relative and follow the repo's existing file-backed conventions"
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Pydantic contracts for corpus config, inventory rows, typed validation state, conversion trace metadata, manifests, QA summaries, and build summary"
      exports: ["CorpusBuildConfig", "CorpusInventoryRow", "CorpusValidationState", "CorpusConversionTrace", "CorpusExample", "CorpusManifest", "CorpusQaSummary", "CorpusBuildSummary"]
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic path helpers for llm corpus build directories and output files"
      exports: ["corpus_build_dir", "syntax_corpus_path", "materials_corpus_path", "corpus_manifest_path", "corpus_qa_path", "corpus_inventory_path", "corpus_rejects_path"]
    - path: "materials-discovery/src/materials_discovery/llm/manifests.py"
      provides: "Manifest builder/writer and build fingerprint logic"
      exports: ["build_corpus_manifest", "write_corpus_manifest", "corpus_build_fingerprint"]
    - path: "materials-discovery/configs/llm/corpus_v1.yaml"
      provides: "Committed example corpus build config for reproducible operator runs"
    - path: "materials-discovery/tests/test_llm_corpus_schema.py"
      provides: "Schema validation coverage for core Phase 6 contracts"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/storage.py"
      to: "materials-discovery/src/materials_discovery/common/io.py"
      via: "workspace_root and ensure_parent path conventions"
      pattern: "workspace_root|ensure_parent"
    - from: "materials-discovery/src/materials_discovery/llm/manifests.py"
      to: "materials-discovery/src/materials_discovery/common/manifest.py"
      via: "reuses file hashing and manifest-writing posture"
      pattern: "file_sha256|write_json_object"
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "references CandidateRecord-compatible source context without modifying CandidateRecord yet"
      pattern: "CandidateRecord|QPhiCoord"
---

<objective>
Create the foundation contracts and storage layout for Phase 6.

Purpose: Establish the additive `llm/` package, the dedicated corpus build
config, and the deterministic artifact/manifest contract that all later Phase 6
work will depend on. This is the lowest-blast-radius way to start the LLM
corpus pipeline while keeping later conversion and builder work concrete.

Output: corpus config/schema/storage/manifests modules, a committed
`configs/llm/corpus_v1.yaml`, and unit tests that lock the artifact and
metadata contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-RESEARCH.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-VALIDATION.md

@materials-discovery/src/materials_discovery/common/io.py
@materials-discovery/src/materials_discovery/common/manifest.py
@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/data_sources/schema.py
@materials-discovery/src/materials_discovery/data_sources/storage.py
@materials-discovery/developers-docs/architecture.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/zomic-llm-data-plan.md

<interfaces>
From common/io.py:
```python
def workspace_root() -> Path: ...
def ensure_parent(path: Path) -> None: ...
def write_json_object(record: dict[str, Any], path: Path) -> None: ...
```

From common/manifest.py:
```python
def file_sha256(path: Path) -> str: ...
```

From common/schema.py:
```python
QPhiCoord = tuple[QPhiPair, QPhiPair, QPhiPair]
class CandidateRecord(BaseModel): ...
```

From data_sources/storage.py:
```python
def workspace_relative(path: Path) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define corpus build config and example contracts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/configs/llm/corpus_v1.yaml,
    materials-discovery/tests/test_llm_corpus_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/data_sources/schema.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/zomic-llm-data-plan.md,
    .planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: CorpusBuildConfig validates a build_id, source-family toggles, systems list, and gold/silver thresholds
    - Test 2: CorpusExample validates required provenance, source family, fidelity tier, pending release tier, and typed validation state fields
    - Test 3: CorpusExample rejects missing source_id, empty zomic_text, and invalid tier strings
    - Test 4: CorpusInventoryRow and CorpusBuildSummary validate record locators, loader hints, output paths, and record counts
    - Test 5: The committed corpus_v1.yaml round-trips through CorpusBuildConfig.model_validate
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/__init__.py` exporting the Phase 6 public contracts.

Create `materials-discovery/src/materials_discovery/llm/schema.py` with these concrete contracts:
- enum-like Literal fields for:
  - `source_family`: `"repo_regression" | "repo_parts" | "materials_design" | "candidate_record" | "generated_export" | "canonical_source" | "reference_pack" | "pyqcstrc_projection"`
  - `fidelity_tier`: `"exact" | "anchored" | "approximate" | "heuristic"`
  - `release_tier`: `"pending" | "gold" | "silver" | "reject"`
  - `validation_status`: `"pending" | "passed" | "failed"`
- `CorpusBuildConfig` with fields:
  - `build_id: str`
  - `systems: list[str]`
  - `include_repo_regression: bool = true`
  - `include_repo_parts: bool = true`
  - `include_materials_designs: bool = true`
  - `include_candidate_records: bool = true`
  - `include_generated_exports: bool = true`
  - `include_canonical_sources: bool = true`
  - `include_reference_packs: bool = true`
  - `include_pyqcstrc_projection: bool = true`
  - `gold_min_sites: int`
  - `gold_max_sites: int`
  - `silver_max_sites: int`
  - `collision_threshold_angstrom: float`
  - `source_keys: list[str]`
  - `reference_pack_ids: list[str]`
- `CorpusInventoryRow` with fields:
  - `source_family: str`
  - `source_path: str`
  - `system: str | None`
  - `source_record_id: str`
  - `input_kind: str`
  - `record_locator: dict[str, Any]`
  - `loader_hint: str`
  - `metadata: dict[str, Any]`
- `CorpusProvenance` with fields:
  - `example_id`, `source_family`, `source_path`, `source_record_id`, `system`, `fidelity_tier`, `release_tier`, `builder_version`
- `CorpusValidationState` with fields:
  - `parse_status: str = "pending"`
  - `compile_status: str = "pending"`
  - `labels_valid: bool | None = None`
  - `collision_free: bool | None = None`
  - `site_count: int | None = None`
  - `geometry_equivalence: bool | None = None`
  - `geometry_error: float | None = None`
  - `raw_export_path: str | None = None`
  - `error_message: str | None = None`
- `CorpusConversionTrace` with fields:
  - `strategy: str`
  - `step_count: int`
  - `fidelity_reason: str`
  - `source_signature: str`
  - `unresolved_axes: list[str] = []`
- `CorpusExample` with fields:
  - `schema_version: str = "llm-corpus-example/v1"`
  - `provenance: CorpusProvenance`
  - `zomic_text: str`
  - `labels: list[str]`
  - `orbit_names: list[str]`
  - `composition: dict[str, float] | None`
  - `properties: dict[str, Any]`
  - `validation: CorpusValidationState`
  - `conversion_trace: CorpusConversionTrace | None`
- `CorpusManifest`, `CorpusQaSummary`, and `CorpusBuildSummary` with explicit counts and workspace-relative output paths.

Add validators that:
- reject blank build_id and example_id
- reject blank zomic_text
- normalize system names and source_keys with surrounding whitespace removed
- require inventory rows to include a non-empty `loader_hint` and `record_locator`
- default newly constructed examples to `release_tier="pending"` until QA promotes them
- require non-pending release tiers to stay consistent with fidelity_tier (for example, `gold` cannot pair with `heuristic`)

Create `materials-discovery/configs/llm/corpus_v1.yaml` with these concrete keys and starter values:
```yaml
build_id: zomic_corpus_v1
systems: [Al-Cu-Fe, Sc-Zn, Ti-Zr-Ni]
include_repo_regression: true
include_repo_parts: true
include_materials_designs: true
include_candidate_records: true
include_generated_exports: true
include_canonical_sources: true
include_reference_packs: true
include_pyqcstrc_projection: true
gold_min_sites: 4
gold_max_sites: 200
silver_max_sites: 400
collision_threshold_angstrom: 0.1
source_keys: [hypodx, cod]
reference_pack_ids: [al_cu_fe_v1, sc_zn_v1]
```

Create `materials-discovery/tests/test_llm_corpus_schema.py` with tests for the behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_corpus_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class CorpusBuildConfig(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class CorpusInventoryRow(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class CorpusValidationState(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class CorpusExample(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "fidelity_tier"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "release_tier"
    - materials-discovery/configs/llm/corpus_v1.yaml exists
    - materials-discovery/configs/llm/corpus_v1.yaml contains "build_id: zomic_corpus_v1"
    - materials-discovery/tests/test_llm_corpus_schema.py contains at least 5 test functions
    - The schema test command exits 0
  </acceptance_criteria>
  <done>The corpus build config, example contract, fidelity/release tiers, and starter config file exist and validate cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add corpus storage layout and manifest helpers</name>
  <files>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/manifests.py,
    materials-discovery/tests/test_llm_corpus_storage.py,
    materials-discovery/tests/test_llm_corpus_manifest.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/io.py,
    materials-discovery/src/materials_discovery/common/manifest.py,
    materials-discovery/src/materials_discovery/data_sources/storage.py,
    materials-discovery/src/materials_discovery/llm/schema.py
  </read_first>
  <behavior>
    - Test 1: corpus_build_dir returns `data/llm_corpus/{build_id}`
    - Test 2: syntax_corpus_path, materials_corpus_path, rejects_path, inventory_path, qa_path, and manifest_path all resolve under the same build directory
    - Test 3: all public path helpers return workspace-relative paths when written into manifest payloads
    - Test 4: corpus_build_fingerprint is deterministic for the same config + counts
    - Test 5: write_corpus_manifest writes manifest.json with output hashes and workspace-relative artifact paths
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/storage.py` with concrete helpers:
- `corpus_build_dir(build_id: str, root: Path | None = None) -> Path`
- `syntax_corpus_path(build_id: str, root: Path | None = None) -> Path`
- `materials_corpus_path(build_id: str, root: Path | None = None) -> Path`
- `corpus_rejects_path(build_id: str, root: Path | None = None) -> Path`
- `corpus_inventory_path(build_id: str, root: Path | None = None) -> Path`
- `corpus_qa_path(build_id: str, root: Path | None = None) -> Path`
- `corpus_manifest_path(build_id: str, root: Path | None = None) -> Path`

All helpers must write under `workspace_root() / "data" / "llm_corpus" / build_id`.

Create `materials-discovery/src/materials_discovery/llm/manifests.py`:
- implement `corpus_build_fingerprint(config: CorpusBuildConfig, syntax_count: int, materials_count: int) -> str`
- implement `build_corpus_manifest(...) -> CorpusManifest`
- implement `write_corpus_manifest(manifest: CorpusManifest, output_path: Path) -> Path`
- include workspace-relative paths for:
  - syntax corpus
  - materials corpus
  - rejects
  - inventory
  - qa
- compute output hashes with `file_sha256` after files exist

Create `materials-discovery/tests/test_llm_corpus_storage.py` and `materials-discovery/tests/test_llm_corpus_manifest.py` with tests for all behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_corpus_storage.py tests/test_llm_corpus_manifest.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/storage.py contains "data\" / \"llm_corpus\"" or "data/llm_corpus"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def corpus_build_dir("
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def syntax_corpus_path("
    - materials-discovery/src/materials_discovery/llm/manifests.py contains "def build_corpus_manifest("
    - materials-discovery/src/materials_discovery/llm/manifests.py contains "def write_corpus_manifest("
    - materials-discovery/src/materials_discovery/llm/manifests.py contains "workspace_relative"
    - materials-discovery/tests/test_llm_corpus_storage.py contains at least 3 test functions
    - materials-discovery/tests/test_llm_corpus_manifest.py contains at least 3 test functions
    - The storage/manifest test command exits 0
  </acceptance_criteria>
  <done>The llm corpus build directory and all artifact paths are deterministic, and manifest writing preserves the repo's file-backed lineage conventions.</done>
</task>

</tasks>
