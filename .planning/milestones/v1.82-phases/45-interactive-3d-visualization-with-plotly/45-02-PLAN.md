---
phase: 45-interactive-3d-visualization-with-plotly
plan: "02"
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - materials-discovery/notebooks/guided_design_tutorial.ipynb
  - materials-discovery/Progress.md
autonomous: false
requirements: [VIZ-01, VIZ-02]
must_haves:
  truths:
    - "Running the notebook Section 4 produces an interactive plotly 3D scatter with 5 orbit-colored traces and hover labels"
    - "Running the notebook Section 4 produces a shell-decomposed Tsai cluster figure with 5 labeled shell layers and ConvexHull cages"
    - "When plotly is not installed, the notebook falls back gracefully to the HTML canvas viewer with a clear install message"
  artifacts:
    - path: "materials-discovery/notebooks/guided_design_tutorial.ipynb"
      provides: "Two new plotly figure cells in Section 4 after existing preview cell"
      contains: "orbit_figure"
    - path: "materials-discovery/notebooks/guided_design_tutorial.ipynb"
      provides: "Shell figure cell"
      contains: "shell_figure"
  key_links:
    - from: "materials-discovery/notebooks/guided_design_tutorial.ipynb"
      to: "materials-discovery/src/materials_discovery/visualization/plotly_3d.py"
      via: "from materials_discovery.visualization.plotly_3d import orbit_figure, shell_figure, load_orbit_library"
      pattern: "from materials_discovery\\.visualization\\.plotly_3d import"
    - from: "materials-discovery/notebooks/guided_design_tutorial.ipynb"
      to: "materials-discovery/data/prototypes/generated/sc_zn_tsai_bridge.json"
      via: "load_orbit_library(WORKDIR / 'data/prototypes/generated/sc_zn_tsai_bridge.json')"
      pattern: "sc_zn_tsai_bridge\\.json"
---

<objective>
Insert interactive plotly 3D visualization cells into the guided design tutorial notebook at Section 4, producing the orbit-colored scatter and shell-decomposed Tsai cluster figures that readers see when running the notebook.

Purpose: Connects the plotly_3d.py module (built in Plan 01) to the notebook, completing the user-facing deliverable for VIZ-01 and VIZ-02. The existing HTML canvas viewer remains as a fallback when plotly is not installed.

Output: Updated notebook with 2 new code cells and 1 new markdown cell in Section 4, visual confirmation that figures render correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-interactive-3d-visualization-with-plotly/45-CONTEXT.md
@.planning/phases/45-interactive-3d-visualization-with-plotly/45-RESEARCH.md
@.planning/phases/45-interactive-3d-visualization-with-plotly/45-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 output -->

From materials-discovery/src/materials_discovery/visualization/plotly_3d.py (created in Plan 01):
```python
def load_orbit_library(path: Path) -> dict:
    """Load the orbit library JSON produced by mdisc export-zomic."""
    ...

def orbit_figure(orbit_lib_data: dict) -> go.Figure:
    """Interactive 3D scatter with one Scatter3d trace per anchor orbit."""
    ...

def shell_figure(orbit_lib_data: dict) -> go.Figure:
    """Shell-decomposed figure with ConvexHull cages per shell."""
    ...
```

Notebook cell layout around Section 4 (current state):
- Cell index 12 (id=f484c001, markdown): "## 4. Programmatic Preview of the Checked Geometry" header
- Cell index 13 (id=4cbab4b3, code): `preview_summary = preview_checked_design()` — existing HTML viewer
- Cell index 14 (id=c2485826, markdown): "## 5. Generate and Screen Candidates" — next section

New cells insert AFTER cell index 13 (4cbab4b3) and BEFORE cell index 14 (c2485826).
The WORKDIR variable is already defined in cell 0 (438dc748) as `WORKDIR = Path(".")`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert plotly 3D figure cells into notebook Section 4</name>
  <read_first>
    - materials-discovery/notebooks/guided_design_tutorial.ipynb
    - materials-discovery/src/materials_discovery/visualization/plotly_3d.py
    - .planning/phases/45-interactive-3d-visualization-with-plotly/45-RESEARCH.md
    - .planning/phases/45-interactive-3d-visualization-with-plotly/45-01-SUMMARY.md
  </read_first>
  <files>
    materials-discovery/notebooks/guided_design_tutorial.ipynb,
    materials-discovery/Progress.md
  </files>
  <action>
Read the notebook JSON. Insert 3 new cells immediately AFTER cell index 13 (id=4cbab4b3, the `preview_checked_design()` cell) and BEFORE cell index 14 (id=c2485826, the Section 5 markdown).

**New cell A (markdown):** Insert after cell 13. Cell type: markdown.
```markdown
### Interactive 3D Orbit Visualization

The cells below render the same 100 anchor-library sites as interactive plotly
figures.  Hover over any site to see its label, anchor orbit, species, and
shell name.  If plotly is not installed the HTML canvas viewer above is the
fallback; install the `[viz]` extra to enable these figures:

```
uv pip install "materials-discovery[viz]"
```
```

**New cell B (code):** Insert after cell A. Cell type: code. Source:
```python
# 4.1 — Interactive orbit-colored scatter (VIZ-01)
try:
    from materials_discovery.visualization.plotly_3d import (
        orbit_figure,
        shell_figure,
        load_orbit_library,
    )
    _PLOTLY_AVAILABLE = True
except ImportError:
    _PLOTLY_AVAILABLE = False

if _PLOTLY_AVAILABLE:
    orbit_lib_data = load_orbit_library(
        WORKDIR / "data/prototypes/generated/sc_zn_tsai_bridge.json"
    )
    fig_orbit = orbit_figure(orbit_lib_data)
    fig_orbit.show(renderer="notebook_connected")
else:
    print("Install plotly for interactive 3D figures:")
    print('  uv pip install "materials-discovery[viz]"')
    print("Falling back to HTML canvas viewer above.")
```

**New cell C (code):** Insert after cell B. Cell type: code. Source:
```python
# 4.2 — Shell-decomposed Tsai cluster figure (VIZ-02)
if _PLOTLY_AVAILABLE:
    fig_shell = shell_figure(orbit_lib_data)
    fig_shell.show(renderer="notebook_connected")
```

Each new cell must have a unique `id` field (use `uuid.uuid4().hex[:8]` or similar 8-char hex). Set `"cell_type"` correctly (markdown vs code), `"metadata": {}`, `"outputs": []` for code cells. Use the notebook's existing `nbformat` and `nbformat_minor` versions.

Implementation approach: Use Python script or direct JSON manipulation to insert the cells. Read the notebook as JSON, find cell index 13 by scanning for id `4cbab4b3`, insert 3 new cells after it, write back. Preserve all existing cells, outputs, and metadata unchanged.

CRITICAL: Do NOT remove the existing `preview_checked_design()` cell (4cbab4b3) — it remains as the EMBED_PREVIEW fallback. The new plotly cells augment, not replace, the HTML viewer.

CRITICAL: Use `renderer="notebook_connected"` (not `renderer="notebook"`) to avoid embedding 3.5MB plotly.js per cell.

Update `materials-discovery/Progress.md` with notebook integration entry.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && python3 -c "
import json
nb = json.load(open('notebooks/guided_design_tutorial.ipynb'))
cells = nb['cells']
# Find cell 4cbab4b3 (existing preview)
preview_idx = next(i for i, c in enumerate(cells) if c.get('id') == '4cbab4b3')
# Next cells should be our new ones
assert cells[preview_idx + 1]['cell_type'] == 'markdown', 'Expected markdown cell after preview'
md_src = ''.join(cells[preview_idx + 1]['source'])
assert 'Interactive 3D' in md_src, 'Missing markdown header'
orbit_cell = cells[preview_idx + 2]
assert orbit_cell['cell_type'] == 'code', 'Expected code cell for orbit figure'
orbit_src = ''.join(orbit_cell['source'])
assert 'orbit_figure' in orbit_src, 'Missing orbit_figure call'
assert 'notebook_connected' in orbit_src, 'Missing notebook_connected renderer'
assert 'sc_zn_tsai_bridge.json' in orbit_src, 'Missing orbit lib path'
shell_cell = cells[preview_idx + 3]
assert shell_cell['cell_type'] == 'code', 'Expected code cell for shell figure'
shell_src = ''.join(shell_cell['source'])
assert 'shell_figure' in shell_src, 'Missing shell_figure call'
assert 'notebook_connected' in shell_src, 'Missing notebook_connected renderer in shell cell'
# Verify preview cell still exists
assert 'preview_checked_design' in ''.join(cells[preview_idx]['source']), 'Preview cell was removed!'
# Verify Section 5 still follows
sec5_idx = preview_idx + 4
assert '## 5.' in ''.join(cells[sec5_idx]['source']), 'Section 5 not in expected position'
print(f'ALL CHECKS PASSED: 3 new cells inserted after index {preview_idx}')
"</automated>
  </verify>
  <acceptance_criteria>
    - Notebook cell with id 4cbab4b3 still exists and contains "preview_checked_design"
    - A new markdown cell immediately after 4cbab4b3 contains "Interactive 3D"
    - A new code cell after the markdown contains "orbit_figure" and "notebook_connected" and "sc_zn_tsai_bridge.json"
    - A new code cell after the orbit cell contains "shell_figure" and "notebook_connected"
    - The cell immediately after the 3 new cells contains "## 5." (Section 5 header preserved)
    - No existing cells were removed or modified (cell count increased by exactly 3)
    - New code cells contain fallback print messages when _PLOTLY_AVAILABLE is False
    - grep "load_orbit_library" on the notebook file returns a match
  </acceptance_criteria>
  <done>
    Notebook has 3 new cells in Section 4: markdown intro, orbit scatter code cell, shell figure code cell. Existing HTML preview cell retained. Section 5 follows immediately after. Fallback message present when plotly unavailable. renderer="notebook_connected" used in both figure cells.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of interactive 3D figures in notebook</name>
  <files>materials-discovery/notebooks/guided_design_tutorial.ipynb</files>
  <action>
Human visually verifies the two interactive plotly 3D figures in the notebook.

What was built:
- Orbit-colored scatter showing all 100 anchor-library sites with 5 distinct colors (VIZ-01)
- Shell-decomposed Tsai cluster figure with polyhedral ConvexHull cages per shell (VIZ-02)

Steps to verify:
1. Open the notebook: `cd materials-discovery && uv run jupyter lab notebooks/guided_design_tutorial.ipynb`
2. Run cells 1-4 (Environment setup through the preview_checked_design cell)
3. Run the new orbit scatter cell (cell 4.1) and verify:
   - 5 orbit traces appear in the legend with distinct colors
   - Hovering a site shows text like "Zn7_01 (tsai_zn7) - Zn - Zn inner shell"
   - All sites are rendered (100 total) as uniform-size markers
   - Figure is interactive (rotate, zoom, pan)
4. Run the new shell figure cell (cell 4.2) and verify:
   - 5 shell layers appear in the legend (Shell 1 through Shell 5)
   - Semi-transparent polyhedral cages (ConvexHull faces) surround each shell's sites
   - Edge wireframes visible on cages
   - Clicking a legend entry toggles that shell's markers AND cage together
   - Shells are ordered innermost (Zn middle shell) to outermost (Zn outer shell)
5. Verify the existing HTML canvas viewer (cell 4cbab4b3) still works above the plotly cells

Resume signal: Type "approved" if both figures render correctly. Otherwise describe what is wrong.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_plotly_3d.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - User confirms orbit scatter figure shows 5 distinct orbit-colored traces
    - User confirms hover text includes site label, orbit name, species, and shell name
    - User confirms shell figure shows 5 toggleable shell layers with polyhedral cages
    - User confirms edge wireframes are visible on shell cages
    - User confirms legend toggling works (markers and cage toggle together)
    - User confirms existing HTML canvas viewer still renders above the plotly cells
  </acceptance_criteria>
  <done>User confirms both plotly 3D figures render correctly with expected colors, hover labels, interactivity, and shell toggling. Existing HTML viewer still functional.</done>
</task>

</tasks>

<verification>
1. Notebook JSON validation script (Task 1 automated verify) passes
2. Visual inspection confirms orbit scatter has 5 colored traces with hover text
3. Visual inspection confirms shell figure has 5 toggleable shell layers with cages
4. Existing HTML viewer still functional
5. `cd materials-discovery && uv run pytest tests/ -x -q --ignore=tests/test_llm_replay_core.py` -- no regressions
</verification>

<success_criteria>
- Notebook Section 4 shows 3 new cells (markdown + 2 code) after the existing preview cell
- Orbit scatter figure renders interactively with 5 orbit-colored traces and correct hover text
- Shell-decomposed figure renders with toggleable shell layers and ConvexHull cages
- Existing HTML canvas viewer still works
- Graceful fallback message when plotly is not installed
- User approves visual output
</success_criteria>

<output>
After completion, create `.planning/phases/45-interactive-3d-visualization-with-plotly/45-02-SUMMARY.md`
</output>
