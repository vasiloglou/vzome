---
phase: 34-benchmark-pack-and-freeze-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_translated_benchmark_schema.py
  - materials-discovery/Progress.md
autonomous: true
requirements: [LLM-31]

must_haves:
  truths:
    - "A frozen translated benchmark set can be described by one typed contract instead of ad hoc flags: source bundle manifests, system filters, one target family, allowed fidelity tiers, and representational-loss posture are all explicit."
    - "The benchmark-pack artifact has stable file-backed homes for the normalized freeze contract, manifest, included rows, and excluded rows under data/benchmarks/llm_external_sets/."
    - "The contract stays additive to shipped translation bundles by reusing TranslationBundleManifest and TranslationInventoryRow lineage rather than inventing a second source format."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed freeze-spec, manifest, included-row, excluded-row, exclusion-reason, and summary models for translated benchmark packs."
      contains: "class TranslatedBenchmarkSetSpec"
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic storage helpers for translated benchmark-pack artifacts."
      exports:
        - "translated_benchmark_set_dir"
        - "translated_benchmark_contract_path"
        - "translated_benchmark_manifest_path"
        - "translated_benchmark_included_path"
        - "translated_benchmark_excluded_path"
    - path: "materials-discovery/tests/test_llm_translated_benchmark_schema.py"
      provides: "Validator coverage for the new benchmark-pack contract and storage layout."
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/translation_bundle.py"
      via: "bundle lineage fields and translation inventory compatibility"
      pattern: "TranslationBundleManifest|TranslationInventoryRow"
    - from: "materials-discovery/src/materials_discovery/llm/storage.py"
      to: "materials-discovery/data/benchmarks/llm_external_sets"
      via: "deterministic benchmark-pack artifact layout"
      pattern: "llm_external_sets"
---

<objective>
Freeze the contract and storage foundation for translated benchmark packs.

Purpose: lock the exact file-backed shape that later freeze, inspect, and
benchmark phases will rely on, so inclusion and exclusion rules stay explicit
and reusable instead of being reconstructed from raw translation bundles.

Output: typed benchmark-pack models, deterministic storage helpers, public
exports, and focused schema/storage tests.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@AGENTS.md
@CLAUDE.md
@.planning/milestones/v1.5-phases/31-translation-contracts-and-representation-loss-semantics/31-02-SUMMARY.md
@.planning/milestones/v1.5-phases/33-cli-benchmark-hooks-and-operator-docs/33-01-SUMMARY.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/translation_bundle.py
@materials-discovery/developers-docs/llm-translation-runbook.md
@materials-discovery/Progress.md

<interfaces>
From materials-discovery/src/materials_discovery/llm/schema.py:
```python
class TranslationInventoryRow(BaseModel):
    export_id: str
    candidate_id: str
    system: str
    template_family: str
    target_family: TranslationTargetFamily
    target_format: TranslationTargetFormat
    fidelity_tier: TranslationFidelityTier
    loss_reasons: list[TranslationLossReason] = Field(default_factory=list)
    diagnostic_codes: list[TranslationDiagnosticCode] = Field(default_factory=list)
    composition: dict[str, float]
    payload_path: str
    payload_hash: str
    emitted_text: str

class TranslationBundleManifest(BaseModel):
    export_id: str
    created_at_utc: str
    input_path: str
    target_family: TranslationTargetFamily
    target_format: TranslationTargetFormat
    inventory_path: str
    payload_dir: str
    candidate_count: int
    exported_count: int
    lossy_count: int
    stage_manifest_path: str | None = None
    source_lineage: dict[str, Any] | None = None
    benchmark_context: dict[str, Any] | None = None
```

From materials-discovery/src/materials_discovery/llm/storage.py:
```python
def llm_translation_export_dir(export_id: str, root: Path | None = None) -> Path: ...
def llm_translation_manifest_path(export_id: str, root: Path | None = None) -> Path: ...
def llm_translation_inventory_path(export_id: str, root: Path | None = None) -> Path: ...
def llm_serving_benchmark_dir(benchmark_id: str, root: Path | None = None) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the translated benchmark freeze contract and exclusion vocabulary</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translated_benchmark_schema.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/translation_bundle.py,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/research/SUMMARY.md,
    .planning/milestones/v1.5-phases/31-translation-contracts-and-representation-loss-semantics/31-02-SUMMARY.md,
    .planning/milestones/v1.5-phases/33-cli-benchmark-hooks-and-operator-docs/33-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `TranslatedBenchmarkSetSpec` rejects blank benchmark IDs, blank bundle-manifest paths, empty bundle lists, and empty fidelity-tier selections.
    - Test 2: the contract exposes explicit fields for `systems`, `target_family`, `allowed_fidelity_tiers`, and `loss_posture` instead of hiding them in free-form dicts.
    - Test 3: `TranslatedBenchmarkIncludedRow` and `TranslatedBenchmarkExcludedRow` preserve source export ID, source bundle manifest path, candidate ID, fidelity tier, and loss reasons.
    - Test 4: `TranslatedBenchmarkSetManifest` carries the normalized contract path plus included/excluded inventory paths and count totals.
    - Test 5: exclusion reasons are typed, stable, and usable later by CLI inspect output without stringly typed branching.
  </behavior>
  <action>
Add the Phase 34 contract models to `materials-discovery/src/materials_discovery/llm/schema.py`.

Create these concrete types:
- `TranslatedBenchmarkLossPosture` with exactly `lossless_only`, `allow_explicit_loss`, and `lossy_only`.
- `TranslatedBenchmarkExclusionReason` with at least `system_not_selected`, `target_family_mismatch`, `fidelity_tier_not_selected`, `loss_posture_rejected`, and `duplicate_translation_row`.
- `TranslatedBenchmarkSetSpec` with `benchmark_set_id`, `bundle_manifest_paths`, optional `systems`, one `target_family`, `allowed_fidelity_tiers`, `loss_posture`, and optional operator note.
- `TranslatedBenchmarkIncludedRow` mirroring the shipped translation inventory surface plus `benchmark_set_id`, `source_export_id`, and `source_bundle_manifest_path`.
- `TranslatedBenchmarkExcludedRow` mirroring the same lineage fields plus `exclusion_reason` and `exclusion_detail`.
- `TranslatedBenchmarkSetManifest` with `contract_path`, `included_inventory_path`, `excluded_inventory_path`, `source_bundle_manifest_paths`, `source_export_ids`, `included_count`, `excluded_count`, `target_family`, `systems`, and the embedded filter contract.
- `TranslatedBenchmarkSetSummary` for CLI success output.

Keep this additive to shipped translation bundles:
- reuse the existing translation-family enums and loss-reason types
- do not create a second translation payload schema
- do not add target registration, runtime, or scorecard fields here

Re-export the new public models from `materials-discovery/src/materials_discovery/llm/__init__.py`.

Create `materials-discovery/tests/test_llm_translated_benchmark_schema.py` with focused validator coverage for the contract, row models, and manifest counts. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class TranslatedBenchmarkSetSpec" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class TranslatedBenchmarkIncludedRow" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class TranslatedBenchmarkExcludedRow" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class TranslatedBenchmarkSetManifest" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "lossless_only|allow_explicit_loss|lossy_only" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "system_not_selected|target_family_mismatch|fidelity_tier_not_selected|loss_posture_rejected|duplicate_translation_row" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "TranslatedBenchmarkSetSpec|TranslatedBenchmarkSetManifest" materials-discovery/src/materials_discovery/llm/__init__.py`
    - `rg -c "^def test_" materials-discovery/tests/test_llm_translated_benchmark_schema.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_schema.py -x -v`
  </acceptance_criteria>
  <done>The repo has one typed, test-backed contract for expressing a translated benchmark freeze and its inclusion/exclusion lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic storage helpers for the benchmark-pack artifact layout</name>
  <files>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/tests/test_llm_translated_benchmark_schema.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/translation_bundle.py,
    .planning/research/SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: storage helpers write under `data/benchmarks/llm_external_sets/{benchmark_set_id}/`.
    - Test 2: blank benchmark-set IDs fail through the existing artifact-id guard instead of creating ambiguous paths.
    - Test 3: the layout includes dedicated `freeze_contract.json`, `manifest.json`, `included.jsonl`, and `excluded.jsonl` files.
    - Test 4: the storage helpers do not reuse translation-bundle or serving-benchmark directories.
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/storage.py` with deterministic helpers for the Phase 34 artifact root:
- `translated_benchmark_set_dir(benchmark_set_id: str, root: Path | None = None) -> Path`
- `translated_benchmark_contract_path(benchmark_set_id: str, root: Path | None = None) -> Path`
- `translated_benchmark_manifest_path(benchmark_set_id: str, root: Path | None = None) -> Path`
- `translated_benchmark_included_path(benchmark_set_id: str, root: Path | None = None) -> Path`
- `translated_benchmark_excluded_path(benchmark_set_id: str, root: Path | None = None) -> Path`

Use the research-approved artifact root `data/benchmarks/llm_external_sets/{benchmark_set_id}/`.
Do not overload `data/llm_translation_exports/` or `data/benchmarks/llm_serving/`.

Expand `materials-discovery/tests/test_llm_translated_benchmark_schema.py` so the storage layout is locked together with the new contract. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def translated_benchmark_set_dir" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def translated_benchmark_contract_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def translated_benchmark_manifest_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def translated_benchmark_included_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def translated_benchmark_excluded_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "data\" / \"benchmarks\" / \"llm_external_sets\"" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "freeze_contract.json|included.jsonl|excluded.jsonl|manifest.json" materials-discovery/tests/test_llm_translated_benchmark_schema.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_schema.py -x -v`
  </acceptance_criteria>
  <done>The benchmark-pack file layout is deterministic, isolated from earlier artifact families, and ready for the freeze engine.</done>
</task>

</tasks>

<verification>
Run the focused schema/storage slice and leave Phase 34 with one stable contract
surface before any freeze logic is added:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_schema.py -x -v`
</verification>

<success_criteria>
Phase 34 has a typed, file-backed benchmark-pack contract that makes source
bundle lineage and filter rules explicit before any CLI or runtime wiring is
built.
</success_criteria>

<output>
After completion, create `.planning/phases/34-benchmark-pack-and-freeze-contract/34-01-SUMMARY.md`
</output>
