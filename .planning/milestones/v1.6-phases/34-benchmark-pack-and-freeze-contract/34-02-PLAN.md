---
phase: 34-benchmark-pack-and-freeze-contract
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - materials-discovery/src/materials_discovery/llm/translated_benchmark.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_translated_benchmark_freeze.py
  - materials-discovery/Progress.md
autonomous: true
requirements: [LLM-31]

must_haves:
  truths:
    - "A translated benchmark pack can be frozen directly from one or more shipped translation bundles by applying explicit system, target-family, fidelity-tier, and loss-posture rules."
    - "Every case that is not eligible ends up in an excluded inventory with a typed reason instead of disappearing silently."
    - "The frozen pack records the normalized freeze contract plus source bundle manifests and export IDs as durable lineage that later phases can reuse unchanged."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/translated_benchmark.py"
      provides: "Spec loader, freeze engine, duplicate-handling rules, and artifact writers for translated benchmark packs."
      exports:
        - "load_translated_benchmark_spec"
        - "freeze_translated_benchmark_set"
    - path: "materials-discovery/tests/test_llm_translated_benchmark_freeze.py"
      provides: "Freeze-core regression coverage for eligibility, exclusions, duplicate handling, and written artifacts."
    - path: "materials-discovery/src/materials_discovery/llm/__init__.py"
      provides: "Public Phase 34 freeze API exports."
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/translated_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "loads TranslationBundleManifest and TranslationInventoryRow, writes TranslatedBenchmarkSetManifest"
      pattern: "TranslationBundleManifest|TranslationInventoryRow|TranslatedBenchmarkSetManifest"
    - from: "materials-discovery/src/materials_discovery/llm/translated_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/storage.py"
      via: "writes freeze contract and included/excluded inventories"
      pattern: "translated_benchmark_contract_path|translated_benchmark_included_path|translated_benchmark_excluded_path"
---

<objective>
Implement the freeze engine that materializes one translated benchmark pack from
shipped translation bundles.

Purpose: make the benchmark slice reproducible and inspectable before later
phases add external target registration or comparative scorecards.

Output: one core freeze module, public exports, and focused tests for rule
evaluation, duplicate handling, and written artifact lineage.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@AGENTS.md
@CLAUDE.md
@.planning/phases/34-benchmark-pack-and-freeze-contract/34-01-PLAN.md
@.planning/milestones/v1.5-phases/31-translation-contracts-and-representation-loss-semantics/31-02-SUMMARY.md
@.planning/milestones/v1.5-phases/33-cli-benchmark-hooks-and-operator-docs/33-01-SUMMARY.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/translation_bundle.py
@materials-discovery/developers-docs/llm-translation-runbook.md
@materials-discovery/Progress.md

<interfaces>
From materials-discovery/src/materials_discovery/llm/translation_bundle.py:
```python
def export_translation_bundle(
    *,
    candidates: Sequence[CandidateRecord],
    input_path: Path,
    target_family: str,
    export_id: str,
    root: Path | None = None,
    stage_manifest_path: str | None = None,
    source_lineage: dict[str, Any] | None = None,
    benchmark_context: dict[str, Any] | None = None,
) -> TranslationExportSummary:
    ...
```

From Plan 01 contract:
```python
class TranslatedBenchmarkSetSpec(BaseModel): ...
class TranslatedBenchmarkIncludedRow(BaseModel): ...
class TranslatedBenchmarkExcludedRow(BaseModel): ...
class TranslatedBenchmarkSetManifest(BaseModel): ...
class TranslatedBenchmarkSetSummary(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the bundle-driven freeze engine and explicit exclusion rules</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/tests/test_llm_translated_benchmark_freeze.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/translation_bundle.py,
    .planning/phases/34-benchmark-pack-and-freeze-contract/34-01-PLAN.md,
    .planning/research/SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `freeze_translated_benchmark_set(...)` loads one or more translation bundle manifests plus their inventory rows and keeps only rows that satisfy `systems`, `target_family`, `allowed_fidelity_tiers`, and `loss_posture`.
    - Test 2: rows that fail a rule are written to the excluded inventory with the correct typed reason rather than being dropped silently.
    - Test 3: exact duplicate rows with the same candidate ID and payload hash are excluded deterministically as duplicates after the first kept row.
    - Test 4: conflicting duplicates with the same candidate ID but different payload hashes fail closed with a clear error naming both bundle sources.
    - Test 5: input ordering differences do not change the included or excluded row order because the freeze engine sorts deterministically by bundle path, candidate ID, and payload hash.
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/translated_benchmark.py` and implement the in-memory freeze engine.

Add these concrete functions:
- `load_translated_benchmark_spec(spec_path: Path) -> TranslatedBenchmarkSetSpec`
- `_load_translation_bundle(bundle_manifest_path: Path) -> tuple[TranslationBundleManifest, list[TranslationInventoryRow]]`
- `_evaluate_translation_row(...) -> tuple[bool, TranslatedBenchmarkExclusionReason | None, str | None]`
- `freeze_translated_benchmark_set(spec_path: Path, root: Path | None = None) -> TranslatedBenchmarkSetSummary`

Required behavior:
- load bundle manifests and inventory rows through the shipped translation-bundle models
- treat `target_family` as a hard filter for the final pack, even if a bundle manifest disagrees
- reject rows outside the selected systems, allowed fidelity tiers, or loss posture with explicit exclusion rows
- treat exact duplicate rows with identical `candidate_id` and `payload_hash` as `duplicate_translation_row`
- raise `ValueError` when one candidate ID resolves to conflicting payload hashes across selected bundles
- do not add external-model registration, prompt, runtime, hardware, or scorecard logic here

Write focused tests in `materials-discovery/tests/test_llm_translated_benchmark_freeze.py` using tmp-path bundle fixtures or small generated bundles from the shipped translation writer. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_freeze.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def load_translated_benchmark_spec" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "def freeze_translated_benchmark_set" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "duplicate_translation_row" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "payload_hash" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "loss_posture|allowed_fidelity_tiers|target_family|systems" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -c "^def test_" materials-discovery/tests/test_llm_translated_benchmark_freeze.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_freeze.py -x -v`
  </acceptance_criteria>
  <done>The repo can deterministically decide which translated cases belong in a frozen benchmark pack and explain every exclusion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist the frozen contract, included inventory, excluded inventory, and manifest lineage</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translated_benchmark_freeze.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    .planning/phases/34-benchmark-pack-and-freeze-contract/34-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: freezing a valid spec writes `freeze_contract.json`, `manifest.json`, `included.jsonl`, and `excluded.jsonl` under `data/benchmarks/llm_external_sets/{benchmark_set_id}/`.
    - Test 2: the written manifest stores source bundle manifest paths and source export IDs unchanged so later phases can trace the pack back to shipped bundles.
    - Test 3: the freeze summary returns manifest and inventory paths plus included/excluded counts for CLI consumption.
    - Test 4: rerunning the same spec yields stable ordering in the written inventories and does not depend on filesystem glob order.
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/translated_benchmark.py` so `freeze_translated_benchmark_set(...)` persists the full artifact family:
- write the normalized contract to `freeze_contract.json`
- write selected rows to `included.jsonl`
- write rejected rows to `excluded.jsonl`
- write `manifest.json` with contract path, inventory paths, counts, source bundle manifests, source export IDs, selected systems, target family, and exclusion-reason tallies

Use the Plan 01 storage helpers for every output path.
Return `TranslatedBenchmarkSetSummary` with absolute manifest and inventory paths.

Re-export the public freeze surface from `materials-discovery/src/materials_discovery/llm/__init__.py`.
Extend the test file so written artifacts are validated with the new schema models, not raw dict assertions. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_freeze.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "freeze_contract.json" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "included.jsonl|excluded.jsonl|manifest.json" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "source_bundle_manifest_paths|source_export_ids|exclusion_reason_counts" materials-discovery/src/materials_discovery/llm/translated_benchmark.py`
    - `rg -n "freeze_translated_benchmark_set|load_translated_benchmark_spec" materials-discovery/src/materials_discovery/llm/__init__.py`
    - `rg -n "freeze_contract.json|included.jsonl|excluded.jsonl|manifest.json" materials-discovery/tests/test_llm_translated_benchmark_freeze.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_freeze.py -x -v`
  </acceptance_criteria>
  <done>The repo can materialize one reusable translated benchmark pack whose contract and lineage survive as first-class files.</done>
</task>

</tasks>

<verification>
Run the focused freeze-core suite after both tasks:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_freeze.py -x -v`
</verification>

<success_criteria>
Phase 34 can now produce a frozen translated benchmark pack whose contents,
exclusions, and lineage are all file-backed and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/34-benchmark-pack-and-freeze-contract/34-02-SUMMARY.md`
</output>
