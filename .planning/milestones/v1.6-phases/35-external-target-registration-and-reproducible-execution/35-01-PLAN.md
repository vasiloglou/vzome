---
phase: 35-external-target-registration-and-reproducible-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_external_target_schema.py
  - materials-discovery/Progress.md
autonomous: true
requirements: [OPS-17]

must_haves:
  truths:
    - "A curated downloaded external model can be described by one typed, immutable registration contract instead of ad hoc notes: target identity, supported translation families, runner choice, snapshot or revision facts, and prompt or parser hooks are explicit."
    - "External target artifacts have a deterministic home under data/llm_external_models/ with dedicated files for registration, environment capture, and smoke results."
    - "The new contract stays additive to the repo's existing checkpoint, serving-identity, and translated-benchmark patterns rather than extending SystemConfig or creating a generic serving platform."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed spec, registration, environment, smoke, and summary models for external benchmark targets."
      contains: "class LlmExternalTargetRegistrationSpec"
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic storage helpers for external target artifacts."
      exports:
        - "llm_external_target_dir"
        - "llm_external_target_registration_path"
        - "llm_external_target_environment_path"
        - "llm_external_target_smoke_path"
    - path: "materials-discovery/tests/test_llm_external_target_schema.py"
      provides: "Validator coverage for the new external-target contract and storage layout."
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/checkpoints.py"
      via: "mirrors immutable registration and fingerprint discipline without lifecycle state"
      pattern: "RegistrationSpec|RegistrationSummary|fingerprint"
    - from: "materials-discovery/src/materials_discovery/llm/storage.py"
      to: "materials-discovery/data/llm_external_models"
      via: "deterministic external-target artifact layout"
      pattern: "llm_external_models"
---

<objective>
Freeze the contract and storage foundation for external benchmark targets.

Purpose: lock the exact typed and file-backed shape that later registration,
smoke, CLI, and benchmark phases will rely on, so downloaded-model identity and
reproducibility do not live in shell history or one-off scripts.

Output: typed external-target models, deterministic storage helpers, public
exports, and focused schema/storage tests.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-CONTEXT.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-RESEARCH.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-VALIDATION.md
@AGENTS.md
@CLAUDE.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/checkpoints.py
@materials-discovery/src/materials_discovery/llm/serving_benchmark.py
@materials-discovery/Progress.md

<interfaces>
Follow the existing checkpoint-registration contract style:
```python
class LlmCheckpointRegistrationSpec(BaseModel): ...
class LlmCheckpointRegistration(BaseModel): ...
class LlmCheckpointRegistrationSummary(BaseModel): ...

def llm_checkpoint_registration_path(checkpoint_id: str, root: Path | None = None) -> Path: ...
```

Reuse runtime-lineage precedent from:
```python
class LlmServingIdentity(BaseModel): ...
class LlmServingSmokeCheck(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the immutable external-target registration and reproducibility contract</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_external_target_schema.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    .planning/phases/35-external-target-registration-and-reproducible-execution/35-CONTEXT.md,
    .planning/phases/35-external-target-registration-and-reproducible-execution/35-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `LlmExternalTargetRegistrationSpec` rejects blank target IDs, blank supported target families, blank runner keys, and missing snapshot or revision identity.
    - Test 2: the contract exposes explicit fields for `model_family`, `supported_systems`, `supported_target_families`, `runner_key`, `provider`, `model`, `model_revision`, `tokenizer_revision`, `local_snapshot_path`, `prompt_contract_id`, and `response_parser_key`.
    - Test 3: `LlmExternalTargetRegistration` carries the normalized fingerprint and immutable target facts separately from mutable smoke status.
    - Test 4: `LlmExternalTargetEnvironmentManifest` carries reproducibility-grade environment fields such as Python version, optional package versions, hardware visibility, and snapshot lineage pointers.
    - Test 5: `LlmExternalTargetSmokeCheck` stores typed pass or fail status, latency, detail text, serving or target identity, and pointers to the captured environment artifact.
  </behavior>
  <action>
Add the Phase 35 contract models to `materials-discovery/src/materials_discovery/llm/schema.py`.

Create these concrete types:
- `LlmExternalTargetRunnerKey` with exactly `transformers_causal_lm` and `peft_causal_lm`.
- `LlmExternalTargetSmokeStatus` with exactly `passed` and `failed`.
- `LlmExternalTargetRegistrationSpec` with `model_id`, `model_family`, optional `supported_systems`, `supported_target_families`, `runner_key`, `provider`, `model`, `model_revision`, optional `tokenizer_revision`, `local_snapshot_path`, optional `snapshot_manifest_path`, optional `dtype`, optional `quantization`, `prompt_contract_id`, `response_parser_key`, and optional notes.
- `LlmExternalTargetRegistration` mirroring the normalized immutable facts plus `fingerprint`, `created_at_utc`, and artifact-path fields needed for later CLI inspect output.
- `LlmExternalTargetEnvironmentManifest` with Python version, package-version map, OS or kernel fields, CUDA or GPU visibility fields, snapshot path, tokenizer lineage, and the registration fingerprint it was captured against.
- `LlmExternalTargetSmokeCheck` with target ID, status, latency, detail, fingerprint, environment-path pointer, and any resolved runner metadata needed later by benchmark execution.
- `LlmExternalTargetRegistrationSummary` for CLI success output.

Keep this additive to the repo:
- mirror checkpoint-registration discipline without adding lifecycle promotion or retirement
- do not add benchmark scorecards or comparative run results here
- do not extend `SystemConfig`

Re-export the new public models from `materials-discovery/src/materials_discovery/llm/__init__.py`.

Create `materials-discovery/tests/test_llm_external_target_schema.py` with focused validator coverage for the new contract, environment, and smoke models. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class LlmExternalTargetRegistrationSpec" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class LlmExternalTargetRegistration" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class LlmExternalTargetEnvironmentManifest" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class LlmExternalTargetSmokeCheck" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "transformers_causal_lm|peft_causal_lm" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "LlmExternalTargetRegistrationSpec|LlmExternalTargetSmokeCheck" materials-discovery/src/materials_discovery/llm/__init__.py`
    - `rg -c "^def test_" materials-discovery/tests/test_llm_external_target_schema.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_schema.py -x -v`
  </acceptance_criteria>
  <done>The repo has one typed, test-backed contract for describing immutable external benchmark targets and their reproducibility artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic storage helpers for external-target artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/tests/test_llm_external_target_schema.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    .planning/phases/35-external-target-registration-and-reproducible-execution/35-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: storage helpers write under `data/llm_external_models/{model_id}/`.
    - Test 2: blank model IDs fail through the existing artifact-ID guard instead of creating ambiguous paths.
    - Test 3: the layout includes dedicated `registration.json`, `environment.json`, and `smoke_check.json` files.
    - Test 4: the storage helpers stay separate from checkpoint, campaign, translated-benchmark, and serving-benchmark directories.
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/storage.py` with deterministic helpers for the Phase 35 artifact root:
- `llm_external_target_dir(model_id: str, root: Path | None = None) -> Path`
- `llm_external_target_registration_path(model_id: str, root: Path | None = None) -> Path`
- `llm_external_target_environment_path(model_id: str, root: Path | None = None) -> Path`
- `llm_external_target_smoke_path(model_id: str, root: Path | None = None) -> Path`

Use the research-approved artifact root `data/llm_external_models/{model_id}/`.
Do not overload `data/llm_checkpoints/`, `data/benchmarks/llm_external_sets/`, or `data/benchmarks/llm_serving/`.

Expand `materials-discovery/tests/test_llm_external_target_schema.py` so the storage layout is locked together with the new contract. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def llm_external_target_dir" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def llm_external_target_registration_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def llm_external_target_environment_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def llm_external_target_smoke_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "data\" / \"llm_external_models\"" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "registration.json|environment.json|smoke_check.json" materials-discovery/tests/test_llm_external_target_schema.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_schema.py -x -v`
  </acceptance_criteria>
  <done>The external-target file layout is deterministic, isolated from earlier artifact families, and ready for the registration core.</done>
</task>

</tasks>

<verification>
Run the focused schema or storage slice after both tasks:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_schema.py -x -v`
</verification>

<success_criteria>
Phase 35 has a typed, file-backed external-target contract that makes
downloaded-model identity, compatibility, and reproducibility artifacts explicit
before any CLI or smoke logic is built.
</success_criteria>

<output>
After completion, create `.planning/phases/35-external-target-registration-and-reproducible-execution/35-01-SUMMARY.md`
</output>
