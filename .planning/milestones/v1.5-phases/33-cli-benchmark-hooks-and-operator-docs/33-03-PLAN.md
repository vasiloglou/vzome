---
phase: 33-cli-benchmark-hooks-and-operator-docs
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - materials-discovery/developers-docs/llm-translation-runbook.md
  - materials-discovery/developers-docs/llm-translation-contract.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/developers-docs/index.md
  - materials-discovery/README.md
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [OPS-15, OPS-16]
---

<objective>
Document the translation workflow so operators and developers can use it safely.

Purpose: finish the milestone with a reusable, explained interop layer instead
of a hidden CLI command.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-CONTEXT.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-RESEARCH.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-01-PLAN.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-02-PLAN.md

@materials-discovery/README.md
@materials-discovery/developers-docs/index.md
@materials-discovery/developers-docs/pipeline-stages.md
@materials-discovery/developers-docs/llm-translation-contract.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add operator runbook and document artifact locations plus fidelity boundaries</name>
  <files>
    materials-discovery/developers-docs/llm-translation-runbook.md,
    materials-discovery/developers-docs/llm-translation-contract.md,
    materials-discovery/developers-docs/pipeline-stages.md
  </files>
  <behavior>
    - The runbook shows both CIF and material-string commands with concrete artifact paths
    - The docs explain when exports are periodic-safe, when they are lossy periodic proxies, and when Zomic must remain authoritative
    - The technical contract note links operators to the new runbook instead of trying to become the runbook itself
  </behavior>
  <action>
Add a new translation runbook and update the pipeline reference so the CLI
workflow, bundle structure, and fidelity/loss posture are all documented from
the operator’s point of view.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update README/docs entry points and lock CLI help discoverability</name>
  <files>
    materials-discovery/README.md,
    materials-discovery/developers-docs/index.md,
    materials-discovery/tests/test_cli.py
  </files>
  <behavior>
    - Test 1: CLI help output includes the new translation commands
    - The README quickstart/docs map point to the new translation workflow
    - The developer docs index lists the translation runbook and command surface
  </behavior>
  <action>
Update the top-level materials-discovery docs entry points so operators can find
the new translation workflow quickly, and add lightweight CLI help coverage so
the command surface remains discoverable.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_cli.py -x -v</automated>
  </verify>
</task>

</tasks>

