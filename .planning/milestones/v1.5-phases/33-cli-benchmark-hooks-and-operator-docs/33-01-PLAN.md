---
phase: 33-cli-benchmark-hooks-and-operator-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/translation_bundle.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_translation_bundle.py
autonomous: true
requirements: [OPS-15]
---

<objective>
Create the file-backed translation bundle layer that Phase 33 CLI commands will
drive.

Purpose: add one deterministic artifact family for exported translation payloads
with raw files, inventory rows, and a bundle manifest so translation output is
auditable before the operator-facing CLI is added.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-CONTEXT.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-RESEARCH.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/translation.py
@materials-discovery/src/materials_discovery/llm/translation_export.py
@materials-discovery/tests/test_llm_translation_fixtures.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add translation bundle schema and deterministic storage helpers</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translation_bundle.py
  </files>
  <behavior>
    - Test 1: translation bundle path helpers resolve under `data/llm_translation_exports/{export_id}/`
    - Test 2: bundle manifest models reject blank IDs and paths
    - Test 3: inventory rows preserve target family, fidelity tier, loss reasons, payload path, and emitted text
    - Test 4: storage helpers also expose a standard stage-manifest path under `data/manifests/`
  </behavior>
  <action>
Add the Phase 33 translation-bundle contract in `llm/schema.py`:
- `TranslationBundleManifest`
- `TranslationInventoryRow`
- `TranslationExportSummary`

Add deterministic storage helpers in `llm/storage.py` for:
- bundle directory
- bundle manifest path
- inventory JSONL path
- payload directory
- standard stage-manifest path

Export the new public helpers from `materials_discovery.llm.__init__`.
Add failing-then-green tests in `test_llm_translation_bundle.py` that lock the
contract and path layout.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_bundle.py -x -v</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write translation bundles from candidate inputs through the shipped export seam</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation_bundle.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translation_bundle.py
  </files>
  <behavior>
    - Test 1: bundle writing over the periodic-safe fixture creates raw payload files plus inventory and bundle manifest
    - Test 2: bundle writing over the lossy fixture preserves explicit loss reasons and diagnostics in inventory rows without polluting the raw material-string body
    - Test 3: inventory rows keep emitted text inline so later experiment code can consume one JSONL directly
    - Test 4: repeated runs with the same fixture/target/export ID are byte-stable
  </behavior>
  <action>
Create `llm/translation_bundle.py` with the workflow helper that:
- accepts candidate records, target family, export ID, and input-path metadata
- prepares translated artifacts through `prepare_translated_structure(...)`
- emits text through `emit_translated_structure(...)`
- writes raw payload files under the bundle payload directory
- writes inventory rows with emitted text inline
- writes the bundle manifest

Keep this module CLI-agnostic; do not add Typer command code yet.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_bundle.py -x -v</automated>
  </verify>
</task>

</tasks>

