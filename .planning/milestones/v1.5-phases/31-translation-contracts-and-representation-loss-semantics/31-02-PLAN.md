---
phase: 31-translation-contracts-and-representation-loss-semantics
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - materials-discovery/src/materials_discovery/llm/translation.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/backends/structure_realization.py
  - materials-discovery/tests/test_llm_translation_core.py
  - materials-discovery/tests/test_structure_realization.py
autonomous: true
requirements: [LLM-27, LLM-30]

must_haves:
  truths:
    - "CandidateRecord remains the only source input for translation normalization; Phase 31 must not reparse raw Zomic or depend on notebook-only state."
    - "The normalization seam must preserve deterministic site ordering and coordinate-origin diagnostics so later CIF and material-string exporters stay stable."
    - "Exact periodic export is only valid when the compiled candidate already carries periodic-safe structure realization; when QC-native semantics are dropped, the artifact must be marked lossy with a reason."
    - "Plan 02 returns a normalized translated structure artifact, not final CIF or CrystalTextLLM/CSLLM text. Concrete text serializers belong to Phase 32."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/translation.py"
      provides: "Deterministic candidate-to-translated-artifact normalization"
      exports:
        [
          "prepare_translated_structure",
          "assess_translation_fidelity",
          "infer_coordinate_sources",
        ]
    - path: "materials-discovery/src/materials_discovery/backends/structure_realization.py"
      provides: "Coordinate-origin helpers reusable by the translation seam"
      exports:
        [
          "candidate_fractional_positions",
          "candidate_cartesian_positions",
          "candidate_cell_matrix",
        ]
  key_links:
    - from: "materials-discovery/src/materials_discovery/backends/structure_realization.py"
      to: "materials-discovery/src/materials_discovery/llm/translation.py"
      via: "translation normalization must reuse the shipped structure realization logic rather than duplicating coordinate conversion rules"
      pattern: "fractional|cartesian|cell_matrix"
    - from: "materials-discovery/src/materials_discovery/common/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/translation.py"
      via: "site-level qphi, fractional, and cartesian data determine coordinate-origin diagnostics and export fidelity"
      pattern: "qphi|fractional_position|cartesian_position|template_family"
---

<objective>
Normalize compiled candidates into a deterministic translated-structure artifact
with coordinate-origin and representation-loss diagnostics.

Purpose: give Phase 32 one stable bridge artifact regardless of whether the
downstream serializer emits CIF or a material-string format.

Output: normalization helpers, deterministic fidelity/loss classification, and
focused regression coverage for periodic-safe versus lossy targets.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-VALIDATION.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-01-PLAN.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/backends/structure_realization.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/tests/test_structure_realization.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a deterministic candidate-to-translated-artifact normalization seam</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/src/materials_discovery/backends/structure_realization.py,
    materials-discovery/tests/test_llm_translation_core.py,
    materials-discovery/tests/test_structure_realization.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/backends/structure_realization.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_structure_realization.py,
    .planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the translation seam preserves deterministic site ordering across repeated runs
    - Test 2: the translation seam prefers stored fractional positions, then stored cartesian positions, then qphi-derived fallback, and records which source was used
    - Test 3: a normalized translated artifact carries a stable cell plus normalized site list without emitting final CIF or material-string text yet
    - Test 4: repeated normalization of the same candidate and target descriptor yields byte-stable model output
    - Test 5: structure-realization regressions still pass after any helper additions
  </behavior>
  <action>
Add a new additive translation core module at
materials-discovery/src/materials_discovery/llm/translation.py.

Implement a deterministic normalization function that accepts a compiled
`CandidateRecord` plus a typed target descriptor and returns the translated
artifact shape introduced in Plan 01.

The normalization seam should:
- reuse the existing structure-realization logic instead of duplicating cell or
  coordinate conversion rules
- record the coordinate origin used for each site or for the artifact overall
  (stored fractional, stored cartesian, or qphi-derived fallback)
- produce a normalized structure payload that later serializers can consume
  consistently
- stay free of mandatory heavy optional dependencies

Extend structure-realization helpers only if needed to expose stable
coordinate-origin information. Any additions must remain additive and keep the
existing tests green.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_core.py tests/test_structure_realization.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/translation.py normalizes a compiled candidate into the translated artifact contract
    - normalized artifacts preserve deterministic site ordering and stable structure payloads
    - coordinate-origin diagnostics are explicit instead of implied
    - structure-realization regressions still pass
    - the core regression command exits 0
  </acceptance_criteria>
  <done>Phase 32 has one deterministic normalized artifact shape to serialize from.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify exact versus lossy downstream translation outcomes</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation.py,
    materials-discovery/tests/test_llm_translation_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/translation.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/llm-quasicrystal-landscape.md
  </read_first>
  <behavior>
    - Test 1: a periodic-safe/approximant-style candidate can classify as exact or anchored when the target family supports its realized structure
    - Test 2: a candidate that relies on qphi-derived fallback positions or QC-native semantics cannot silently claim exact periodic export
    - Test 3: lossy translations require an explicit reason that explains which source semantics the target family cannot preserve
    - Test 4: unsupported exactness claims fail clearly instead of degrading silently to success
  </behavior>
  <action>
Implement deterministic fidelity/loss classification in the translation module.

Use repo-local evidence only, such as:
- compiled candidate structure fields
- coordinate-origin diagnostics from Task 1
- target-family requirements from Plan 01
- explicit provenance or template-family hints when they exist

The fidelity decision must stay honest:
- exact should only be used when the normalized candidate already satisfies the
  target family's representation assumptions
- anchored or approximate can cover cases where structure intent is preserved
  but the source was partially derived or target semantics are narrower
- lossy must be used when a QC-native source cannot be represented faithfully by
  the target family

Do not emit final CIF or material strings in this phase. The phase only decides
how faithful the future export can be and records the reason.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_core.py tests/test_structure_realization.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - translation fidelity is classified deterministically from repo-local evidence
    - lossy artifacts require an explicit reason
    - exact periodic export cannot be claimed silently for QC-native/lossy cases
    - tests cover both periodic-safe and lossy scenarios
    - the core regression command exits 0
  </acceptance_criteria>
  <done>The translation bridge now records honest exact-versus-lossy outcomes before serializers are added.</done>
</task>

</tasks>
