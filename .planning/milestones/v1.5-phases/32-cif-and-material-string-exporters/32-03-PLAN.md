---
phase: 32-cif-and-material-string-exporters
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_expected.cif
  - materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_expected.material_string.txt
  - materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_expected.cif
  - materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_expected.material_string.txt
  - materials-discovery/tests/test_llm_translation_export_fixtures.py
  - materials-discovery/tests/test_llm_translation_cif.py
  - materials-discovery/tests/test_llm_translation_export.py
autonomous: true
requirements: [LLM-28, LLM-29]

must_haves:
  truths:
    - "Phase 32 stability must be anchored on checked-in golden outputs, not only inline string fragments."
    - "Representative exporter coverage must include both the periodic-safe Al-Cu-Fe case and the lossy QC-native Sc-Zn case across both target families."
    - "Failure coverage should reject malformed normalized artifacts while allowing legitimate lossy periodic-proxy exports to succeed with explicit metadata."
    - "CIF regression should prove parser compatibility with repo-local CIF tooling."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/tests/test_llm_translation_export_fixtures.py"
      provides: "Golden-output regression coverage for both target families"
      exports:
        [
          "fixture regression suite",
        ]
    - path: "materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_expected.cif"
      provides: "Periodic-safe exact CIF golden output"
      exports:
        [
          "golden file",
        ]
    - path: "materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_expected.material_string.txt"
      provides: "Lossy QC-native material-string golden output"
      exports:
        [
          "golden file",
        ]
  key_links:
    - from: "materials-discovery/tests/fixtures/llm_translation/"
      to: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      via: "golden files must freeze the exact exporter output bytes for both target families"
      pattern: "expected.cif|expected.material_string.txt|emit_translated_structure"
    - from: "materials-discovery/src/materials_discovery/generator/prototype_import.py"
      to: "materials-discovery/tests/test_llm_translation_cif.py"
      via: "exported CIF should stay parseable by repo-local CIF tooling"
      pattern: "parse_cif"
---

<objective>
Freeze exporter behavior with golden outputs, parser compatibility, and explicit
failure-case regressions.

Purpose: make the new CIF and material-string serializers stable enough for
later CLI/workflow integration by anchoring them on checked-in expected outputs
and parser-readable periodic text.

Output: golden-output fixtures, cross-target regression suite, and exporter
failure coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
@.planning/phases/32-cif-and-material-string-exporters/32-VALIDATION.md
@.planning/phases/32-cif-and-material-string-exporters/32-01-PLAN.md
@.planning/phases/32-cif-and-material-string-exporters/32-02-PLAN.md

@materials-discovery/src/materials_discovery/llm/translation_export.py
@materials-discovery/src/materials_discovery/generator/prototype_import.py
@materials-discovery/tests/test_llm_translation_fixtures.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add golden-output fixtures and regression coverage for exact and lossy exporter paths</name>
  <files>
    materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_expected.cif,
    materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_expected.material_string.txt,
    materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_expected.cif,
    materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_expected.material_string.txt,
    materials-discovery/tests/test_llm_translation_export_fixtures.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/tests/test_llm_translation_fixtures.py,
    .planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the periodic-safe fixture emits bytes identical to the checked-in exact CIF golden file
    - Test 2: the periodic-safe fixture emits bytes identical to the checked-in exact material-string golden file
    - Test 3: the QC-native fixture emits bytes identical to the checked-in lossy CIF golden file
    - Test 4: the QC-native fixture emits bytes identical to the checked-in lossy material-string golden file
    - Test 5: golden outputs keep explicit periodic-proxy/loss metadata visible in the lossy case
  </behavior>
  <action>
Add checked-in golden outputs under `materials-discovery/tests/fixtures/llm_translation/`
with these exact file names:
- `al_cu_fe_periodic_expected.cif`
- `al_cu_fe_periodic_expected.material_string.txt`
- `sc_zn_qc_expected.cif`
- `sc_zn_qc_expected.material_string.txt`

Add `materials-discovery/tests/test_llm_translation_export_fixtures.py` that:
- loads the existing Phase 31 candidate fixtures
- builds translated artifacts for both target families
- emits text with `emit_translated_structure(...)`
- compares exact emitted bytes to the checked-in golden files

The fixture suite should lock the two boundary cases:
- exact periodic-safe export
- explicit lossy periodic-proxy export
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_export_fixtures.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - all four checked-in golden output files exist under `tests/fixtures/llm_translation/`
    - exact emitted bytes match the checked-in golden files for both target families
    - lossy golden outputs keep explicit periodic-proxy metadata
    - the golden-output regression command exits 0
  </acceptance_criteria>
  <done>Exporter stability is now anchored on repo-backed expected outputs instead of ad hoc string fragments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parser compatibility and malformed-artifact failure coverage</name>
  <files>
    materials-discovery/tests/test_llm_translation_cif.py,
    materials-discovery/tests/test_llm_translation_export.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/generator/prototype_import.py,
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/tests/test_llm_translation_cif.py,
    materials-discovery/tests/test_llm_translation_export.py
  </read_first>
  <behavior>
    - Test 1: repo-local `parse_cif()` can parse the emitted periodic-safe CIF golden output and recover the expected cell/scalar structure
    - Test 2: repo-local `parse_cif()` can parse the lossy QC-native CIF payload after comment stripping and still recover the atom loop
    - Test 3: export helpers raise `ValueError` when a periodic export artifact is missing `cell`
    - Test 4: export helpers raise `ValueError` when a periodic export artifact has no sites or a site missing fractional coordinates
    - Test 5: those malformed-artifact failure cases stay separate from legitimate lossy exports, which continue to serialize successfully
  </behavior>
  <action>
Extend the CIF and shared export tests with two final guard rails:

1. Parser compatibility:
   - feed emitted CIF text into `materials_discovery.generator.prototype_import.parse_cif`
   - assert the required cell scalars and atom-site loop remain recoverable for
     both the exact and lossy fixture cases

2. Failure coverage:
   - construct malformed `TranslatedStructureArtifact` cases with missing `cell`,
     empty `sites`, and missing per-site `fractional_position`
   - assert `validate_translated_structure_for_export(...)` or
     `emit_translated_structure(...)` raises `ValueError`

Keep the malformed-artifact failures distinct from the legitimate lossy
QC-native fixture path, which must continue to serialize with explicit proxy
metadata instead of failing.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_cif.py tests/test_llm_translation_export.py tests/test_llm_translation_export_fixtures.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - repo-local CIF parsing tests pass on emitted exact and lossy payloads
    - malformed normalized artifacts fail clearly with `ValueError`
    - legitimate lossy periodic-proxy exports still serialize successfully
    - the parser/failure regression command exits 0
  </acceptance_criteria>
  <done>Phase 32 ends with parser-compatible CIF output, golden-output stability, and explicit malformed-artifact failure behavior.</done>
</task>

</tasks>
