---
phase: 32-cif-and-material-string-exporters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/translation_export.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_translation_export.py
  - materials-discovery/tests/test_llm_translation_cif.py
autonomous: true
requirements: [LLM-28, LLM-29]

must_haves:
  truths:
    - "Phase 32 exporters must consume `TranslatedStructureArtifact`, not raw `CandidateRecord` inputs and not reparsed Zomic text."
    - "CIF and material-string output must share one deterministic export seam for float formatting, cell ordering, site ordering, and export readiness validation."
    - "CIF emission must stay pure Python text serialization; do not introduce optional CIF-writer dependencies that can reorder fields or normalize numbers unpredictably."
    - "Lossy periodic-proxy posture must stay visible in emitted text, not only in sidecar docs or tests."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      provides: "Shared deterministic export helpers and CIF text emission"
      exports:
        [
          "emit_translated_structure",
          "emit_cif_text",
          "validate_translated_structure_for_export",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/__init__.py"
      provides: "Public export surface for Phase 32 serializers"
      exports:
        [
          "emit_translated_structure",
          "emit_cif_text",
          "validate_translated_structure_for_export",
        ]
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/translation.py"
      to: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      via: "exporters must serialize the normalized translated artifact that Phase 31 already defines"
      pattern: "TranslatedStructureArtifact|prepare_translated_structure|fidelity_tier|loss_reasons"
    - from: "materials-discovery/src/materials_discovery/generator/prototype_import.py"
      to: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      via: "repo-local CIF parsing surface defines the required cell and atom-loop field names the new exporter should remain compatible with"
      pattern: "_cell_length_a|_atom_site_fract_x|parse_cif"
---

<objective>
Implement the shared exporter seam and the deterministic CIF serializer from the
translated artifact contract.

Purpose: land one stable export module that validates export readiness, formats
numbers deterministically, and emits parser-compatible CIF text before the
material-string path is added.

Output: shared export helpers, CIF emission, and focused unit coverage for
stable/explicit periodic export.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
@.planning/phases/32-cif-and-material-string-exporters/32-VALIDATION.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-VERIFICATION.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/translation.py
@materials-discovery/src/materials_discovery/generator/prototype_import.py
@materials-discovery/tests/test_llm_translation_schema.py
@materials-discovery/tests/test_llm_translation_core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared export validation and deterministic formatting helpers</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translation_export.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/translation.py,
    materials-discovery/developers-docs/llm-translation-contract.md,
    .planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: repeated export of the same translated artifact yields byte-stable output metadata and text
    - Test 2: export readiness validation rejects periodic-target artifacts with missing cell data
    - Test 3: export readiness validation rejects empty site lists and any site missing fractional coordinates for periodic targets
    - Test 4: the shared dispatcher returns a new artifact with `emitted_text` populated and does not mutate the source artifact in place
    - Test 5: the dispatcher raises `NotImplementedError` for `material_string` until Plan 02 lands that serializer
  </behavior>
  <action>
Create a new module
`materials-discovery/src/materials_discovery/llm/translation_export.py`.

Implement the shared Phase 32 export seam with these exact public functions:
- `validate_translated_structure_for_export(artifact: TranslatedStructureArtifact) -> None`
- `emit_translated_structure(artifact: TranslatedStructureArtifact) -> TranslatedStructureArtifact`
- `emit_cif_text(artifact: TranslatedStructureArtifact) -> str`

In the shared validation/export path:
- require `artifact.cell` for any target whose descriptor has
  `requires_periodic_cell=True`
- require at least one site
- require every site to have `fractional_position` when the target requires
  fractional coordinates
- preserve the original artifact object and return a copied artifact with
  `emitted_text` filled

Implement deterministic scalar formatting in this module using one shared helper
that formats floats with fixed six-decimal precision, trims trailing zeroes, and
keeps at least one decimal digit (for example `1.0`, `0.125`, `90.0`).

For the dispatcher:
- support `cif` by calling `emit_cif_text(...)`
- raise `NotImplementedError(\"material_string export not implemented yet\")`
  for `material_string` until Plan 02

Export the new public functions from `materials-discovery/src/materials_discovery/llm/__init__.py`.

Add focused tests in `materials-discovery/tests/test_llm_translation_export.py`
that lock the validation and dispatch behavior.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_export.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `materials-discovery/src/materials_discovery/llm/translation_export.py` defines `validate_translated_structure_for_export`
    - `materials-discovery/src/materials_discovery/llm/translation_export.py` defines `emit_translated_structure`
    - `materials-discovery/src/materials_discovery/llm/translation_export.py` keeps material-string export explicitly not implemented until Plan 02
    - export validation fails clearly on missing periodic export prerequisites
    - the shared export regression command exits 0
  </acceptance_criteria>
  <done>The repo has one deterministic export seam and explicit export-readiness failures before format-specific serializers expand it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit deterministic CIF text from translated artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/tests/test_llm_translation_cif.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/src/materials_discovery/generator/prototype_import.py,
    materials-discovery/tests/test_llm_translation_fixtures.py,
    .planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: CIF output starts with a deterministic comment preamble exposing source candidate ID, fidelity tier, and loss reasons
    - Test 2: CIF output includes one `data_` block with the six required cell scalar lines in fixed order
    - Test 3: CIF output includes one atom-site loop with `_atom_site_label`, `_atom_site_type_symbol`, `_atom_site_fract_x`, `_atom_site_fract_y`, `_atom_site_fract_z`, and `_atom_site_occupancy`
    - Test 4: atom rows preserve translated-artifact site order exactly
    - Test 5: repo-local CIF parsing (`parse_cif`) can still read the emitted payload after comments are stripped
    - Test 6: lossy QC-native exports keep explicit periodic-proxy metadata in the preamble instead of pretending the payload is exact
  </behavior>
  <action>
Implement `emit_cif_text(artifact)` as a pure-text serializer with this exact
layout:

1. Comment preamble lines beginning with `# `:
   - `# source_candidate_id=<candidate_id>`
   - `# system=<system or none>`
   - `# target_family=cif`
   - `# fidelity_tier=<tier>`
   - `# loss_reasons=<comma-separated reasons or none>`

2. One CIF data block name:
   - `data_<candidate_id>` sanitized to letters, digits, `_`, or `-`

3. Scalar cell lines in this exact order:
   - `_cell_length_a`
   - `_cell_length_b`
   - `_cell_length_c`
   - `_cell_angle_alpha`
   - `_cell_angle_beta`
   - `_cell_angle_gamma`

4. One atom-site loop with headers in this exact order:
   - `_atom_site_label`
   - `_atom_site_type_symbol`
   - `_atom_site_fract_x`
   - `_atom_site_fract_y`
   - `_atom_site_fract_z`
   - `_atom_site_occupancy`

5. One row per translated site, preserving artifact order and using the shared
   float formatter from Task 1.

Hook `emit_translated_structure(...)` so the `cif` branch returns the copied
artifact with `emitted_text` set to this payload.

Add focused tests in `materials-discovery/tests/test_llm_translation_cif.py`
covering both the periodic-safe and lossy fixture paths plus repo-local CIF
parser compatibility.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_cif.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `emit_cif_text` writes the fixed cell scalar order and fixed atom-site loop headers
    - CIF output preserves translated site order
    - CIF output comment preamble contains source candidate ID, fidelity tier, and loss-reason visibility
    - repo-local CIF parsing tests pass on emitted text
    - the CIF regression command exits 0
  </acceptance_criteria>
  <done>The repo can now emit deterministic CIF text from translated artifacts without notebook-only conversion code.</done>
</task>

</tasks>
