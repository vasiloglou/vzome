---
phase: 20-specialized-lane-integration-and-workflow-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/common/schema.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/evaluate.py
  - materials-discovery/src/materials_discovery/llm/specialist.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_evaluate_schema.py
autonomous: true
requirements: [LLM-15, OPS-09]

must_haves:
  truths:
    - "Phase 20 introduces the first operational specialized lane through evaluation-first workflow behavior, not through a new generator."
    - "llm-evaluate must reuse Phase 19 lane resolution and readiness semantics rather than inventing a second serving selection path."
    - "Evaluation artifacts must gain additive serving identity while legacy evaluation artifacts remain readable."
    - "The core artifact family remains stable; Phase 20 extends lineage rather than forking a specialized-only output contract."
    - "Requested unavailable specialized lanes still fail clearly unless explicit fallback is configured."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/common/schema.py"
      provides: "Evaluation-lane config selector that stays additive to the existing system config contract"
      exports:
        [
          "LlmEvaluateConfig",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed evaluation serving-identity lineage"
      exports:
        [
          "LlmAssessment",
          "LlmEvaluationRunManifest",
          "LlmServingIdentity",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/evaluate.py"
      provides: "Lane-aware evaluation core that reuses the shared serving resolver"
      exports:
        [
          "evaluate_llm_candidates",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/specialist.py"
      provides: "Thin specialized-lane prompt and payload seam so specialized evaluation is not just endpoint selection"
      exports:
        [
          "build_specialized_evaluation_payload",
        ]
    - path: "materials-discovery/tests/test_llm_evaluate_schema.py"
      provides: "Focused regression coverage for specialized evaluation lane config, lineage, and backward-compatible reads"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/evaluate.py"
      to: "materials-discovery/src/materials_discovery/llm/launch.py"
      via: "evaluation must reuse the existing serving-lane resolver and explicit fallback contract"
      pattern: "resolve_serving_lane|build_serving_identity"
    - from: "materials-discovery/src/materials_discovery/llm/evaluate.py"
      to: "materials-discovery/src/materials_discovery/llm/runtime.py"
      via: "specialized evaluation lanes must run the same readiness validation before provider execution"
      pattern: "validate_llm_adapter_ready|resolve_llm_adapter"
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/compare.py"
      via: "compare and report surfaces can only show specialized evaluation lineage if assessment artifacts carry it first"
      pattern: "LlmAssessment|LlmEvaluationRunManifest|serving_identity"
---

<objective>
Define the additive specialized-lane evaluation contract before CLI, compare,
or report work widens the regression surface.

Purpose: make `llm-evaluate` a first-class lane-aware workflow step that can
select a specialized materials endpoint honestly, record the exact serving
identity it used, and remain backward-compatible with existing evaluation
artifacts.

Output: additive evaluation config fields, typed assessment/run-manifest
lineage, a thin specialized prompt seam, and a lane-aware
`evaluate_llm_candidates(...)` core.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-RESEARCH.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-VALIDATION.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/evaluate.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/developers-docs/llm-integration.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend evaluation config and artifact schemas for specialized-lane identity</name>
  <files>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_evaluate_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/specialist.py,
    materials-discovery/tests/test_llm_evaluate_schema.py,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `llm_evaluate` can declare `model_lane` as `general_purpose` or `specialized_materials` without breaking configs that omit it
    - Test 2: `LlmAssessment` accepts additive requested/resolved lane fields plus optional nested `serving_identity`
    - Test 3: `LlmEvaluationRunManifest` accepts additive lane and serving-identity fields while still reading legacy manifests that only carry adapter/provider/model
    - Test 4: blank optional lane or identity strings normalize away rather than writing meaningless whitespace
    - Test 5: `LlmEvaluateSummary` can carry resolved lane information additively so CLI output can expose what actually ran
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/common/schema.py`
additively.

In `LlmEvaluateConfig`, add:
- `model_lane: Literal["general_purpose", "specialized_materials"] | None = None`

Normalize it as an optional lane selector and keep all existing evaluation
fields intact.

Extend `materials-discovery/src/materials_discovery/llm/schema.py` additively:

For `LlmAssessment`, add:
- `requested_model_lanes: list[str] = Field(default_factory=list)`
- `resolved_model_lane: str | None = None`
- `resolved_model_lane_source: ResolvedModelLaneSource | None = None`
- `serving_identity: LlmServingIdentity | None = None`

For `LlmEvaluationRunManifest`, add the same lane and serving-identity fields:
- `requested_model_lanes`
- `resolved_model_lane`
- `resolved_model_lane_source`
- `serving_identity`

For `LlmEvaluateSummary`, add:
- `requested_model_lanes: list[str] = Field(default_factory=list)`
- `resolved_model_lane: str | None = None`
- `resolved_model_lane_source: ResolvedModelLaneSource | None = None`
- `serving_identity: LlmServingIdentity | None = None`

Rules:
- new writes may populate lane and serving-identity fields
- legacy evaluation artifacts must still deserialize when those fields are absent
- existing adapter/provider/model fields remain present for backward-compatible
  audit readability
- CLI summary JSON should continue to serialize through the existing model-dump
  path rather than weakening the typing of `serving_identity`

Update `materials-discovery/src/materials_discovery/llm/__init__.py` exports
for any newly used schema symbols.

Extend `materials-discovery/tests/test_llm_evaluate_schema.py` to cover the
new config and artifact behavior above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_evaluate_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/common/schema.py contains "model_lane"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "requested_model_lanes" within LlmAssessment or LlmEvaluationRunManifest
    - materials-discovery/src/materials_discovery/llm/schema.py contains "serving_identity" for evaluation artifacts
    - legacy evaluation artifacts still deserialize when lane-aware fields are absent
    - materials-discovery/tests/test_llm_evaluate_schema.py covers additive evaluation-lane lineage
    - the evaluation schema regression command exits 0
  </acceptance_criteria>
  <done>The repo has an additive schema contract for specialized-lane evaluation without breaking existing evaluation artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the shared serving resolver in llm-evaluate and thread specialized identity into provenance</name>
  <files>
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/specialist.py,
    materials-discovery/tests/test_llm_evaluate_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/llm/specialist.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_llm_evaluate_schema.py,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: evaluation can target `specialized_materials` through `llm_evaluate.model_lane` without changing the standard `llm_evaluated` JSONL contract
    - Test 2: evaluation reuses the existing shared serving resolver instead of duplicating lane-selection logic
    - Test 3: `openai_compat_v1` evaluation lanes run readiness validation before provider execution
    - Test 4: candidate provenance `llm_assessment` carries requested/resolved lane and serving identity additively
    - Test 5: specialized evaluation routes through a thin specialist payload seam instead of sharing an identical prompt path with the general-purpose lane
    - Test 6: configs without `llm_evaluate.model_lane` keep the current backend-driven behavior
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/evaluate.py` so
`evaluate_llm_candidates(...)` becomes lane-aware without becoming a second
runtime system.

Concrete changes:
- add an additive `requested_model_lane: str | None = None` function argument
  to `evaluate_llm_candidates(...)`
- determine the winning lane using the shared serving-lane helper from
  `materials-discovery/src/materials_discovery/llm/launch.py`
- lane resolution must pass `config.llm_generate` into the shared resolver;
  do **not** add a second lane registry (`model_lanes`, `default_model_lane`,
  or `fallback_model_lane`) under `LlmEvaluateConfig`
- precedence must be:
  1. explicit function or CLI-requested lane
  2. `config.llm_evaluate.model_lane`
  3. existing Phase 19 default/fallback/backend-default semantics
- if the resolved adapter is `openai_compat_v1`, call the same readiness
  validation helper used by manual generation and launch before any provider
  attempt begins

Thread the resolved serving identity through the evaluation flow:
- `LlmAssessment`
- `LlmEvaluationRunManifest`
- `LlmEvaluateSummary`
- `candidate_copy.provenance["llm_assessment"]`

Add a thin specialized prompt seam in
`materials-discovery/src/materials_discovery/llm/specialist.py`.

It should:
- provide a specialist-specific payload builder such as
  `build_specialized_evaluation_payload(...)`
- reshape the candidate context additively for structure-aware endpoints
  without requiring a full `pymatgen` or CIF-first bridge
- allow at least an explicit `evaluation_mode: specialized_materials` marker
  plus composition/structure-oriented fields so the specialized lane is not
  only differentiated by endpoint selection

Route `evaluate.py` through the standard evaluation prompt path for
`general_purpose` lanes and through the thin specialist payload seam for
`specialized_materials` lanes.

Keep the standard `llm_evaluated` candidate JSONL shape unchanged. Only add
lane identity and serving identity inside the additive provenance block.

Keep current mock-mode and non-lane configs working exactly as before when no
evaluation lane is requested or configured.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_evaluate_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/evaluate.py calls the shared serving-lane resolver
    - materials-discovery/src/materials_discovery/llm/evaluate.py calls readiness validation for openai_compat_v1 evaluation lanes
    - materials-discovery/src/materials_discovery/llm/specialist.py exists and is used for specialized evaluation payload shaping
    - candidate provenance llm_assessment includes requested/resolved lane fields additively
    - configs without llm_evaluate.model_lane still evaluate successfully via the existing backend path
    - the lane-aware evaluation regression command exits 0
  </acceptance_criteria>
  <done>The evaluation core can now run honestly against a specialized lane and record exactly what served the assessment.</done>
</task>

</tasks>

<verification>
  <check>Lane-aware evaluation artifacts remain additive and backward-compatible.</check>
  <check>`llm-evaluate` now reuses the existing Phase 19 lane contract rather than inventing a second serving system.</check>
  <check>Specialized evaluation lineage is available for later compare/report work.</check>
</verification>
