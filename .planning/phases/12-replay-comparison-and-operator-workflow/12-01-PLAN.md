---
phase: 12-replay-comparison-and-operator-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/replay.py
  - materials-discovery/src/materials_discovery/llm/compare.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_replay_core.py
  - materials-discovery/tests/test_llm_compare_core.py
autonomous: true
requirements: [LLM-09, LLM-11]

must_haves:
  truths:
    - "Replay is driven by recorded launch artifacts; it must not reinterpret approval-time actions or accept behavioral overrides."
    - "Strict replay requires the full launch bundle: launch_summary.json, resolved_launch.json, campaign_spec.json, run_manifest.json, and prompt.json."
    - "Comparison must evaluate current outcomes against both the originating acceptance-pack metrics and the most recent prior launch when available."
    - "Standard artifact roots remain primary for generated outputs, while campaign-root wrapper artifacts store replay/comparison audit files."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed replay/comparison contracts and additive replay lineage fields on launch artifacts"
      exports:
        [
          "LlmCampaignOutcomeSnapshot",
          "LlmCampaignComparisonResult",
          "LlmCampaignLaunchSummary",
          "LlmCampaignResolvedLaunch",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic campaign comparison and outcome-snapshot paths"
      exports:
        [
          "llm_campaign_outcome_snapshot_path",
          "llm_campaign_comparisons_dir",
          "llm_campaign_comparison_path",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/replay.py"
      provides: "Launch-bundle loading and strict replay config reconstruction"
      exports:
        [
          "load_campaign_launch_bundle",
          "build_replay_config",
          "build_replay_campaign_metadata",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/compare.py"
      provides: "Outcome snapshot collection and deterministic prior-launch resolution"
      exports:
        [
          "build_campaign_outcome_snapshot",
          "find_prior_campaign_launch",
          "build_campaign_comparison",
        ]
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/replay.py"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "replay must treat Phase 11 launch_summary.json as the operator-facing entry artifact"
      pattern: "llm-launch|launch_summary"
    - from: "materials-discovery/src/materials_discovery/llm/replay.py"
      to: "materials-discovery/src/materials_discovery/llm/generate.py"
      via: "replay config must freeze the same prompt and runtime inputs used by generate_llm_candidates"
      pattern: "prompt.json|run_manifest.json|temperature|max_tokens|max_attempts"
    - from: "materials-discovery/src/materials_discovery/llm/compare.py"
      to: "materials-discovery/src/materials_discovery/llm/acceptance.py"
      via: "comparison must reuse the Phase 9 acceptance metric vocabulary"
      pattern: "parse_success_rate|compile_success_rate|validation_pass_rate|novelty_score_mean"
---

<objective>
Create the typed replay/comparison contract foundation for Phase 12.

Purpose: freeze the exact artifact and helper semantics for replay bundles,
outcome snapshots, and prior-launch comparison before any CLI wiring starts.

Output: additive replay fields on launch artifacts, new comparison artifact
models, campaign-root storage helpers, and core helpers that can reconstruct a
strict replay config and deterministic comparison baselines from existing
artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-CONTEXT.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-VALIDATION.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/generate.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/src/materials_discovery/llm/acceptance.py
@materials-discovery/src/materials_discovery/llm/pipeline_benchmark.py
@materials-discovery/src/materials_discovery/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed replay/comparison artifacts and campaign storage helpers</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_replay_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-CONTEXT.md,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: launch-summary and resolved-launch models accept additive replay-lineage fields without breaking existing non-replay launch artifacts
    - Test 2: comparison and outcome-snapshot models reject blank campaign IDs, launch IDs, summary paths, and invalid metric structures
    - Test 3: storage helpers create deterministic paths for `outcome_snapshot.json` under the launch directory and comparison JSON under `data/llm_campaigns/{campaign_id}/comparisons/`
    - Test 4: comparison artifacts can represent missing downstream metrics explicitly instead of coercing them to zero
    - Test 5: existing launch-schema tests still remain compatible with the additive fields
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/schema.py` with additive
Phase 12 contracts.

Add optional replay-lineage fields to both `LlmCampaignResolvedLaunch` and
`LlmCampaignLaunchSummary`:
- `replay_of_launch_id: str | None = None`
- `replay_of_launch_summary_path: str | None = None`
- `current_system_config_hash: str | None = None`

Add typed models:
- `LlmCampaignOutcomeSnapshot`
  - `schema_version: "llm-campaign-outcome-snapshot/v1"`
  - `campaign_id`
  - `launch_id`
  - `system`
  - `launch_summary_path`
  - `campaign_spec_path`
  - `acceptance_pack_path`
  - `requested_model_lanes`
  - `resolved_model_lane`
  - `resolved_model_lane_source`
  - `parse_success_rate`
  - `compile_success_rate`
  - `generation_success_rate`
  - `shortlist_pass_rate: float | None = None`
  - `validation_pass_rate: float | None = None`
  - `novelty_score_mean: float | None = None`
  - `synthesizability_mean: float | None = None`
  - `report_release_gate_ready: bool | None = None`
  - `missing_metrics: list[str] = Field(default_factory=list)`
  - `stage_manifest_paths: dict[str, str] = Field(default_factory=dict)`
- `LlmCampaignComparisonResult`
  - `schema_version: "llm-campaign-comparison/v1"`
  - `comparison_id`
  - `campaign_id`
  - `launch_id`
  - `system`
  - `generated_at_utc`
  - `current_outcome: LlmCampaignOutcomeSnapshot`
  - `acceptance_baseline: LlmAcceptanceSystemMetrics`
  - `prior_outcome: LlmCampaignOutcomeSnapshot | None = None`
  - `delta_vs_acceptance: dict[str, float] = Field(default_factory=dict)`
  - `delta_vs_prior: dict[str, float] = Field(default_factory=dict)`
  - `summary_lines: list[str] = Field(default_factory=list)`

Validation rules:
- required string fields must reject blanks
- `missing_metrics` must be normalized/deduped
- `stage_manifest_paths` values must be non-blank strings
- `delta_vs_acceptance` and `delta_vs_prior` keys must be limited to the
  acceptance-pack metric names above

Extend `materials-discovery/src/materials_discovery/llm/storage.py` with:
- `llm_campaign_outcome_snapshot_path(campaign_id, launch_id, root=None)`
- `llm_campaign_comparisons_dir(campaign_id, root=None)`
- `llm_campaign_comparison_path(campaign_id, comparison_id, root=None)`

Update `materials-discovery/src/materials_discovery/llm/__init__.py` exports for
all new models and storage helpers.

Use `materials-discovery/tests/test_llm_replay_core.py` as the first focused
schema/storage coverage file. Add targeted cases that validate the new models
and path helpers directly.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_replay_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmCampaignOutcomeSnapshot"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmCampaignComparisonResult"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "replay_of_launch_id"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def llm_campaign_outcome_snapshot_path"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def llm_campaign_comparison_path"
    - the focused replay-core test command exits 0
  </acceptance_criteria>
  <done>The repo has typed replay/comparison contracts and deterministic storage paths that later CLI work can rely on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build launch-bundle replay helpers and deterministic comparison baselines</name>
  <files>
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_replay_core.py,
    materials-discovery/tests/test_llm_compare_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/llm/acceptance.py,
    materials-discovery/src/materials_discovery/llm/pipeline_benchmark.py,
    materials-discovery/src/materials_discovery/cli.py,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: replay helpers load and validate the full launch bundle: launch summary, resolved launch, campaign spec, run manifest, and prompt payload
    - Test 2: replay config reconstruction preserves the recorded prompt template, temperature, max_tokens, max_attempts, resolved backend tuple, composition bounds, example-pack path, and seed path even when the current config hash has drifted
    - Test 3: replay reconstruction fails only when the current config no longer matches the recorded system or template family, or when required launch-bundle files are missing
    - Test 4: prior-launch lookup picks the latest prior launch for the same campaign/system by `created_at_utc`, then `launch_id`
    - Test 5: outcome snapshots always compute launch-level generation metrics from the run-level bundle and only mark downstream metrics present when matching stage manifests still reference the same `launch_id`
    - Test 6: prior launches with a committed `outcome_snapshot.json` use that snapshot directly instead of rescanning mutable standard stage paths
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/replay.py`.

Implement:
- `CampaignLaunchBundle` (dataclass or Pydantic model) containing:
  - `launch_summary`
  - `resolved_launch`
  - `campaign_spec`
  - `run_manifest`
  - `prompt_payload`
- `load_campaign_launch_bundle(launch_summary_path: Path, root: Path | None = None)`
  - load `launch_summary.json`
  - follow `resolved_launch_path`, `campaign_spec_path`, and `run_manifest_path`
  - load `prompt.json` from `run_manifest.prompt_path`
  - validate matching `campaign_id`, `launch_id`, and `system` across the bundle
- `build_replay_config(bundle: CampaignLaunchBundle, current_config: SystemConfig) -> SystemConfig`
  - start from `current_config.model_copy(deep=True)`
  - require the same `system_name` and `template_family` as the bundle
  - overwrite the replay-critical fields with recorded values:
    - `backend.llm_adapter`
    - `backend.llm_provider`
    - `backend.llm_model`
    - `backend.llm_api_base`
    - `composition_bounds`
    - `llm_generate.prompt_template`
    - `llm_generate.temperature`
    - `llm_generate.max_tokens`
    - `llm_generate.max_attempts`
    - `llm_generate.example_pack_path`
    - `llm_generate.max_conditioning_examples`
    - `llm_generate.seed_zomic`
  - do not fail only because the current config hash differs; that drift is recorded later
- `build_replay_campaign_metadata(...)`
  - return the additive campaign metadata block with:
    - `campaign_id`
    - `launch_id`
    - `campaign_spec_path`
    - `proposal_id`
    - `approval_id`
    - `requested_model_lanes`
    - `resolved_model_lane`
    - `resolved_model_lane_source`
    - `replay_of_launch_id`
    - `replay_of_launch_summary_path`

Create `materials-discovery/src/materials_discovery/llm/compare.py`.

Implement:
- `find_prior_campaign_launch(current_launch_summary: LlmCampaignLaunchSummary, root: Path | None = None) -> Path | None`
- `build_campaign_outcome_snapshot(bundle: CampaignLaunchBundle, root: Path | None = None)`
  - compute parse/compile pass rates from `compile_results.jsonl`
  - compute generation success rate from `run_manifest.requested_count` and `generated_count`
  - try to enrich downstream metrics only from manifests whose
    `source_lineage.llm_campaign.launch_id` matches the bundle launch
  - when downstream artifacts are absent or do not match, append the metric name
    to `missing_metrics` instead of inserting zeros
  - if `outcome_snapshot.json` already exists for the target launch, load and
    return it unchanged
- `build_campaign_comparison(...)`
  - load the acceptance pack from `campaign_spec.lineage.acceptance_pack_path`
  - choose the matching system row
  - compute `delta_vs_acceptance` and `delta_vs_prior` only for metrics present
    on both sides
  - produce deterministic `summary_lines`

Update `materials-discovery/src/materials_discovery/llm/__init__.py` exports and
create focused coverage in:
- `materials-discovery/tests/test_llm_replay_core.py`
- `materials-discovery/tests/test_llm_compare_core.py`
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_replay_core.py tests/test_llm_compare_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/replay.py contains "def load_campaign_launch_bundle"
    - materials-discovery/src/materials_discovery/llm/replay.py contains "def build_replay_config"
    - materials-discovery/src/materials_discovery/llm/compare.py contains "def build_campaign_outcome_snapshot"
    - materials-discovery/src/materials_discovery/llm/compare.py contains "def find_prior_campaign_launch"
    - materials-discovery/src/materials_discovery/llm/compare.py contains "def build_campaign_comparison"
    - the replay-core + compare-core regression command exits 0
  </acceptance_criteria>
  <done>The repo can now load a strict replay bundle and derive deterministic comparison baselines before any operator CLI is added.</done>
</task>

</tasks>

