---
phase: 12-replay-comparison-and-operator-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/replay.py
  - materials-discovery/src/materials_discovery/llm/compare.py
  - materials-discovery/src/materials_discovery/llm/generate.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/tests/test_llm_replay_cli.py
  - materials-discovery/tests/test_llm_compare_cli.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-09, LLM-11, OPS-07]

must_haves:
  truths:
    - "Replay launches are new executions, but they stay traceable back to the source launch through additive replay lineage."
    - "Replay must not accept behavioral overrides; the recorded launch bundle is the execution authority."
    - "Compare must always evaluate the current launch against the originating acceptance pack and should include the most recent prior launch when available."
    - "Operator-facing commands must emit both durable JSON artifacts and concise terminal summaries."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator-facing replay and compare commands"
    - path: "materials-discovery/src/materials_discovery/llm/replay.py"
      provides: "Replay execution over the existing generate_llm_candidates runtime"
    - path: "materials-discovery/src/materials_discovery/llm/compare.py"
      provides: "Campaign comparison artifact creation and terminal-summary formatting"
    - path: "materials-discovery/tests/test_llm_replay_cli.py"
      provides: "Replay CLI regression coverage"
    - path: "materials-discovery/tests/test_llm_compare_cli.py"
      provides: "Compare CLI regression coverage"
  key_links:
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/replay.py"
      via: "llm-replay must build a strict replay config from the launch bundle before calling generate_llm_candidates"
      pattern: "load_campaign_launch_bundle|build_replay_config|generate_llm_candidates"
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/compare.py"
      via: "llm-compare should create outcome snapshots and a typed comparison JSON before printing its terminal summary"
      pattern: "build_campaign_outcome_snapshot|build_campaign_comparison"
    - from: "materials-discovery/src/materials_discovery/llm/generate.py"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "replayed runs need additive replay lineage in run manifests and candidate provenance"
      pattern: "campaign_metadata|llm_campaign"
---

<objective>
Ship the operator-facing replay and compare commands for the closed-loop
campaign workflow.

Purpose: turn the Phase 12 contract foundation into usable CLI commands that
replay launches strictly, compare outcomes durably, and surface concise
operator-readable summaries without weakening the existing launch flow.

Output: `mdisc llm-replay`, `mdisc llm-compare`, replay-aware campaign
provenance, and focused CLI regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-CONTEXT.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-VALIDATION.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-01-PLAN.md

@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/replay.py
@materials-discovery/src/materials_discovery/llm/compare.py
@materials-discovery/src/materials_discovery/llm/generate.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict `llm-replay` over the existing generate runtime</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_replay_cli.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_llm_launch_cli.py,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-replay --launch-summary PATH` loads the source launch bundle and creates a new replay launch without requiring any behavioral override flags
    - Test 2: replay writes a new `resolved_launch.json` and `launch_summary.json` under the same campaign `launches/` tree, with additive `replay_of_launch_id` and `replay_of_launch_summary_path`
    - Test 3: replayed runs preserve the recorded effective prompt/runtime inputs even when the current system-config hash differs, and the drift is recorded in the replay artifacts
    - Test 4: replayed runs write to a deterministic replay-safe candidates path under the standard candidates root instead of overwriting the original launch output
    - Test 5: replayed run manifests and candidate provenance gain additive replay lineage without breaking the existing `llm_campaign` block
    - Test 6: missing source-bundle files or system/template mismatches fail clearly with exit code 2
  </behavior>
  <action>
Add a new command to `materials-discovery/src/materials_discovery/cli.py`:
`mdisc llm-replay --launch-summary PATH`

Behavior:
- load the source bundle with `load_campaign_launch_bundle(...)`
- load the current system config from `bundle.campaign_spec.launch_baseline.system_config_path`
- build a strict replay config with `build_replay_config(...)`
- generate a fresh `launch_id`
- write replay outputs under:
  - `data/llm_campaigns/{campaign_id}/launches/{new_launch_id}/resolved_launch.json`
  - `data/llm_campaigns/{campaign_id}/launches/{new_launch_id}/launch_summary.json`
- write candidates to a deterministic standard-root path:
  - `data/candidates/{system_slug}_replay_{new_launch_id}.jsonl`
- populate additive replay fields on the new resolved launch and launch summary:
  - `replay_of_launch_id`
  - `replay_of_launch_summary_path`
  - `current_system_config_hash`
- call `generate_llm_candidates(...)` with replay campaign metadata that also
  includes:
  - `replay_of_launch_id`
  - `replay_of_launch_summary_path`
- update `materials-discovery/src/materials_discovery/llm/generate.py` so
  `LlmRunManifest` and candidate `provenance.llm_campaign` can carry the replay
  lineage additively when that metadata is present
- on success, print the typed replay launch summary JSON
- on failure, still write a failed launch summary for the replay launch and exit 2

Do not add behavioral flags such as `--temperature`, `--lane`, `--seed-zomic`,
or `--out` to `llm-replay`. Phase 12 replay is strict.

Create `materials-discovery/tests/test_llm_replay_cli.py` to cover success,
drift-recording, replay-safe output paths, and failure cases.

Update `materials-discovery/tests/test_cli.py` only where needed so shared CLI
registration/help output stays green.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_replay_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains '@app.command("llm-replay")'
    - materials-discovery/src/materials_discovery/cli.py contains '--launch-summary'
    - materials-discovery/src/materials_discovery/llm/generate.py writes replay lineage when replay metadata is present
    - materials-discovery/tests/test_llm_replay_cli.py exists
    - the replay CLI regression command exits 0
  </acceptance_criteria>
  <done>Operators can replay a saved launch strictly, with durable replay lineage and no override knobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `llm-compare` with JSON artifacts and operator-readable summaries</name>
  <files>
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_compare_cli.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/llm/acceptance.py,
    materials-discovery/src/materials_discovery/lake/compare.py,
    materials-discovery/tests/test_llm_acceptance_benchmarks.py,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-compare --launch-summary PATH` writes `outcome_snapshot.json` under the current launch directory and a comparison JSON under the campaign comparisons directory
    - Test 2: comparison always includes the acceptance-pack baseline for the same system
    - Test 3: comparison includes the most recent prior launch when one exists and surfaces its absence clearly when it does not
    - Test 4: comparison prints a concise summary that names the current launch, the acceptance-pack comparison, and the prior-launch status
    - Test 5: missing downstream metrics are represented explicitly rather than silently converted to zeros
    - Test 6: the shared CLI suite remains green
  </behavior>
  <action>
Add a new command to `materials-discovery/src/materials_discovery/cli.py`:
`mdisc llm-compare --launch-summary PATH`

Behavior:
- load the current launch bundle with `load_campaign_launch_bundle(...)`
- build or load `outcome_snapshot.json` for the current launch
- locate the acceptance-pack baseline from
  `bundle.campaign_spec.lineage.acceptance_pack_path` and select the matching
  system row
- resolve the most recent prior launch with `find_prior_campaign_launch(...)`
- when a prior launch exists:
  - prefer its existing `outcome_snapshot.json`
  - otherwise derive a launch-level-only snapshot and leave downstream metrics
    in `missing_metrics`
- compute a `comparison_id` using the current `launch_id`
- write the comparison artifact to
  `data/llm_campaigns/{campaign_id}/comparisons/{comparison_id}.json`
- print a concise terminal summary built from `summary_lines`

Comparison summary requirements:
- first line identifies the system, campaign, and current launch
- include whether a prior launch baseline was found
- include the highest-signal deltas for:
  - `parse_success_rate`
  - `compile_success_rate`
  - `generation_success_rate`
  - `shortlist_pass_rate`
  - `validation_pass_rate`
  - `novelty_score_mean`
  - `synthesizability_mean`
  - `report_release_gate_ready`
- explicitly call out missing metrics instead of implying zero

Create `materials-discovery/tests/test_llm_compare_cli.py` covering:
- comparison with acceptance-pack baseline only
- comparison with acceptance-pack baseline plus prior launch
- output JSON path creation
- terminal summary content

Update `materials-discovery/tests/test_cli.py` only where needed so command help
and shared CLI behavior remain valid.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_compare_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains '@app.command("llm-compare")'
    - materials-discovery/src/materials_discovery/cli.py contains "llm_campaign_comparison_path"
    - materials-discovery/src/materials_discovery/llm/compare.py formats summary_lines for operator output
    - materials-discovery/tests/test_llm_compare_cli.py exists
    - the compare CLI regression command exits 0
  </acceptance_criteria>
  <done>The workflow now has a durable compare surface that produces both machine-readable artifacts and useful CLI summaries.</done>
</task>

</tasks>

