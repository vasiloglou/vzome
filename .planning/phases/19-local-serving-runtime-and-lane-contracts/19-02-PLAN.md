---
phase: 19-local-serving-runtime-and-lane-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - materials-discovery/src/materials_discovery/llm/generate.py
  - materials-discovery/src/materials_discovery/llm/launch.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/tests/test_llm_generate_core.py
  - materials-discovery/tests/test_llm_generate_cli.py
  - materials-discovery/tests/test_llm_launch_core.py
  - materials-discovery/tests/test_llm_launch_cli.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-13, LLM-14, OPS-08]

must_haves:
  truths:
    - "llm-generate remains the single candidate-generation engine; manual local serving is an additive overlay over the existing runtime."
    - "Campaign launch must reuse the same lane-resolution rules as manual generation rather than inventing a second selection path."
    - "No requested local or specialized lane may silently downgrade to the baseline backend tuple; fallback must be explicitly configured."
    - "Run manifests and launch artifacts must record enough serving identity that operators can tell which lane and endpoint actually ran."
    - "When no lane is requested and no default lane is configured, the current backend tuple remains the backend_default path for backward compatibility."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/generate.py"
      provides: "Manual local-lane execution through the existing llm-generate path"
      exports:
        [
          "generate_llm_candidates",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/launch.py"
      provides: "Shared lane-resolution and fallback semantics for manual generation and campaign launch"
      exports:
        [
          "resolve_campaign_model_lane",
          "resolve_campaign_launch",
          "resolve_serving_lane",
        ]
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator-facing --model-lane support and early local-serving diagnostics"
    - path: "materials-discovery/tests/test_llm_generate_cli.py"
      provides: "Manual CLI regression coverage for local-lane selection and failure behavior"
    - path: "materials-discovery/tests/test_llm_launch_cli.py"
      provides: "Launch CLI regression coverage for explicit lane resolution and local-serving failures"
  key_links:
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/runtime.py"
      via: "manual generation and launch must validate local-lane readiness before provider attempts begin"
      pattern: "validate_llm_adapter_ready|resolve_llm_adapter"
    - from: "materials-discovery/src/materials_discovery/llm/generate.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "run manifests need additive serving identity and lane-source provenance"
      pattern: "LlmRunManifest|LlmServingIdentity"
    - from: "materials-discovery/src/materials_discovery/llm/launch.py"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "launch lane selection must honor default and explicit fallback policy from config"
      pattern: "default_model_lane|fallback_model_lane|model_lanes"
---

<objective>
Execute local and lane-aware serving through the current manual and campaign
generation paths.

Purpose: satisfy the real operator-facing Phase 19 requirements by making
`mdisc llm-generate` and `mdisc llm-launch` choose local or specialized lanes
deterministically, fail early when those lanes are unavailable, and record what
actually ran.

Output: shared lane-resolution helpers, `--model-lane` support, local-serving
preflight diagnostics, and additive run/launch artifact identity.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-CONTEXT.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-VALIDATION.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-01-PLAN.md

@materials-discovery/src/materials_discovery/llm/generate.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared serving-lane resolution and thread rich identity into generate and launch artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_generate_core.py,
    materials-discovery/tests/test_llm_launch_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/tests/test_llm_generate_core.py,
    materials-discovery/tests/test_llm_launch_core.py,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: manual generation can target an explicitly requested configured lane and still produce standard candidate and manifest outputs
    - Test 2: when no lane is requested, generation uses `default_model_lane` if configured, otherwise preserves the legacy backend tuple as `backend_default`
    - Test 3: unavailable requested lanes fail unless `fallback_model_lane` is explicitly configured
    - Test 4: explicit fallback records that a configured fallback was used rather than reporting a generic baseline fallback
    - Test 5: campaign launch reuses the same lane-resolution semantics and writes the same serving identity shape as manual generation
    - Test 6: existing manual generation and Phase 11 launch behavior remain green when configs do not declare any model lanes
  </behavior>
  <action>
Create or extend one shared lane-resolution helper, such as
`resolve_serving_lane(...)`, that both manual generation and campaign launch
can call.

Concrete semantics:
- requested lane wins if configured and valid
- otherwise `default_model_lane` may be used when no lane was explicitly requested
- otherwise `fallback_model_lane` may be used only when it is explicitly configured
- otherwise the existing backend tuple is the `backend_default` path
- requested unavailable lanes without explicit fallback must fail clearly

Update `materials-discovery/src/materials_discovery/llm/generate.py` so
`generate_llm_candidates(...)` can accept additive lane-selection input and
record the resolved serving identity in the run manifest without changing the
standard candidate JSONL contract.

Update `materials-discovery/src/materials_discovery/llm/launch.py` so campaign
launch resolution uses the same helper and records the same serving identity
shape on `LlmCampaignResolvedLaunch` / `LlmCampaignLaunchSummary`.

Keep current behavior intact when:
- no lane is requested
- no `model_lanes` are configured
- the config is still using the old hosted/mock path

Extend `materials-discovery/tests/test_llm_generate_core.py` and
`materials-discovery/tests/test_llm_launch_core.py` for the semantics above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_core.py tests/test_llm_launch_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/generate.py accepts additive lane-selection input
    - materials-discovery/src/materials_discovery/llm/launch.py contains a shared serving-lane resolution helper
    - launch and manual generation no longer rely on implicit "baseline_fallback" for new writes
    - run manifests record richer serving identity while preserving existing adapter/provider/model fields
    - the core local-lane regression command exits 0
  </acceptance_criteria>
  <done>Manual generation and campaign launch now resolve serving lanes through one deterministic contract and record what actually ran.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CLI lane selection and early local-serving diagnostics into llm-generate and llm-launch</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_generate_cli.py,
    materials-discovery/tests/test_llm_launch_cli.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/tests/test_llm_generate_cli.py,
    materials-discovery/tests/test_llm_launch_cli.py,
    materials-discovery/tests/test_cli.py,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-generate --model-lane ...` resolves a configured lane and still prints the standard summary payload shape
    - Test 2: local-serving readiness errors are emitted before generation attempts and include lane and endpoint context
    - Test 3: `llm-launch` records local-serving identity and refuses unavailable lanes unless fallback is explicitly configured
    - Test 4: launch failures still preserve the existing audit behavior rather than skipping artifact writing entirely
    - Test 5: the shared CLI suite stays green and legacy configs without model lanes remain valid
  </behavior>
  <action>
Add an additive `--model-lane` option to
`materials-discovery/src/materials_discovery/cli.py` for `llm-generate`.

`llm-generate` behavior:
- load the config exactly as today
- resolve the requested/default/fallback/backend-default lane through the
  shared helper
- run local-serving readiness validation before candidate-generation attempts
  when the selected adapter is `openai_compat_v1`
- pass the resolved serving identity into `generate_llm_candidates(...)`
- keep the standard summary JSON contract intact

`llm-launch` behavior:
- reuse the same readiness validation for the resolved lane before provider execution
- preserve the current launch summary / resolved launch artifacts
- add the richer serving identity and more precise lane-source values to those
  artifacts
- emit especially clear errors when a requested local or specialized lane is
  unavailable and no explicit fallback is configured

Update `materials-discovery/tests/test_llm_generate_cli.py`,
`materials-discovery/tests/test_llm_launch_cli.py`, and
`materials-discovery/tests/test_cli.py` for the manual and launch CLI behavior
above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_cli.py tests/test_llm_launch_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains the "model_lane" option on llm-generate
    - local-serving readiness failures mention the lane and endpoint/path before provider attempts begin
    - llm-launch writes richer serving identity into resolved launch or launch summary artifacts
    - legacy llm-generate invocation without --model-lane still works
    - the CLI regression command exits 0
  </acceptance_criteria>
  <done>Operators can now target configured local or specialized lanes through the existing CLI without losing early diagnostics or standard artifact behavior.</done>
</task>

</tasks>
