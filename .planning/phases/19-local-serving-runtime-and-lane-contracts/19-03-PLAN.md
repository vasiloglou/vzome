---
phase: 19-local-serving-runtime-and-lane-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - materials-discovery/src/materials_discovery/llm/replay.py
  - materials-discovery/configs/systems/al_cu_fe_llm_local.yaml
  - materials-discovery/configs/systems/sc_zn_llm_local.yaml
  - materials-discovery/developers-docs/configuration-reference.md
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/tests/test_llm_replay_core.py
  - materials-discovery/tests/test_llm_generate_cli.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-13, LLM-14, OPS-08]

must_haves:
  truths:
    - "Phase 19 does not add process management; example configs and docs must say the local server is already running."
    - "Phase 19 does not claim full local/specialized replay workflow parity; it only makes the new identity fields replay-safe and non-breaking."
    - "Off-the-shelf specialized materials models are not assumed Zomic-native; docs and configs must not imply otherwise."
    - "Committed example configs should be good operator references and test fixtures, not hidden one-off experiments."
    - "Existing mock and hosted paths must remain green after the local-serving additions land."
    - "Docs changes in Phase 19 must stay additive clarifications and examples, not broad rewrites of the shipped operator guidance."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/replay.py"
      provides: "Replay-safe handling of richer serving identity without silent local-lane drift"
      exports:
        [
          "build_replay_config",
          "build_replay_campaign_metadata",
        ]
    - path: "materials-discovery/configs/systems/al_cu_fe_llm_local.yaml"
      provides: "Committed example of a local-serving system config"
    - path: "materials-discovery/developers-docs/configuration-reference.md"
      provides: "Operator-visible reference for local-serving config fields and fallback policy"
    - path: "materials-discovery/developers-docs/llm-integration.md"
      provides: "Phase 19 serving architecture and scope boundaries"
    - path: "materials-discovery/tests/test_llm_replay_core.py"
      provides: "Replay compatibility coverage for new serving identity fields and local endpoint drift"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/replay.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "replay must preserve or reject the new serving identity fields explicitly rather than losing them"
      pattern: "LlmServingIdentity|LlmCampaignResolvedLaunch|LlmCampaignLaunchSummary"
    - from: "materials-discovery/developers-docs/configuration-reference.md"
      to: "materials-discovery/configs/systems/al_cu_fe_llm_local.yaml"
      via: "docs and committed example configs must tell the same local-serving story"
      pattern: "default_model_lane|fallback_model_lane|openai_compat_v1"
    - from: "materials-discovery/developers-docs/pipeline-stages.md"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "docs must reflect the actual operator contract for llm-generate and llm-launch with local lanes"
      pattern: "llm-generate|llm-launch|model-lane"
---

<objective>
Finish the local-serving contract with replay-safe identity handling, committed
example configs, and operator-facing docs that reflect the real Phase 19
boundary.

Purpose: make the new local-serving seam understandable and reusable for
operators without breaking strict replay assumptions or the shipped hosted/mock
workflow.

Output: replay-safe serving identity handling, committed local config examples,
docs for local setup and lane selection, and a focused backward-compatibility
proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-CONTEXT.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-VALIDATION.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-01-PLAN.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-02-PLAN.md

@materials-discovery/src/materials_discovery/llm/replay.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/developers-docs/configuration-reference.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make replay and committed configs safe with the new local-serving identity</name>
  <files>
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/configs/systems/al_cu_fe_llm_local.yaml,
    materials-discovery/configs/systems/sc_zn_llm_local.yaml,
    materials-discovery/tests/test_llm_replay_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/tests/test_llm_replay_core.py,
    materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml,
    materials-discovery/configs/systems/sc_zn_llm_mock.yaml,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: replay can rebuild a config from recorded local-serving identity when the current config still offers a compatible lane
    - Test 2: replay distinguishes hard identity drift from transport or environment drift instead of treating every local-field difference the same way
    - Test 3: committed local example configs validate through `SystemConfig` without requiring a live server
    - Test 4: legacy v1.1 launch bundles using `baseline_fallback` still load successfully
  </behavior>
  <action>
Update `materials-discovery/src/materials_discovery/llm/replay.py` so replay is
safe with the richer serving identity from Plans 01 and 02.

Concrete replay behavior:
- preserve the recorded adapter/provider/model tuple as today
- treat adapter/provider/model and `checkpoint_id` when present as hard replay
  identity; mismatches on those fields must fail clearly
- treat endpoint/path and other transport/environment details distinctly from
  hard identity, so replay can surface transport drift without pretending the
  model/checkpoint changed
- when local-serving identity includes endpoint, revision, checkpoint, or local
  path detail, use those fields only when the current config still offers a
  compatible lane or backend-default path
- if the current config no longer matches the hard local-serving identity,
  fail clearly instead of silently replaying against a different target
- accept historical launch artifacts that still use the old lane-source value

Add committed example configs:
- `materials-discovery/configs/systems/al_cu_fe_llm_local.yaml`
- `materials-discovery/configs/systems/sc_zn_llm_local.yaml`

These example configs must:
- assume the local server is already running
- declare explicit model lanes
- show a placeholder local endpoint such as `http://localhost:8000` plus any
  optional checkpoint or revision identity fields
- avoid any implication that the CLI starts or supervises the server

Extend `materials-discovery/tests/test_llm_replay_core.py` for the behaviors
above. All endpoint use in tests must be monkeypatched; no Phase 19 test may
contact a live local server.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_replay_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/replay.py preserves or explicitly rejects new local-serving identity fields
    - committed local example configs exist under materials-discovery/configs/systems/
    - replay tests cover legacy baseline_fallback loading plus hard-identity mismatch vs transport/environment drift
    - the replay/config regression command exits 0
  </acceptance_criteria>
  <done>The new local-serving contract is replay-safe enough for Phase 19 and shipped with committed example configs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document the operator-facing local-serving contract and prove backward compatibility</name>
  <files>
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_llm_generate_cli.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/tests/test_llm_generate_cli.py,
    materials-discovery/tests/test_cli.py,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: docs explain that Phase 19 assumes the local server is already running and does not manage processes
    - Test 2: docs explain `--model-lane`, `default_model_lane`, explicit fallback behavior, and the richer serving-identity fields
    - Test 3: docs explicitly say specialized-materials lanes are not assumed Zomic-native yet
    - Test 4: a focused CLI regression proves the existing no-lane hosted/mock path still works after local-serving additions
  </behavior>
  <action>
Update docs in additive sections only:
- `materials-discovery/developers-docs/configuration-reference.md`
- `materials-discovery/developers-docs/llm-integration.md`
- `materials-discovery/developers-docs/pipeline-stages.md`

Document:
- the `openai_compat_v1` local-serving seam
- the fact that the local server must already be running
- `--model-lane` manual usage
- `default_model_lane` and explicit `fallback_model_lane`
- what serving identity is recorded
- that specialized-materials lanes in v1.2 may be generation-adjacent or
  evaluation-focused, not necessarily Zomic-native generators
- that Phase 19 changes the serving contract, not the operator governance or
  process-management boundary

Add one focused backward-compatibility regression in
`materials-discovery/tests/test_llm_generate_cli.py` and/or
`materials-discovery/tests/test_cli.py` that proves existing manual
generation with the old configs still works without selecting a lane and still
emits the standard summary/manifests.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - configuration-reference.md documents the local-serving fields and explicit fallback policy
    - llm-integration.md documents the local server assumption and non-Zomic-native specialized-lane scope
    - pipeline-stages.md reflects the actual manual generation contract with model-lane selection
    - docs changes are additive clarifications rather than broad rewrites
    - the focused backward-compatibility CLI regression command exits 0
  </acceptance_criteria>
  <done>Operators have documented local-serving guidance, committed example configs, and proof that the existing hosted/mock path still behaves as expected.</done>
</task>

</tasks>
