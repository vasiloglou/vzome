---
phase: 19-local-serving-runtime-and-lane-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/common/schema.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/runtime.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_launch_schema.py
  - materials-discovery/tests/test_llm_runtime.py
autonomous: true
requirements: [LLM-13, LLM-14, OPS-08]

must_haves:
  truths:
    - "Phase 19 adds local-serving capability by extending the existing llm runtime seam, not by introducing a second generator."
    - "The first local-serving cut targets an already-running OpenAI-compatible HTTP server, not in-process transformers."
    - "Transport defaults belong in backend config; lane selection stays under llm_generate.model_lanes."
    - "Requested local or specialized lanes must fail clearly unless fallback is explicitly configured."
    - "Older launch/replay artifacts that still use resolved_model_lane_source=baseline_fallback must remain readable even if new writes use more precise source values."
    - "New writes may populate serving_identity, but legacy launch and replay artifacts must remain readable when serving_identity is absent or None."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/common/schema.py"
      provides: "Additive local-serving config and lane-selection contract"
      exports:
        [
          "BackendConfig",
          "LlmModelLaneConfig",
          "LlmGenerateConfig",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed serving-identity and lane-source contract for run and launch artifacts"
      exports:
        [
          "LlmServingIdentity",
          "LlmRunManifest",
          "LlmCampaignResolvedLaunch",
          "LlmCampaignLaunchSummary",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/runtime.py"
      provides: "OpenAI-compatible local adapter and explicit readiness diagnostics"
      exports:
        [
          "OpenAICompatLlmAdapter",
          "resolve_llm_adapter",
          "validate_llm_adapter_ready",
        ]
    - path: "materials-discovery/tests/test_llm_runtime.py"
      provides: "Focused runtime coverage for local-serving adapter, readiness probe, and operator-facing failures"
  key_links:
    - from: "materials-discovery/src/materials_discovery/common/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/runtime.py"
      via: "backend transport defaults and lane identity must feed the runtime seam without bypassing existing backend fields"
      pattern: "llm_api_base|llm_request_timeout_s|llm_probe_timeout_s|llm_probe_path"
    - from: "materials-discovery/src/materials_discovery/llm/runtime.py"
      to: "materials-discovery/src/materials_discovery/llm/generate.py"
      via: "generate stays the single runtime authority and only consumes adapters the runtime resolves"
      pattern: "resolve_llm_adapter|validate_llm_adapter_ready"
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/launch.py"
      via: "launch and replay artifacts must be able to carry richer serving identity without breaking prior contracts"
      pattern: "LlmCampaignResolvedLaunch|LlmCampaignLaunchSummary"
---

<objective>
Define the additive local-serving contract before CLI or launch integration
widens the regression surface.

Purpose: pin the schema, adapter, and readiness-diagnostic behavior for local
OpenAI-compatible serving so later plans can wire manual generation and launch
against one stable runtime contract.

Output: additive backend/lane config fields, typed serving-identity models,
`openai_compat_v1` runtime support, and focused schema/runtime regression
coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-CONTEXT.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
@.planning/phases/19-local-serving-runtime-and-lane-contracts/19-VALIDATION.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/src/materials_discovery/llm/generate.py
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/configuration-reference.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config and artifact schemas for local-serving identity and explicit lane policy</name>
  <files>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_launch_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/tests/test_llm_launch_schema.py,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: configs can declare local-serving transport defaults on backend without breaking existing mock or hosted YAMLs
    - Test 2: lane configs can carry additive local-serving identity such as endpoint override, checkpoint ID, model revision, and local path
    - Test 3: `llm_generate` can declare `default_model_lane` and `fallback_model_lane`, and unknown lane keys are still rejected
    - Test 4: run/launch artifact schemas accept richer serving identity plus more explicit lane-source values while remaining able to read legacy `baseline_fallback`
    - Test 5: blank optional identity strings are normalized or rejected consistently so manifests do not record meaningless whitespace
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/common/schema.py` additively.

In `BackendConfig`, add transport defaults for local serving:
- `llm_request_timeout_s: float = 120.0`
- `llm_probe_timeout_s: float = 5.0`
- `llm_probe_path: str | None = None`

Normalize and validate them so:
- timeout values must be > 0
- blank `llm_probe_path` normalizes to `None`
- `llm_api_base` and any lane-level `api_base` normalize away trailing `/`

Extend `LlmModelLaneConfig` with additive identity fields:
- `checkpoint_id: str | None = None`
- `model_revision: str | None = None`
- `local_model_path: str | None = None`

Extend `LlmGenerateConfig` additively with:
- `default_model_lane: Literal["general_purpose", "specialized_materials"] | None = None`
- `fallback_model_lane: Literal["general_purpose", "specialized_materials"] | None = None`

In `materials-discovery/src/materials_discovery/llm/schema.py`, add a typed
`LlmServingIdentity` model and thread it additively into:
- `LlmRunManifest`
- `LlmCampaignResolvedLaunch`
- `LlmCampaignLaunchSummary`

The identity contract must carry at least:
- requested lane
- resolved lane
- resolved-lane source
- adapter
- provider
- model
- effective API base or local path
- checkpoint ID
- model revision
- local model path

Treat `serving_identity` as additive and optional on artifact models:
- new writes may populate `LlmServingIdentity`
- legacy or partial artifacts must still deserialize with `serving_identity=None`
- existing flat adapter/provider/model-style fields must remain alongside the
  nested identity for backward compatibility and audit readability

Update lane-source validation so new artifacts can write:
- `configured_lane`
- `default_lane`
- `configured_fallback`
- `backend_default`

But old artifacts using `baseline_fallback` must still deserialize cleanly.

Update `materials-discovery/src/materials_discovery/llm/__init__.py` exports
for any new models.

Extend `materials-discovery/tests/test_llm_launch_schema.py` to cover the new
config and artifact behavior.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_launch_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/common/schema.py contains "llm_request_timeout_s"
    - materials-discovery/src/materials_discovery/common/schema.py contains "default_model_lane"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmServingIdentity"
    - new artifact writes can populate `serving_identity` while legacy reads still succeed with `serving_identity=None`
    - materials-discovery/src/materials_discovery/llm/schema.py accepts legacy "baseline_fallback" on read
    - materials-discovery/tests/test_llm_launch_schema.py covers local-serving config and lane-source compatibility
    - the schema regression command exits 0
  </acceptance_criteria>
  <done>The repo has an additive local-serving schema contract and artifact identity model that later plans can execute against safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an OpenAI-compatible local adapter with explicit readiness diagnostics</name>
  <files>
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_runtime.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/tests/test_llm_runtime.py,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-CONTEXT.md,
    .planning/phases/19-local-serving-runtime-and-lane-contracts/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `resolve_llm_adapter(...)` can return a local OpenAI-compatible adapter in real mode
    - Test 2: the local adapter lazily imports `httpx`, just like the existing Anthropic adapter
    - Test 3: missing endpoint or model fails before provider execution with a clear operator-facing error
    - Test 4: readiness validation probes the configured endpoint/path and surfaces endpoint, lane, adapter, and status/detail context on failure
    - Test 5: the default readiness probe uses `/v1/models` when no explicit probe path is configured
    - Test 6: OpenAI-compatible responses prefer `choices[0].message.content` and only fall back to `choices[0].text` as a compatibility path
    - Test 7: mock and anthropic adapters continue to resolve and behave exactly as before
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/runtime.py` with one
additive real-mode adapter for local serving:
- adapter key: `openai_compat_v1`
- protocol: OpenAI-compatible `/v1/chat/completions`

Implement an adapter class that:
- takes provider, model, API base, timeout settings, and optional probe path
- formats a standard chat-completions request from `LlmGenerationRequest`
- extracts the first text content from the OpenAI-compatible response,
  preferring `choices[0].message.content` and only accepting `choices[0].text`
  as an explicit compatibility fallback
- keeps `httpx` import lazy inside runtime methods

Add an explicit readiness helper such as `validate_llm_adapter_ready(...)` that:
- is safe to call before generation attempts
- uses the configured endpoint and probe path
- defaults to probing `GET /v1/models` when no explicit probe path is configured
- does not start or manage any local process
- raises operator-facing `RuntimeError` messages that name the requested lane
  when available, plus endpoint/path and adapter key
- tells the operator to confirm the local server is already running when
  connectivity fails

Update `resolve_llm_adapter(...)` so:
- `llm_fixture_v1` and `anthropic_api_v1` keep working unchanged
- `openai_compat_v1` requires a nonblank model and endpoint
- unsupported adapters still fail clearly
- the runtime continues to lazy-import any new `httpx` dependency rather than
  making it import-time mandatory for mock-only or hosted-only paths

Extend `materials-discovery/tests/test_llm_runtime.py` with fully offline,
monkeypatched coverage for:
- local adapter request shape
- readiness success
- readiness failures
- default `/v1/models` probe behavior
- supported OpenAI-compatible response shapes
- endpoint override handling
- backward compatibility for mock and anthropic adapters
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_runtime.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "openai_compat_v1"
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "validate_llm_adapter_ready"
    - readiness validation defaults to `/v1/models` when no probe path is configured
    - OpenAI-compatible response parsing is explicit about supported response shapes
    - local readiness errors mention endpoint/path and adapter identity
    - anthropic and mock runtime tests remain present
    - materials-discovery/tests/test_llm_runtime.py covers the local adapter without live network calls
    - the runtime regression command exits 0
  </acceptance_criteria>
  <done>The runtime seam can now target an already-running local OpenAI-compatible server and fail early when the local lane is not actually ready.</done>
</task>

</tasks>
