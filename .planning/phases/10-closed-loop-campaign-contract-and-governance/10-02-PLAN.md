---
phase: 10-closed-loop-campaign-contract-and-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - materials-discovery/src/materials_discovery/llm/suggest.py
  - materials-discovery/src/materials_discovery/llm/campaigns.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/tests/test_llm_suggest_core.py
  - materials-discovery/tests/test_llm_suggest_cli.py
autonomous: true
requirements: [LLM-06, OPS-05]

must_haves:
  truths:
    - "Phase 10 keeps llm-suggest as a dry-run operator aid while making its outputs materializable."
    - "Every proposal must stay system-scoped and map into the three locked action families only."
    - "Proposal output should support both general-purpose and specialized-materials model lanes without hard-coding one vendor."
    - "Dry-run proposal generation must never mutate configs, candidate data, or active-learning artifacts."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/campaigns.py"
      provides: "Proposal construction helpers and acceptance-pack to proposal mapping logic"
      exports:
        [
          "build_campaign_proposals",
          "summarize_campaign_proposals",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/suggest.py"
      provides: "Dry-run suggestion bundle writing built on top of typed proposals"
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Updated llm-suggest command contract"
    - path: "materials-discovery/tests/test_llm_suggest_core.py"
      provides: "Core proposal-mapping coverage"
    - path: "materials-discovery/tests/test_llm_suggest_cli.py"
      provides: "CLI coverage for bundle/proposal artifact writing"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/campaigns.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "typed proposal and action-family models"
      pattern: "LlmCampaignProposal|LlmCampaignAction|LlmCampaignSuggestion"
    - from: "materials-discovery/src/materials_discovery/llm/campaigns.py"
      to: "materials-discovery/src/materials_discovery/llm/acceptance.py"
      via: "acceptance-pack metrics determine failing metrics and proposal priority"
      pattern: "LlmAcceptancePack|failing_metrics|report_release_gate_ready"
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/storage.py"
      via: "default suggestion and proposal artifact paths"
      pattern: "llm_acceptance_suggestion_path|llm_acceptance_proposal_path"
---

<objective>
Upgrade `mdisc llm-suggest` from plain-language advice to typed dry-run
campaign proposals.

Purpose: make the existing operator-facing suggestion surface specific enough to
be approved later, while keeping the command non-executing and safe by default.

Output: typed proposal-building helpers, a suggestion bundle plus per-system
proposal artifacts, CLI wiring, and focused core/CLI regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-CONTEXT.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-VALIDATION.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-01-PLAN.md

@materials-discovery/src/materials_discovery/llm/acceptance.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/suggest.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map acceptance-pack evidence into typed system-scoped proposals</name>
  <files>
    materials-discovery/src/materials_discovery/llm/campaigns.py,
    materials-discovery/src/materials_discovery/llm/suggest.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_suggest_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/acceptance.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/suggest.py,
    materials-discovery/developers-docs/llm-integration.md,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: each system in an acceptance pack produces at most one system-scoped proposal
    - Test 2: parse/compile failures map to prompt/conditioning actions with high priority
    - Test 3: shortlist/validation pass-through weakness maps to composition-window and/or prompt-conditioning actions
    - Test 4: synthesizability weakness can target the specialized-materials lane while still using the typed action families
    - Test 5: release-gate-not-ready systems remain dry-run and low- or medium-priority, not auto-approved
    - Test 6: proposal summaries point at deterministic per-system proposal paths
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/campaigns.py` and move
the Phase 10 proposal-mapping logic there.

Implement:
- `build_campaign_proposals(pack: LlmAcceptancePack, *, acceptance_pack_path: Path, system_config_path: Path | None = None) -> list[LlmCampaignProposal]`
- `summarize_campaign_proposals(pack: LlmAcceptancePack, proposals: list[LlmCampaignProposal], *, proposal_paths: dict[str, Path]) -> LlmCampaignSuggestion`

Proposal-building rules:
- one proposal per `pack.systems[]`
- `proposal_id` should be deterministic from `pack_id` and system slug
- `priority` stays high/medium/low
- `overall_status` follows the pack status semantics already used by Phase 9

Map the current heuristic branches into typed actions:
- parse or compile weakness:
  - high-priority `prompt_conditioning`
  - evidence metrics include `parse_success_rate` and/or `compile_success_rate`
  - preferred model lane defaults to `general_purpose`
- shortlist or validation weakness:
  - high-priority `composition_window` and/or `prompt_conditioning`
  - evidence metrics include `shortlist_pass_rate` and/or `validation_pass_rate`
- synthesizability weakness:
  - medium-priority `prompt_conditioning`
  - preferred model lane `specialized_materials`
  - `specialized_model_family` may be a stable string like `csllm_inspired`
- report gate not ready:
  - medium-priority dry-run action without execution semantics
- fully healthy systems:
  - low-priority exploratory action that still remains dry-run and approvable only later

Preserve the current `materials-discovery/src/materials_discovery/llm/suggest.py`
module as the thin writing/orchestration layer:
- `build_llm_suggestions(...)` should now build a `LlmCampaignSuggestion`
- it should write the bundle plus per-proposal files, not just one advice blob

Export the new helpers from `materials-discovery/src/materials_discovery/llm/__init__.py`.

Create `materials-discovery/tests/test_llm_suggest_core.py` with focused cases
for the branches above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_suggest_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/campaigns.py contains "def build_campaign_proposals"
    - materials-discovery/src/materials_discovery/llm/campaigns.py contains "def summarize_campaign_proposals"
    - materials-discovery/src/materials_discovery/llm/suggest.py writes a typed suggestion bundle and per-system proposal files
    - materials-discovery/tests/test_llm_suggest_core.py contains branch coverage for parse/compile, pass-through, and synthesizability cases
    - The core suggestion test command exits 0
  </acceptance_criteria>
  <done>Acceptance-pack analysis now produces typed, system-scoped, dry-run campaign proposals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the llm-suggest CLI to emit dry-run proposal artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/suggest.py,
    materials-discovery/tests/test_llm_suggest_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/suggest.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/tests/test_cli.py,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-suggest` remains dry-run and exits 0 on a valid acceptance pack
    - Test 2: the command writes `suggestions.json` by default and also creates per-system proposal files under `proposals/`
    - Test 3: the command prints typed suggestion JSON rather than plain-language-only items
    - Test 4: invalid acceptance-pack input still exits 2 with a clear error
    - Test 5: optional config-path enrichment, if implemented, remains optional and does not break the current acceptance-pack-only command path
  </behavior>
  <action>
Update `materials-discovery/src/materials_discovery/cli.py` so `llm-suggest`:
- keeps the same command name
- keeps acceptance-pack input mandatory
- stays non-executing and dry-run by default
- writes the suggestion bundle to `suggestions.json` by default using the new
  storage helper
- writes per-system proposal files under the acceptance-pack `proposals/` dir
- prints the typed suggestion bundle JSON to stdout

You may add an optional `--config PATH` enrichment input if it helps record
baseline config lineage or enrich proposal hints, but it must remain optional so
the existing acceptance-pack-only workflow still works.

Create `materials-discovery/tests/test_llm_suggest_cli.py` for:
- happy-path bundle/proposal writing
- invalid input handling
- stdout summary contract

If shared CLI behavior changes enough to justify it, update
`materials-discovery/tests/test_cli.py` only after the focused CLI test exists.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_suggest_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py still contains the "llm-suggest" command
    - running llm-suggest now writes proposal artifacts in addition to the top-level bundle
    - the CLI test command exits 0
    - no Phase 10 code path launches llm-generate or edits active-learning outputs
  </acceptance_criteria>
  <done>`mdisc llm-suggest` remains safe and operator-facing, but now emits materializable typed proposal artifacts.</done>
</task>

</tasks>
