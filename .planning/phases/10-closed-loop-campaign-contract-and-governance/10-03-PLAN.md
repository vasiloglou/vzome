---
phase: 10-closed-loop-campaign-contract-and-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - materials-discovery/src/materials_discovery/llm/campaigns.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/tests/test_llm_campaign_spec.py
  - materials-discovery/tests/test_llm_approve_cli.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-06, OPS-05]

must_haves:
  truths:
    - "Approval must be a real governance boundary, not a disguised launch path."
    - "Campaign specs must be self-contained enough for Phase 11 to launch later without rereading the original suggestion heuristics."
    - "Approval should pin source config lineage because acceptance packs alone do not contain the full launch baseline."
    - "Phase 10 ends at spec materialization; no execution bridge into llm-generate belongs here."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/campaigns.py"
      provides: "Approval-materialization and campaign-spec construction helpers"
      exports:
        [
          "create_campaign_approval",
          "materialize_campaign_spec",
        ]
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator approval/spec CLI for the new governance boundary"
    - path: "materials-discovery/tests/test_llm_campaign_spec.py"
      provides: "Deterministic spec-materialization coverage"
    - path: "materials-discovery/tests/test_llm_approve_cli.py"
      provides: "CLI coverage proving approval writes artifacts but does not launch runs"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/campaigns.py"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "system config baseline snapshot and config hashing"
      pattern: "SystemConfig|config_sha256|composition_bounds|llm_generate"
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/generate.py"
      via: "must remain decoupled in Phase 10"
      pattern: "generate_llm_candidates"
    - from: "materials-discovery/developers-docs/pipeline-stages.md"
      to: "materials-discovery/developers-docs/llm-integration.md"
      via: "same dry-run versus approve boundary"
      pattern: "llm-suggest|llm-approve|campaign spec"
---

<objective>
Materialize the governance boundary: turn typed dry-run proposals into separate
approval artifacts and self-contained campaign specs without launching anything.

Purpose: close `LLM-06` and `OPS-05` by giving operators a real approval step
and a durable spec artifact that later phases can execute and replay.

Output: approval/spec helpers, a non-launching approval CLI, docs updates, and
focused governance-boundary regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-CONTEXT.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-VALIDATION.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-01-PLAN.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-02-PLAN.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/common/manifest.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/campaigns.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add approval and self-contained campaign-spec materialization helpers</name>
  <files>
    materials-discovery/src/materials_discovery/llm/campaigns.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_campaign_spec.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/campaigns.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/common/manifest.py,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: approving a proposal writes a separate typed approval artifact
    - Test 2: rejected proposals still produce an approval artifact but do not produce a campaign spec
    - Test 3: approved proposals produce a `campaign_spec.json` under `data/llm_campaigns/{campaign_id}/`
    - Test 4: the campaign spec captures source config path/hash plus a launch baseline snapshot
    - Test 5: the spec lineage references acceptance-pack, proposal, approval, and eval-set inputs
    - Test 6: spec materialization is deterministic for the same proposal/config/operator inputs
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/campaigns.py` with
helpers:
- `create_campaign_approval(...) -> LlmCampaignApproval`
- `materialize_campaign_spec(...) -> LlmCampaignSpec`

Materialization rules:
- approval requires a typed `LlmCampaignProposal`
- approval requires an explicit operator identity string
- approval may be `approved` or `rejected`
- approved proposals create a stable `campaign_id`
- rejected proposals do not create a spec

For approved proposals, build `LlmCampaignSpec` from:
- the proposal
- the approval artifact
- a required system config input
- a config hash from `materials_discovery.common.manifest.config_sha256`

The spec must pin a launch baseline snapshot from the config:
- `system`
- `template_family`
- `default_count`
- `composition_bounds`
- relevant `llm_generate` baseline fields when present

The spec should carry the original typed actions unchanged so Phase 11 can later
resolve them into actual `llm-generate` launch inputs without rereading the
acceptance-pack heuristics.

Create `materials-discovery/tests/test_llm_campaign_spec.py` covering approved
and rejected flows plus deterministic lineage fields.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_campaign_spec.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/campaigns.py contains "def create_campaign_approval"
    - materials-discovery/src/materials_discovery/llm/campaigns.py contains "def materialize_campaign_spec"
    - approved proposals write campaign specs under data/llm_campaigns
    - rejected proposals do not create a spec artifact
    - The campaign-spec test command exits 0
  </acceptance_criteria>
  <done>The repo can turn a dry-run proposal into a separately approved, self-contained campaign spec without launching anything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the operator approval CLI and document the governance boundary</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_llm_approve_cli.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_cli.py,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: there is a CLI command that writes an approval artifact and, when approved, a campaign spec
    - Test 2: the approval command requires explicit operator identity
    - Test 3: the approval command requires a system config path when approving, so the campaign spec can pin a baseline
    - Test 4: the approval command does not call `generate_llm_candidates` or launch a downstream pipeline stage
    - Test 5: docs explain the new dry-run versus approval boundary and the new artifact paths
  </behavior>
  <action>
Add an operator-facing approval command to `materials-discovery/src/materials_discovery/cli.py`.

Recommended command shape:
`mdisc llm-approve --proposal PATH --decision approved|rejected --operator TEXT [--config PATH] [--notes TEXT]`

Behavior:
- load and validate the proposal artifact
- write a separate approval artifact under the acceptance-pack approvals dir
- when `--decision approved`, require `--config` and write a self-contained
  campaign spec under `data/llm_campaigns/{campaign_id}/campaign_spec.json`
- when `--decision rejected`, write only the approval artifact
- print typed JSON summary with approval path and optional spec path

This command must stop at artifact materialization. It must not:
- call `generate_llm_candidates`
- call `evaluate_llm_candidates`
- write candidate JSONL
- alter active-learning outputs

Update docs:
- `materials-discovery/developers-docs/llm-integration.md`
- `materials-discovery/developers-docs/pipeline-stages.md`

Document:
- `llm-suggest` remains dry-run
- proposals are immutable dry-run artifacts
- approval is the gateway to a self-contained campaign spec
- launch belongs to the next phase, not Phase 10

Add focused CLI coverage in `materials-discovery/tests/test_llm_approve_cli.py`
and update `materials-discovery/tests/test_cli.py` only as needed to keep the
shared CLI surface green.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_approve_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains an approval command for the Phase 10 governance flow
    - CLI tests prove approval writes artifacts but never launches llm-generate
    - developers-docs/llm-integration.md explains the dry-run versus approval boundary
    - developers-docs/pipeline-stages.md documents the Phase 10 command/artifact flow
    - The CLI/doc regression command exits 0
  </acceptance_criteria>
  <done>Operators can approve or reject typed dry-run proposals and materialize a campaign spec, with the docs reflecting the new governance boundary.</done>
</task>

</tasks>
