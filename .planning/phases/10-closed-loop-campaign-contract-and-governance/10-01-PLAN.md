---
phase: 10-closed-loop-campaign-contract-and-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_campaign_schema.py
  - materials-discovery/tests/test_llm_campaign_storage.py
autonomous: true
requirements: [LLM-06, OPS-05]

must_haves:
  truths:
    - "Phase 10 locks the governance contract before Phase 11 launches anything."
    - "Proposal, approval, and campaign-spec artifacts stay file-backed and additive to the existing LLM surface."
    - "Specialized materials models must be first-class metadata in the contract, but provider/model choice is not the main action vocabulary."
    - "Approval is a separate artifact; dry-run proposals stay immutable."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed campaign proposal, approval, action, lineage, and campaign-spec contracts"
      exports:
        [
          "LlmCampaignAction",
          "LlmCampaignProposal",
          "LlmCampaignApproval",
          "LlmCampaignSpec",
          "LlmCampaignLineage",
          "LlmCampaignSuggestion",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic artifact roots for proposals, approvals, and campaign specs"
      exports:
        [
          "llm_acceptance_proposals_dir",
          "llm_acceptance_proposal_path",
          "llm_acceptance_approvals_dir",
          "llm_acceptance_approval_path",
          "llm_campaign_dir",
          "llm_campaign_spec_path",
        ]
    - path: "materials-discovery/tests/test_llm_campaign_schema.py"
      provides: "Schema coverage for the new governance contract"
    - path: "materials-discovery/tests/test_llm_campaign_storage.py"
      provides: "Artifact layout coverage for the new campaign roots"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "reuse of CompositionBound and existing config-bound primitives"
      pattern: "CompositionBound|LlmGenerateConfig|SystemConfig"
    - from: "materials-discovery/src/materials_discovery/llm/storage.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "artifact roots must match the proposal/approval/spec identity model"
      pattern: "proposal_id|approval_id|campaign_id|pack_id"
---

<objective>
Define the Phase 10 contract foundation: typed proposal, approval, lineage, and
campaign-spec models plus the storage helpers that pin their artifact layout.

Purpose: land the lowest-blast-radius foundation first so later work can revise
`llm-suggest` and add approval/spec materialization on durable types instead of
ad hoc dicts or CLI-only logic.

Output: additive campaign models in `llm/schema.py`, deterministic artifact
helpers in `llm/storage.py`, exports in `llm/__init__.py`, and focused schema
and storage tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-CONTEXT.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
@.planning/phases/10-closed-loop-campaign-contract-and-governance/10-VALIDATION.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/__init__.py
@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/llm/suggest.py
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md

<interfaces>
From llm/schema.py:
```python
class LlmAcceptancePack(BaseModel): ...
class LlmSuggestion(BaseModel): ...
class LlmSuggestionItem(BaseModel): ...
ValidationStatus = Literal["pending", "passed", "failed"]
```

From common/schema.py:
```python
class CompositionBound(BaseModel): ...
class LlmGenerateConfig(BaseModel): ...
class SystemConfig(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed campaign governance models to llm/schema.py</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_campaign_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/suggest.py,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-CONTEXT.md,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: every proposal is system-scoped and references exactly one acceptance pack
    - Test 2: action families are limited to prompt/conditioning, composition-window, and seed/motif variation
    - Test 3: model-lane metadata supports both general-purpose and specialized-materials lanes without requiring a concrete provider
    - Test 4: approval is a separate typed artifact and can represent approved or rejected decisions
    - Test 5: campaign specs pin proposal/approval/acceptance-pack/config lineage and a launch baseline snapshot without implying a launched run
    - Test 6: validators reject blank IDs, blank paths, empty action lists where prohibited, and mismatched action-family payloads
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/schema.py` with a new
Phase 10 contract family. Keep all existing Phase 6-9 models intact.

Add new schema-version constants:
- `DEFAULT_LLM_CAMPAIGN_SUGGESTION_VERSION = "llm-campaign-suggestion/v1"`
- `DEFAULT_LLM_CAMPAIGN_PROPOSAL_VERSION = "llm-campaign-proposal/v1"`
- `DEFAULT_LLM_CAMPAIGN_APPROVAL_VERSION = "llm-campaign-approval/v1"`
- `DEFAULT_LLM_CAMPAIGN_SPEC_VERSION = "llm-campaign-spec/v1"`

Add typed literals:
- `CampaignActionFamily = Literal["prompt_conditioning", "composition_window", "seed_motif_variation"]`
- `CampaignModelLane = Literal["general_purpose", "specialized_materials"]`
- `CampaignDecision = Literal["approved", "rejected"]`

Add action payload models:
- `LlmPromptConditioningActionData`
  - `instruction_delta: str`
  - `conditioning_strategy: str`
  - `target_example_family: str | None = None`
  - `preferred_max_conditioning_examples: int | None = None`
- `LlmCompositionWindowActionData`
  - `window_strategy: str`
  - `focus_species: list[str]`
  - `target_bounds: dict[str, CompositionBound] | None = None`
- `LlmSeedMotifVariationActionData`
  - `variation_strategy: str`
  - `seed_source_hint: str | None = None`
  - `motif_tags: list[str] = Field(default_factory=list)`

Add `LlmCampaignAction` with:
- `action_id: str`
- `family: CampaignActionFamily`
- `title: str`
- `rationale: str`
- `priority: Literal["high", "medium", "low"]`
- `evidence_metrics: list[str] = Field(default_factory=list)`
- `preferred_model_lane: CampaignModelLane`
- `preferred_provider: str | None = None`
- `preferred_model: str | None = None`
- `specialized_model_family: str | None = None`
- one optional payload field for each action family

Validate that:
- IDs and required strings are non-blank
- `evidence_metrics` are normalized and deduped
- only the payload matching `family` is populated
- `specialized_model_family` is optional, but blank strings normalize to `None`

Add proposal/suggestion models:
- `LlmCampaignProposal`
  - schema_version
  - `proposal_id`
  - `pack_id`
  - `system`
  - `generated_at_utc`
  - `acceptance_pack_path`
  - `eval_set_manifest_path: str | None`
  - `generate_comparison_path`
  - `pipeline_comparison_path`
  - `overall_status: Literal["ready", "needs_improvement"]`
  - `priority: Literal["high", "medium", "low"]`
  - `failing_metrics: list[str]`
  - `actions: list[LlmCampaignAction]`
- `LlmCampaignProposalSummary`
  - `proposal_id`
  - `system`
  - `priority`
  - `failing_metrics`
  - `action_families`
  - `proposal_path`
- `LlmCampaignSuggestion`
  - schema_version
  - `pack_id`
  - `overall_status`
  - `generated_at_utc`
  - `proposal_count`
  - `proposals: list[LlmCampaignProposalSummary]`

Add approval/spec models:
- `LlmCampaignApproval`
  - schema_version
  - `approval_id`
  - `proposal_id`
  - `proposal_path`
  - `decision: CampaignDecision`
  - `operator`
  - `decided_at_utc`
  - `notes: str | None = None`
  - `campaign_id: str | None = None`
- `LlmCampaignLaunchBaseline`
  - `system_config_path`
  - `system_config_hash`
  - `system`
  - `template_family`
  - `default_count`
  - `composition_bounds: dict[str, CompositionBound]`
  - `prompt_template: str | None = None`
  - `example_pack_path: str | None = None`
  - `max_conditioning_examples: int | None = None`
  - `seed_zomic_path: str | None = None`
- `LlmCampaignLineage`
  - `acceptance_pack_path`
  - `eval_set_manifest_path: str | None = None`
  - `proposal_path`
  - `approval_path`
  - `source_system_config_path`
  - `source_system_config_hash`
- `LlmCampaignSpec`
  - schema_version
  - `campaign_id`
  - `proposal_id`
  - `approval_id`
  - `system`
  - `created_at_utc`
  - `actions: list[LlmCampaignAction]`
  - `launch_baseline: LlmCampaignLaunchBaseline`
  - `lineage: LlmCampaignLineage`

Add focused tests in `materials-discovery/tests/test_llm_campaign_schema.py`
covering the behaviors above, and export the new models from
`materials-discovery/src/materials_discovery/llm/__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_campaign_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmCampaignAction(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmCampaignProposal(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmCampaignApproval(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmCampaignSpec(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "CampaignModelLane"
    - materials-discovery/src/materials_discovery/llm/__init__.py exports the new Phase 10 models
    - materials-discovery/tests/test_llm_campaign_schema.py contains at least 6 test functions
    - The schema test command exits 0
  </acceptance_criteria>
  <done>The repo has durable typed contracts for dry-run proposals, approvals, and self-contained campaign specs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic storage helpers for suggestion, proposal, approval, and spec artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_campaign_storage.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_llm_corpus_storage.py,
    .planning/phases/10-closed-loop-campaign-contract-and-governance/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: proposal artifacts live under the acceptance-pack root
    - Test 2: approval artifacts live under the acceptance-pack root
    - Test 3: campaign specs live under a dedicated `data/llm_campaigns/{campaign_id}` root
    - Test 4: helper functions accept an optional root override for test isolation
    - Test 5: blank IDs are not silently normalized into bad paths
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/storage.py` with new
helpers that complement the existing acceptance-pack and eval-set roots:

- `llm_acceptance_suggestion_path(pack_id, root=None) -> Path`
- `llm_acceptance_proposals_dir(pack_id, root=None) -> Path`
- `llm_acceptance_proposal_path(pack_id, proposal_id, root=None) -> Path`
- `llm_acceptance_approvals_dir(pack_id, root=None) -> Path`
- `llm_acceptance_approval_path(pack_id, approval_id, root=None) -> Path`
- `llm_campaign_dir(campaign_id, root=None) -> Path`
- `llm_campaign_spec_path(campaign_id, root=None) -> Path`

Keep the current acceptance-pack helper names unchanged so older phases stay
green. Use the same `_artifact_root()` convention and deterministic directory
shape as the existing corpus/eval-set helpers.

Create `materials-discovery/tests/test_llm_campaign_storage.py` that proves:
- proposal and approval helpers root under `data/benchmarks/llm_acceptance/{pack_id}/`
- campaign spec helpers root under `data/llm_campaigns/{campaign_id}/`
- optional root overrides behave correctly

Export the new helpers from `materials-discovery/src/materials_discovery/llm/__init__.py`.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_campaign_storage.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def llm_acceptance_proposals_dir"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def llm_acceptance_proposal_path"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "def llm_campaign_spec_path"
    - materials-discovery/tests/test_llm_campaign_storage.py contains at least 4 test functions
    - The storage test command exits 0
  </acceptance_criteria>
  <done>The repo has a deterministic artifact layout for the new Phase 10 governance surface.</done>
</task>

</tasks>
