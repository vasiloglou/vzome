---
phase: 06-zomic-training-corpus-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py
  - materials-discovery/src/materials_discovery/llm/corpus_builder.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/tests/fixtures/hypodx_approximant_sample.cif
  - materials-discovery/tests/test_llm_cif2zomic.py
  - materials-discovery/tests/test_llm_corpus_builder.py
  - materials-discovery/tests/test_llm_corpus_cli.py
autonomous: true
requirements: [LLM-01]

must_haves:
  truths:
    - "cif2zomic reuses existing CIF and canonical-source seams instead of inventing a new raw parser stack"
    - "The final builder emits two corpora plus inventory, QA, rejects, and manifest outputs under data/llm_corpus/{build_id}"
    - "The build path stays offline and deterministic, using committed fixtures and staged source/reference-pack artifacts"
    - "Phase 6 ends with an operator-facing CLI command that can build the corpus from the committed corpus_v1 config"
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py"
      provides: "Open approximant CIF/canonical-record conversion into corpus examples"
      exports: ["cif_path_to_zomic", "canonical_record_to_zomic"]
    - path: "materials-discovery/src/materials_discovery/llm/corpus_builder.py"
      provides: "Inventory-driven corpus assembly, QA, manifest writing, and summary output"
      exports: ["build_llm_corpus"]
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator-facing llm-corpus build command"
    - path: "materials-discovery/tests/test_llm_cif2zomic.py"
      provides: "Fixture-backed CIF conversion coverage"
    - path: "materials-discovery/tests/test_llm_corpus_builder.py"
      provides: "End-to-end build coverage for syntax and materials corpora"
    - path: "materials-discovery/tests/test_llm_corpus_cli.py"
      provides: "CLI command contract coverage"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py"
      to: "materials-discovery/src/materials_discovery/generator/prototype_import.py"
      via: "reuses CIF orbit expansion and structure extraction"
      pattern: "expand_cif_orbits|parse_cif"
    - from: "materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py"
      to: "materials-discovery/src/materials_discovery/data_sources/adapters/cif_conversion.py"
      via: "reads canonical-source CIF representations and source metadata"
      pattern: "cif_to_canonical_record|structure_representations"
    - from: "materials-discovery/src/materials_discovery/llm/corpus_builder.py"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "mdisc llm-corpus build subcommand"
      pattern: "llm-corpus|CorpusBuildSummary"
---

<objective>
Implement the CIF/open-approximant converter and the end-to-end corpus builder,
then wire the operator-facing build command.

Purpose: Complete Phase 6 by joining the foundation, inventory, QA, and
converter work into one reproducible build path that emits the two corpus
artifacts plus their audit trail.

Output: `cif2zomic`, `build_llm_corpus`, `mdisc llm-corpus build`, and
end-to-end tests proving the build works offline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-RESEARCH.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-VALIDATION.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-02-PLAN.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-03-PLAN.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/manifests.py
@materials-discovery/src/materials_discovery/llm/inventory.py
@materials-discovery/src/materials_discovery/llm/qa.py
@materials-discovery/src/materials_discovery/llm/compiler.py
@materials-discovery/src/materials_discovery/generator/prototype_import.py
@materials-discovery/src/materials_discovery/data_sources/adapters/cif_conversion.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/tests/fixtures/cod_sample.cif

<interfaces>
From prototype_import.py:
```python
def parse_cif(path: Path) -> dict[str, Any]: ...
def expand_cif_orbits(cif_path: Path) -> dict[str, Any]: ...
```

From llm/inventory.py (after 06-02):
```python
def build_inventory(config: CorpusBuildConfig, root: Path | None = None) -> list[dict[str, Any]]: ...
```

From llm/compiler.py (after 06-03):
```python
def compile_zomic_script(zomic_text: str, *, prototype_key: str, system_name: str, template_family: str) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CIF/open-approximant conversion for COD and HYPOD-X-style inputs</name>
  <files>
    materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py,
    materials-discovery/tests/fixtures/hypodx_approximant_sample.cif,
    materials-discovery/tests/test_llm_cif2zomic.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/generator/prototype_import.py,
    materials-discovery/src/materials_discovery/data_sources/adapters/cif_conversion.py,
    materials-discovery/tests/fixtures/cod_sample.cif,
    .planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: cif_path_to_zomic converts the COD fixture into a CorpusExample with source_family canonical_source or canonical_source-derived CIF
    - Test 2: canonical_record_to_zomic accepts a canonical raw source record with a CIF structure representation payload
    - Test 3: CIF conversion marks approximate or heuristic fidelity when qphi mapping is not exact
    - Test 4: optional pymatgen imports remain lazy; tests can pass using fixture-backed parsing paths
    - Test 5: HYPOD-X-style sample CIF fixture follows the same path as COD and preserves source provenance
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py` with:
- `cif_path_to_zomic(cif_path: Path, *, source_family: str, source_record_id: str, system: str | None = None) -> CorpusExample`
- `canonical_record_to_zomic(record: CanonicalRawSourceRecord) -> CorpusExample`

Implementation rules:
- reuse `prototype_import.parse_cif` or `expand_cif_orbits` as the primary lightweight parsing path
- only import `pymatgen` inside a helper when a richer fallback is actually needed
- derive orbit/site groups from CIF atom-site labels and expanded symmetry positions
- map each unique site group into deterministic labels and a bounded Zomic walk using the same serialization style established in `record2zomic`
- assign fidelity `"approximate"` by default for CIF-derived mappings unless the conversion can prove direct qphi compatibility
- copy source provenance into:
  - `source_family`
  - `source_record_id`
  - `source_path`
  - `properties["source_name"]`
  - `properties["space_group"]`

Add `materials-discovery/tests/fixtures/hypodx_approximant_sample.cif` as a thin committed approximant fixture used only for offline testing.

Create `materials-discovery/tests/test_llm_cif2zomic.py` with tests for the behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_cif2zomic.py tests/test_prototype_import.py tests/test_data_source_cod.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py contains "def cif_path_to_zomic("
    - materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py contains "def canonical_record_to_zomic("
    - materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py contains "expand_cif_orbits" or "parse_cif"
    - materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py contains "approximate"
    - materials-discovery/tests/fixtures/hypodx_approximant_sample.cif exists
    - materials-discovery/tests/test_llm_cif2zomic.py contains at least 5 test functions
    - The CIF conversion test command exits 0
  </acceptance_criteria>
  <done>Open approximant structures from COD and HYPOD-X-style fixtures can be converted into tiered corpus examples without adding a fragile dependency surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build syntax/materials corpora and write inventory, QA, rejects, and manifest outputs</name>
  <files>
    materials-discovery/src/materials_discovery/llm/corpus_builder.py,
    materials-discovery/tests/test_llm_corpus_builder.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/manifests.py,
    materials-discovery/src/materials_discovery/llm/inventory.py,
    materials-discovery/src/materials_discovery/llm/qa.py,
    materials-discovery/src/materials_discovery/llm/converters/record2zomic.py,
    materials-discovery/src/materials_discovery/llm/converters/projection2zomic.py,
    materials-discovery/src/materials_discovery/llm/converters/cif2zomic.py
  </read_first>
  <behavior>
    - Test 1: build_llm_corpus emits syntax_corpus.jsonl and materials_corpus.jsonl under data/llm_corpus/{build_id}
    - Test 2: build_llm_corpus writes rejects.jsonl, inventory.json, qa.json, and manifest.json
    - Test 3: syntax corpus includes repo-native Zomic examples even when no materials metadata is present
    - Test 4: materials corpus includes candidate-record, CIF-derived, and PyQCstrc-derived examples when fixtures are available
    - Test 5: QA summary counts match the corpus contents after dedupe and tiering
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/corpus_builder.py` with:
- `build_llm_corpus(config: CorpusBuildConfig, *, root: Path | None = None) -> CorpusBuildSummary`

Implementation details:
- call `build_inventory(config, root=root)` first
- route inventory entries by source_family:
  - repo_regression, repo_parts, materials_design -> syntax corpus rows
  - candidate_record -> convert with `candidate_to_zomic`
  - generated_export -> build syntax/materials rows from labeled raw export metadata where possible
  - canonical_source / reference_pack -> convert via `canonical_record_to_zomic`
  - pyqcstrc_projection -> convert via `projection_payload_to_zomic`
- grade every example with `grade_corpus_example`
- dedupe with `dedupe_corpus_examples`
- write:
  - `syntax_corpus.jsonl`
  - `materials_corpus.jsonl`
  - `rejects.jsonl`
  - `inventory.json`
  - `qa.json`
  - `manifest.json`

The materials corpus rows must include the `composition`, `properties`, and `validation` blocks; the syntax corpus rows may leave `composition` empty when the source is purely grammatical.

Create `materials-discovery/tests/test_llm_corpus_builder.py` with end-to-end offline tests using fixture inventory rows and monkeypatched compile helper responses.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_corpus_builder.py tests/test_llm_corpus_inventory.py tests/test_llm_corpus_qa.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/corpus_builder.py contains "def build_llm_corpus("
    - materials-discovery/src/materials_discovery/llm/corpus_builder.py contains "syntax_corpus.jsonl"
    - materials-discovery/src/materials_discovery/llm/corpus_builder.py contains "materials_corpus.jsonl"
    - materials-discovery/src/materials_discovery/llm/corpus_builder.py contains "rejects.jsonl"
    - materials-discovery/tests/test_llm_corpus_builder.py contains at least 5 test functions
    - The corpus builder test command exits 0
  </acceptance_criteria>
  <done>The Phase 6 builder can assemble both corpus views plus the full audit trail in one deterministic offline build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add operator-facing mdisc llm-corpus build command</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_corpus_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/corpus_builder.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/configs/llm/corpus_v1.yaml,
    materials-discovery/developers-docs/pipeline-stages.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-corpus build --config configs/llm/corpus_v1.yaml` loads the config and prints a JSON summary to stdout
    - Test 2: CLI returns code 2 with stderr message when the config path is missing or invalid
    - Test 3: successful CLI output includes syntax_count, materials_count, reject_count, manifest_path, and qa_path
    - Test 4: CLI honors workspace-root-relative config and output paths
  </behavior>
  <action>
Modify `materials-discovery/src/materials_discovery/cli.py` to add a new Typer sub-application:
- `llm_corpus_app = typer.Typer(help="LLM corpus preparation commands")`
- `app.add_typer(llm_corpus_app, name="llm-corpus")`

Add command:
```python
@llm_corpus_app.command("build")
def llm_corpus_build_cmd(
    config: Path = typer.Option(..., "--config", exists=True, dir_okay=False),
) -> None:
    ...
```

Behavior:
- load YAML config with `load_yaml`
- validate through `CorpusBuildConfig`
- call `build_llm_corpus`
- print `CorpusBuildSummary` as JSON to stdout
- on `FileNotFoundError`, `ValidationError`, or `ValueError`, emit stderr and exit code 2 following existing CLI conventions

Create `materials-discovery/tests/test_llm_corpus_cli.py` with CLI runner tests for all behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_corpus_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains "llm-corpus"
    - materials-discovery/src/materials_discovery/cli.py contains "def llm_corpus_build_cmd("
    - materials-discovery/src/materials_discovery/cli.py contains "CorpusBuildSummary"
    - materials-discovery/tests/test_llm_corpus_cli.py contains at least 4 test functions
    - The corpus CLI test command exits 0
  </acceptance_criteria>
  <done>Operators can build the Phase 6 corpus from a committed config using the standard `mdisc` CLI contract.</done>
</task>

</tasks>
