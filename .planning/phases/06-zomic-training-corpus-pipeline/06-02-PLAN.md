---
phase: 06-zomic-training-corpus-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - materials-discovery/src/materials_discovery/llm/inventory.py
  - materials-discovery/src/materials_discovery/llm/qa.py
  - materials-discovery/tests/test_llm_corpus_inventory.py
  - materials-discovery/tests/test_llm_corpus_qa.py
autonomous: true
requirements: [LLM-01]

must_haves:
  truths:
    - "Phase 6 inventories all user-selected required source families: repo regression scripts, part scripts, materials designs, candidate records, generated exports, canonical sources, reference packs, and PyQCstrc projection payloads"
    - "Inventory stays offline and fixture-backed; no live provider calls or live PyQCstrc install are required"
    - "QA assigns explicit gold, silver, or reject tiers instead of using a single pass/fail outcome"
    - "Deduplication and tiering preserve enough provenance to explain why an example was accepted, downgraded, or rejected"
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/inventory.py"
      provides: "Source family discovery and example staging inventory"
      exports: ["collect_repo_zomic_inventory", "collect_candidate_inventory", "collect_source_inventory", "collect_pyqcstrc_inventory", "build_inventory"]
    - path: "materials-discovery/src/materials_discovery/llm/qa.py"
      provides: "Tiering, dedupe, and quality grading for corpus examples"
      exports: ["grade_corpus_example", "dedupe_corpus_examples", "summarize_corpus_quality"]
    - path: "materials-discovery/tests/test_llm_corpus_inventory.py"
      provides: "Inventory coverage for every required source family"
    - path: "materials-discovery/tests/test_llm_corpus_qa.py"
      provides: "Gold/silver/reject grading and dedupe coverage"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/inventory.py"
      to: "materials-discovery/src/materials_discovery/llm/storage.py"
      via: "writes inventory and resolves build paths"
      pattern: "corpus_inventory_path|workspace_root"
    - from: "materials-discovery/src/materials_discovery/llm/inventory.py"
      to: "materials-discovery/data/external/sources"
      via: "reads staged canonical source inputs"
      pattern: "data/external/sources|canonical_records.jsonl"
    - from: "materials-discovery/src/materials_discovery/llm/qa.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "uses fidelity and release tiers"
      pattern: "fidelity_tier|release_tier"
---

<objective>
Build the source inventory and tiered QA layer for Phase 6.

Purpose: Give the corpus builder a deterministic view of all required source
families and lock the gold/silver/reject grading logic before the more complex
conversion work begins. This is the seam that keeps the user's source-coverage
and quality decisions explicit instead of scattering them across converters.

Output: inventory module, QA module, and focused tests proving that all
required source families are discoverable and gradable offline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-RESEARCH.md
@.planning/phases/06-zomic-training-corpus-pipeline/06-01-PLAN.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/data_sources/storage.py
@materials-discovery/src/materials_discovery/data_sources/schema.py
@materials-discovery/src/materials_discovery/generator/zomic_bridge.py
@materials-discovery/data/external/sources
@materials-discovery/data/external/reference_packs
@materials-discovery/designs/zomic
@core/src/regression/files/Zomic
@core/src/main/resources/com/vzome/core/parts

<interfaces>
From llm/schema.py (after 06-01):
```python
class CorpusBuildConfig(BaseModel): ...
class CorpusExample(BaseModel): ...
class CorpusQaSummary(BaseModel): ...
```

From data_sources/schema.py:
```python
class CanonicalRawSourceRecord(BaseModel): ...
```

From zomic_bridge.py:
```python
def _infer_orbit_name(label: str) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build source inventory for all required Phase 6 source families</name>
  <files>
    materials-discovery/src/materials_discovery/llm/inventory.py,
    materials-discovery/tests/test_llm_corpus_inventory.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/data_sources/schema.py,
    materials-discovery/src/materials_discovery/data_sources/storage.py,
    materials-discovery/developers-docs/zomic-llm-data-plan.md,
    .planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: build_inventory discovers repo regression scripts, part scripts, and materials designs
    - Test 2: build_inventory discovers candidate JSONL and generated raw exports under data/candidates and data/prototypes/generated
    - Test 3: build_inventory discovers staged canonical source inputs and reference-pack canonical records
    - Test 4: build_inventory supports a committed PyQCstrc projection payload fixture path without requiring a live PyQCstrc install
    - Test 5: inventory rows preserve source_family, source_path, system, and minimal source-specific metadata
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/inventory.py` with these concrete functions:
- `collect_repo_zomic_inventory(root: Path) -> list[dict[str, Any]]`
  - glob `core/src/regression/files/Zomic/**/*.zomic`
  - glob `core/src/main/resources/com/vzome/core/parts/**/*.zomic`
  - glob `materials-discovery/designs/zomic/*.zomic`
- `collect_candidate_inventory(root: Path, systems: list[str]) -> list[dict[str, Any]]`
  - scan `materials-discovery/data/candidates/*.jsonl`
  - include only files whose slug matches one of the configured systems when systems are provided
- `collect_generated_export_inventory(root: Path) -> list[dict[str, Any]]`
  - scan `materials-discovery/data/prototypes/generated/*.raw.json`
- `collect_source_inventory(root: Path, source_keys: list[str], reference_pack_ids: list[str]) -> list[dict[str, Any]]`
  - scan `materials-discovery/data/external/sources/{source_key}/*/canonical_records.jsonl`
  - scan `materials-discovery/data/external/reference_packs/*/{pack_id}/canonical_records.jsonl`
- `collect_pyqcstrc_inventory(root: Path) -> list[dict[str, Any]]`
  - scan `materials-discovery/tests/fixtures/pyqcstrc_projection_sample.json`
  - return source_family `pyqcstrc_projection`
- `build_inventory(config: CorpusBuildConfig, root: Path | None = None) -> list[dict[str, Any]]`
  - combine the enabled source-family collectors
  - return deterministic order sorted by source_family then source_path

Each inventory row must include:
- `source_family`
- `source_path`
- `system`
- `source_record_id`
- `input_kind`
- `metadata`

Create `materials-discovery/tests/test_llm_corpus_inventory.py` with tmp-path or fixture-backed tests for all behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_corpus_inventory.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/inventory.py contains "def build_inventory("
    - materials-discovery/src/materials_discovery/llm/inventory.py contains "core/src/regression/files/Zomic"
    - materials-discovery/src/materials_discovery/llm/inventory.py contains "data/external/sources"
    - materials-discovery/src/materials_discovery/llm/inventory.py contains "pyqcstrc_projection"
    - materials-discovery/tests/test_llm_corpus_inventory.py contains at least 5 test functions
    - The inventory test command exits 0
  </acceptance_criteria>
  <done>All required Phase 6 source families are discoverable through one deterministic inventory layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement gold/silver/reject QA grading and dedupe</name>
  <files>
    materials-discovery/src/materials_discovery/llm/qa.py,
    materials-discovery/tests/test_llm_corpus_qa.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/generator/zomic_bridge.py,
    .planning/phases/06-zomic-training-corpus-pipeline/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: grade_corpus_example returns gold for exact/anchored examples that pass parse, compile, label, collision, and site-count thresholds
    - Test 2: grade_corpus_example downgrades to silver when fidelity_tier is approximate but other checks pass
    - Test 3: grade_corpus_example rejects heuristic examples that fail compile, collision, or minimum site-count checks
    - Test 4: dedupe_corpus_examples removes duplicate example_ids or duplicate zomic_text hashes deterministically
    - Test 5: summarize_corpus_quality reports gold_count, silver_count, reject_count, and issue tallies
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/qa.py` with these concrete functions:
- `grade_corpus_example(example: CorpusExample, config: CorpusBuildConfig) -> CorpusExample`
  - set `release_tier="gold"` only when:
    - `validation["parse_status"] == "passed"`
    - `validation["compile_status"] == "passed"`
    - `validation["labels_valid"] is True`
    - `validation["collision_free"] is True`
    - `config.gold_min_sites <= validation["site_count"] <= config.gold_max_sites`
    - `provenance.fidelity_tier` is `"exact"` or `"anchored"`
  - set `release_tier="silver"` when parse + compile + label checks pass but fidelity is `"approximate"` or site_count exceeds gold limits and stays below `silver_max_sites`
  - set `release_tier="reject"` otherwise
- `dedupe_corpus_examples(examples: list[CorpusExample]) -> list[CorpusExample]`
  - keep first example by deterministic sort on `(release_tier, source_family, example_id)`
  - drop later duplicates by `example_id` or identical `zomic_text` SHA256
- `summarize_corpus_quality(examples: list[CorpusExample]) -> CorpusQaSummary`
  - report counts for gold, silver, reject, duplicate_dropped_count, parse_fail_count, compile_fail_count, collision_fail_count, label_fail_count

Use the existing orbit-label rules from `zomic_bridge._infer_orbit_name` when validating label-derived orbit names.

Create `materials-discovery/tests/test_llm_corpus_qa.py` with tests for all behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_corpus_qa.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/qa.py contains "def grade_corpus_example("
    - materials-discovery/src/materials_discovery/llm/qa.py contains "def dedupe_corpus_examples("
    - materials-discovery/src/materials_discovery/llm/qa.py contains "def summarize_corpus_quality("
    - materials-discovery/src/materials_discovery/llm/qa.py contains "gold"
    - materials-discovery/src/materials_discovery/llm/qa.py contains "silver"
    - materials-discovery/src/materials_discovery/llm/qa.py contains "reject"
    - materials-discovery/tests/test_llm_corpus_qa.py contains at least 5 test functions
    - The QA test command exits 0
  </acceptance_criteria>
  <done>Phase 6 has one explicit quality gate that assigns gold, silver, or reject tiers and explains why.</done>
</task>

</tasks>
