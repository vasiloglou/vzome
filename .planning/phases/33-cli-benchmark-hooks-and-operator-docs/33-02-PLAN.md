---
phase: 33-cli-benchmark-hooks-and-operator-docs
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/src/materials_discovery/llm/translation_bundle.py
  - materials-discovery/tests/test_llm_translation_cli.py
autonomous: true
requirements: [OPS-15]
---

<objective>
Expose translation bundle creation and inspection through the shipped CLI.

Purpose: make operators able to create, inspect, and trace translation artifacts
without custom Python, while preserving the repo’s manifest and exit-code
conventions.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-CONTEXT.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-RESEARCH.md
@.planning/phases/33-cli-benchmark-hooks-and-operator-docs/33-01-PLAN.md

@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/translation_bundle.py
@materials-discovery/src/materials_discovery/common/manifest.py
@materials-discovery/tests/test_llm_serving_benchmark_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `mdisc llm-translate` with stage-manifest and lineage/benchmark hooks</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/translation_bundle.py,
    materials-discovery/tests/test_llm_translation_cli.py
  </files>
  <behavior>
    - Test 1: the CLI requires `--config`, `--input`, `--target`, and `--export-id`
    - Test 2: successful translation writes a bundle, a standard stage manifest, and prints a JSON summary
    - Test 3: the CLI attaches campaign lineage when present and benchmark context from the configured system
    - Test 4: missing input path or invalid target returns exit code 2 with a clear error
  </behavior>
  <action>
Add `@app.command("llm-translate")` to `cli.py`.

The command should:
- load the supplied config
- read candidate JSONL from the explicit input path
- call the Plan 01 bundle writer
- write a standard stage manifest under `data/manifests/`
- pass through campaign lineage and benchmark-context hooks where available
- print the resulting summary as JSON

Keep the command additive: it must not mutate the source candidate JSONL.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_cli.py -x -v</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `mdisc llm-translate-inspect` for operator tracing</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_translation_cli.py
  </files>
  <behavior>
    - Test 1: inspect prints export ID, target, input path, candidate count, and lossy count from the bundle manifest
    - Test 2: inspect can filter down to one candidate ID and print its payload/inventory details
    - Test 3: missing manifest path returns exit code 2 with a clear error
  </behavior>
  <action>
Add `@app.command("llm-translate-inspect")` to `cli.py`.

This command should:
- read the bundle manifest and inventory JSONL
- print a concise human-readable summary
- optionally filter to one `--candidate-id`
- give operators a no-Python tracing path for raw payload location and fidelity
  posture
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_cli.py -x -v</automated>
  </verify>
</task>

</tasks>

