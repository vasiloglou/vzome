---
phase: 28-checkpoint-lifecycle-and-promotion-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - materials-discovery/configs/llm/al_cu_fe_checkpoint_promotion.yaml
  - materials-discovery/configs/llm/al_cu_fe_checkpoint_retirement.yaml
  - materials-discovery/developers-docs/configuration-reference.md
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/tests/test_llm_replay_core.py
  - materials-discovery/tests/test_llm_checkpoint_cli.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-23, OPS-13]

must_haves:
  truths:
    - "Phase 28 must end with a real lifecycle contract and operator examples, not just schema types."
    - "Example promotion and retirement specs should be committed fixtures, not one-off local notes."
    - "Example evidence references must be clearly labeled as illustrative repo-relative placeholder paths unless the plan also adds committed fixture evidence files; tests and docs must agree on that choice."
    - "The docs must be explicit that promoted-default resolution through generation, launch, and replay lands in Phase 29."
    - "Backward compatibility matters: existing single-checkpoint configs and historical replay artifacts must still be understandable after lifecycle artifacts appear."
    - "Retired checkpoints remain replayable and auditable even though they are no longer implicitly selectable."
    - "Compatibility proof must include replaying a historical launch bundle after its checkpoint was later retired."
    - "Phase 28 docs should say that demotion happens by promoting a different checkpoint and that workflow-integrated RUNBOOK updates stay deferred until Phase 29."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/configs/llm/al_cu_fe_checkpoint_promotion.yaml"
      provides: "Committed example promotion action spec"
    - path: "materials-discovery/configs/llm/al_cu_fe_checkpoint_retirement.yaml"
      provides: "Committed example retirement action spec"
    - path: "materials-discovery/developers-docs/configuration-reference.md"
      provides: "Operator-visible contract for checkpoint families, lifecycle actions, and stale-write expectations"
    - path: "materials-discovery/developers-docs/llm-integration.md"
      provides: "Lifecycle architecture notes and explicit phase boundary to Phase 29"
  key_links:
    - from: "materials-discovery/developers-docs/configuration-reference.md"
      to: "materials-discovery/configs/llm/al_cu_fe_checkpoint_promotion.yaml"
      via: "docs and committed examples must tell the same promotion-action story"
      pattern: "checkpoint_family|checkpoint_id|evidence"
    - from: "materials-discovery/developers-docs/llm-integration.md"
      to: "materials-discovery/src/materials_discovery/llm/checkpoints.py"
      via: "the lifecycle architecture description must match the shipped helper boundary"
      pattern: "promote|retire|lifecycle"
    - from: "materials-discovery/tests/test_llm_replay_core.py"
      to: "materials-discovery/src/materials_discovery/llm/checkpoints.py"
      via: "replay compatibility tests must prove retired checkpoints stay auditable even before Phase 29 resolution wiring"
      pattern: "checkpoint_id|fingerprint"
---

<objective>
Close Phase 28 with committed examples, additive docs, and compatibility proof
that the lifecycle layer stays audit-friendly without overclaiming workflow
integration.

Purpose: make the lifecycle contract usable and understandable now while
keeping promoted-default execution and pin-driven workflow resolution for Phase
29.

Output: committed promotion and retirement specs, additive docs, and focused
compatibility tests for replay-facing auditability and CLI stability.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-CONTEXT.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-RESEARCH.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-VALIDATION.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-01-PLAN.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-02-PLAN.md

@materials-discovery/src/materials_discovery/llm/checkpoints.py
@materials-discovery/src/materials_discovery/llm/replay.py
@materials-discovery/developers-docs/configuration-reference.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/tests/test_llm_replay_core.py
@materials-discovery/tests/test_llm_checkpoint_cli.py
@materials-discovery/tests/test_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit lifecycle action examples and compatibility proof for historical auditability</name>
  <files>
    materials-discovery/configs/llm/al_cu_fe_checkpoint_promotion.yaml,
    materials-discovery/configs/llm/al_cu_fe_checkpoint_retirement.yaml,
    materials-discovery/tests/test_llm_replay_core.py,
    materials-discovery/tests/test_llm_checkpoint_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/tests/test_llm_replay_core.py,
    materials-discovery/tests/test_llm_checkpoint_cli.py,
    materials-discovery/configs/llm/al_cu_fe_zomic_adapted_checkpoint.yaml,
    .planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: committed promotion and retirement specs validate against the new action schema and use clearly documented repo-relative placeholder evidence paths
    - Test 2: replay-facing tests prove that a launch bundle whose checkpoint was later retired still resolves by checkpoint fingerprint identity rather than lifecycle mutability
    - Test 3: retired checkpoints remain auditable after lifecycle actions land
    - Test 4: the lifecycle CLI can consume the committed example specs without repo-specific hacks
  </behavior>
  <action>
Add committed example action specs for one real checkpoint family.

Ship at least:
- one promotion spec for the Al-Cu-Fe adapted checkpoint family
- one retirement spec for the same family

The examples should show:
- checkpoint_family
- checkpoint_id
- evidence references as repo-relative illustrative placeholder paths, and the
  docs must say they are structural examples rather than committed benchmark
  outputs
- stale-write guard input such as expected revision or expected promoted member
- operator note fields where useful

Use materials-discovery/tests/test_llm_replay_core.py and
materials-discovery/tests/test_llm_checkpoint_cli.py to prove that:
- replay-facing checkpoint identity remains tied to registration fingerprint
  even after the source checkpoint was retired later
- lifecycle actions do not delete or weaken historical registration artifacts
- the example action specs are operationally real enough to use as fixtures
  later in the milestone
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_replay_core.py tests/test_llm_checkpoint_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - committed promotion and retirement specs exist under materials-discovery/configs/llm/
    - the committed example specs make their evidence-path semantics explicit
    - replay tests still protect hard checkpoint fingerprint identity after the referenced checkpoint is retired
    - lifecycle CLI tests cover the committed example specs
    - the compatibility regression command exits 0
  </acceptance_criteria>
  <done>The lifecycle contract is now backed by real example action specs and compatibility proof for historical auditability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document the lifecycle contract and preserve clear phase boundaries</name>
  <files>
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    materials-discovery/tests/test_cli.py,
    .planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: docs explain checkpoint_family, hybrid lifecycle state, and file-backed action artifacts clearly
    - Test 2: docs state explicitly that promoted-default execution and explicit pin resolution are Phase 29 work
    - Test 3: docs describe retirement as no implicit future selection while preserving replay and auditability
    - Test 4: docs explain that demotion happens by promoting a different checkpoint and that the RUNBOOK remains workflow-integrated work for Phase 29
    - Test 5: shared CLI regression still passes after lifecycle documentation and command examples land
  </behavior>
  <action>
Update docs additively in:
- materials-discovery/developers-docs/configuration-reference.md
- materials-discovery/developers-docs/llm-integration.md

Document:
- the new checkpoint_family selector on lanes
- the hybrid model of immutable registration plus mutable lifecycle index
- promotion and retirement action artifacts and monotonic stale-write
  expectations
- the fact that retired checkpoints remain replayable and auditable
- the JSON shape exposed by llm-list-checkpoints
- that example evidence paths in the committed promotion/retirement specs are
  illustrative repo-relative placeholders unless future phases add committed
  benchmark fixtures
- that demotion happens by promoting a different checkpoint, not by a separate
  demote command
- the explicit Phase 28 boundary: no promoted-default execution integration yet,
  and no workflow pin resolution until Phase 29
- that workflow-integrated RUNBOOK guidance stays deferred to Phase 29 because
  Phase 28 only ships the lifecycle contract and CLI surface

Keep the docs additive. Do not rewrite the v1.3 operator workflow into a
different system; explain how lifecycle state layers onto it.

Use materials-discovery/tests/test_cli.py as the full shared-CLI regression so
the lifecycle command additions and import wiring stay coherent after the docs
examples land.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - configuration-reference.md documents checkpoint families and lifecycle action semantics
    - llm-integration.md states clearly that promoted-default execution is deferred to Phase 29
    - docs describe retired checkpoints as replayable/auditable but not implicitly selectable
    - docs state that demotion is done by promoting a different checkpoint and that RUNBOOK workflow integration is deferred to Phase 29
    - the CLI regression command exits 0
  </acceptance_criteria>
  <done>Operators and downstream planners can read the lifecycle contract clearly without confusing it for full workflow integration.</done>
</task>

</tasks>
