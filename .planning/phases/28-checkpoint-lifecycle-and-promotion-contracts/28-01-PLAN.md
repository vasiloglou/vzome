---
phase: 28-checkpoint-lifecycle-and-promotion-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/common/schema.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_launch_schema.py
  - materials-discovery/tests/test_llm_checkpoint_registry.py
autonomous: true
requirements: [LLM-23, OPS-13]

must_haves:
  truths:
    - "Phase 28 is contract and storage work first; it must not yet change llm-generate, llm-launch, or llm-replay to consume promoted defaults."
    - "The lifecycle model is hybrid: immutable per-checkpoint registration facts remain with each checkpoint, while mutable promotion/retirement state lives in a central family index."
    - "Config stays authoritative for which checkpoint family a lane can use, but the promoted member for that family is resolved from registry state."
    - "Pinned checkpoint selection needs a typed contract in this phase, but workflow execution of pinning is deferred to Phase 29."
    - "The existing v1.3 checkpoint registration, fingerprint, and lineage contract must remain readable without lifecycle artifacts."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/common/schema.py"
      provides: "Additive checkpoint-family config contract"
      exports:
        [
          "LlmModelLaneConfig",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed lifecycle, promotion, retirement, and pin-selection models"
      exports:
        [
          "LlmCheckpointLifecycleIndex",
          "LlmCheckpointPromotionSpec",
          "LlmCheckpointRetirementSpec",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic lifecycle-index and action-artifact paths under data/llm_checkpoints/"
      exports:
        [
          "llm_checkpoint_family_dir",
          "llm_checkpoint_lifecycle_index_path",
          "llm_checkpoint_action_dir",
        ]
  key_links:
    - from: "materials-discovery/src/materials_discovery/common/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/checkpoints.py"
      via: "lane config needs an additive family selector that later registry resolution can consume without replacing checkpoint_id support"
      pattern: "checkpoint_family|checkpoint_id|require_checkpoint_registration"
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/storage.py"
      via: "typed lifecycle artifacts need stable storage paths and file names"
      pattern: "LlmCheckpointLifecycleIndex|PromotionSpec|RetirementSpec"
    - from: "materials-discovery/src/materials_discovery/llm/storage.py"
      to: "materials-discovery/src/materials_discovery/llm/checkpoints.py"
      via: "storage helpers must be reusable by later lifecycle action and resolution logic"
      pattern: "llm_checkpoint_"
---

<objective>
Define the additive schema and storage contract for multi-checkpoint lifecycle
management before CLI actions or workflow integration widen the regression
surface.

Purpose: pin the lifecycle, promotion, retirement, and explicit pin-selection
models so later plans can implement file-backed actions without re-litigating
the artifact shape.

Output: additive config fields, typed lifecycle artifacts, deterministic
storage helpers, and focused schema/storage regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-CONTEXT.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-RESEARCH.md
@.planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-VALIDATION.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/checkpoints.py
@materials-discovery/tests/test_llm_launch_schema.py
@materials-discovery/tests/test_llm_checkpoint_registry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config and artifact schemas for checkpoint families and lifecycle actions</name>
  <files>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_launch_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    materials-discovery/tests/test_llm_launch_schema.py,
    .planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-CONTEXT.md,
    .planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: lane config can declare checkpoint_family additively without breaking existing checkpoint_id-only configs
    - Test 2: registration and serving-lineage models can record checkpoint_family while preserving older registrations that do not have it
    - Test 3: typed lifecycle artifacts cover family index state, promotion evidence references, retirement metadata, and explicit pin-selection contract
    - Test 4: blank or conflicting lifecycle selector strings are normalized or rejected consistently
    - Test 5: the new models deserialize older v1.3 artifacts without requiring lifecycle data
  </behavior>
  <action>
Extend materials-discovery/src/materials_discovery/common/schema.py additively.

In LlmModelLaneConfig, add a checkpoint_family field that:
- is optional
- normalizes blank strings to None
- can coexist with checkpoint_id during the transition from one-checkpoint to
  promoted-family workflows
- does not force current v1.3 configs to opt into families immediately

Extend materials-discovery/src/materials_discovery/llm/schema.py with typed
contract models for:
- a family-level lifecycle index
- lifecycle member summaries keyed by checkpoint_id and fingerprint
- typed promotion artifacts that reference benchmark or evaluation evidence
- typed retirement artifacts with explicit reason fields
- typed pin-selection artifacts for later manual/campaign pinning in Phase 29

Keep the separation explicit:
- the family index is mutable lifecycle state
- checkpoint registration stays immutable lineage
- pin-selection is an explicit override contract, not a silent mutation of the
  promoted default

Update registration-related models additively so checkpoint_family can be
threaded through new registrations and summaries, but legacy registrations
without it still load cleanly.

Update materials-discovery/src/materials_discovery/llm/__init__.py exports for
the new lifecycle models.

Extend materials-discovery/tests/test_llm_launch_schema.py to cover the new
config and additive artifact behavior.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_launch_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/common/schema.py contains checkpoint_family on lane config
    - materials-discovery/src/materials_discovery/llm/schema.py defines typed lifecycle and action models
    - older registration and serving-lineage artifacts still deserialize without lifecycle fields
    - the schema regression command exits 0
  </acceptance_criteria>
  <done>The repo has an additive checkpoint-family and lifecycle schema contract that later plans can execute against safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic storage helpers for family indexes and lifecycle action artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/tests/test_llm_checkpoint_registry.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_llm_checkpoint_registry.py,
    .planning/phases/28-checkpoint-lifecycle-and-promotion-contracts/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: family lifecycle paths are deterministic and rooted under the existing data/llm_checkpoints surface
    - Test 2: action artifacts for promotion and retirement are stored separately from immutable registration
    - Test 3: storage helpers keep existing registration paths untouched
    - Test 4: stale or conflicting writes can rely on stable revision/index paths rather than ad hoc file naming
  </behavior>
  <action>
Extend materials-discovery/src/materials_discovery/llm/storage.py with additive
helpers for checkpoint-family lifecycle state.

Use a deterministic storage layout under the existing data/llm_checkpoints
surface, for example:
- one family directory keyed by checkpoint_family
- one lifecycle index path inside that family directory
- one actions directory for promotion and retirement artifacts

Preserve existing registration storage exactly as shipped in v1.3.

The helpers should make it straightforward for later lifecycle actions to:
- update the family index
- append auditable action artifacts
- validate revision or stale-write expectations without deleting immutable
  checkpoint registration state

Extend materials-discovery/tests/test_llm_checkpoint_registry.py so the
foundational storage paths and backward compatibility are locked before CLI and
resolver work begins.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_checkpoint_registry.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/storage.py defines deterministic family lifecycle paths
    - v1.3 registration paths remain unchanged
    - registry tests cover new family/action path helpers
    - the storage regression command exits 0
  </acceptance_criteria>
  <done>The lifecycle contract now has stable storage primitives for family state and auditable action artifacts.</done>
</task>

</tasks>
