---
phase: 15-phase-12-replay-and-operator-workflow-audit-closure
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/15-phase-12-replay-and-operator-workflow-audit-closure/15-VALIDATION.md
autonomous: true
requirements: [LLM-09, LLM-11, OPS-07]

must_haves:
  truths:
    - "Traceability must not mark LLM-09, LLM-11, or OPS-07 complete until the retroactive proof artifacts exist."
    - "Phase 15 should close only the Phase 12 proof gap; it must not claim the milestone is archived or shipped until the milestone audit reruns successfully."
    - "State handoff must point directly to gsd-audit-milestone as the next action once Phase 15 closes."
    - "Phase 15 should also close its own validation loop; do not hand off to the milestone audit while 15-VALIDATION.md still looks like an untouched draft."
    - "Standard Phase 15 execution summaries are still expected outputs of the workflow even though this is a gap-closure phase."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement checkbox and traceability closeout for LLM-09, LLM-11, and OPS-07"
      exports:
        [
          "LLM-09",
          "LLM-11",
          "OPS-07",
          "Phase 15 traceability row",
        ]
    - path: ".planning/STATE.md"
      provides: "Milestone handoff after the final v1.1 proof gap is closed"
      exports:
        [
          "current_phase",
          "status",
          "last_activity",
        ]
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/12-replay-comparison-and-operator-workflow/12-VERIFICATION.md"
      via: "requirements should only move back to complete after the verification artifact exists"
      pattern: "LLM-09|LLM-11|OPS-07"
    - from: ".planning/STATE.md"
      to: ".planning/v1.1-MILESTONE-AUDIT.md"
      via: "state should hand off to a milestone audit rerun rather than prematurely archiving the milestone"
      pattern: "gsd-audit-milestone|Phase 15"
---

<objective>
Restore clean requirement traceability and state handoff after the Phase 12
proof gap is formally closed.

Purpose: make project state reflect the newly proven requirements without
pretending the full milestone audit has already passed.

Output: updated `REQUIREMENTS.md`, updated `STATE.md`, and a clean handoff to
`gsd-audit-milestone`, with `15-VALIDATION.md` finalized for closeout.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/15-phase-12-replay-and-operator-workflow-audit-closure/15-CONTEXT.md
@.planning/phases/15-phase-12-replay-and-operator-workflow-audit-closure/15-VALIDATION.md
@.planning/phases/12-replay-comparison-and-operator-workflow/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore requirement completion for LLM-09, LLM-11, and OPS-07</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/phases/12-replay-comparison-and-operator-workflow/12-VERIFICATION.md,
    .planning/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - Update only the Phase 15-owned requirements
    - Make the checkbox state and traceability rows consistent with the new proof artifacts
    - Avoid altering already-complete Phase 13 or Phase 14 traceability
  </behavior>
  <action>
Update `REQUIREMENTS.md` so `LLM-09`, `LLM-11`, and `OPS-07` reflect the
successful closure of the Phase 12 audit gap.

Expected end state:

- the three requirement checkboxes are `[x]`
- their Phase 15 traceability rows move from `Pending` to `Complete`
- all eight v1.1 requirements now show complete

Do not alter the phase mapping created by the milestone-gap plan unless a real
audit-consistency problem requires it.
  </action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains "[x] **LLM-09**"
    - .planning/REQUIREMENTS.md contains "[x] **LLM-11**"
    - .planning/REQUIREMENTS.md contains "[x] **OPS-07**"
    - .planning/REQUIREMENTS.md contains "| LLM-09 | Phase 15 | Complete |"
    - .planning/REQUIREMENTS.md contains "| LLM-11 | Phase 15 | Complete |"
    - .planning/REQUIREMENTS.md contains "| OPS-07 | Phase 15 | Complete |"
    - git diff --check exits 0
  </acceptance_criteria>
  <done>The requirement traceability reflects that the Phase 12 proof gap is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Move milestone state to ready-for-audit handoff</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Keep the milestone explicitly in audit-handoff posture rather than marking it archived
    - Record that Phase 15 closed the final proof gap
    - Point the next action directly at `gsd-audit-milestone`
    - Finalize `15-VALIDATION.md` frontmatter/sign-off state so this phase does not create a fresh documentary gap
  </behavior>
  <action>
Update `STATE.md` so the active milestone posture becomes ready for a milestone
audit rerun after Phase 15 completes.

The updated state should:

- keep Phase 15 as the final completed gap-closure phase
- set a clear status such as `ready_for_milestone_audit`
- record that Phase 15 closed the final Phase 12 audit gap
- point the next action directly at `gsd-audit-milestone`
- update `15-VALIDATION.md` so its closeout state matches the finished phase
  (for example, no longer draft, Nyquist-compliant when warranted, and with
  final sign-off consistent with execution evidence)

Do not mark the milestone archived or shipped in this step.
  </action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md contains "current_phase: 15"
    - .planning/STATE.md contains "ready_for_milestone_audit"
    - .planning/STATE.md contains a Phase 15 completion note
    - .planning/STATE.md contains "gsd-audit-milestone"
    - .planning/phases/15-phase-12-replay-and-operator-workflow-audit-closure/15-VALIDATION.md contains "nyquist_compliant: true"
    - git diff --check exits 0
  </acceptance_criteria>
  <done>The project state points to a milestone-audit rerun instead of another implementation phase.</done>
</task>

</tasks>
