---
phase: 34-benchmark-pack-and-freeze-contract
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/tests/test_llm_translated_benchmark_cli.py
  - materials-discovery/tests/test_cli.py
  - materials-discovery/configs/llm/al_cu_fe_translated_benchmark_freeze.yaml
  - materials-discovery/developers-docs/llm-translated-benchmark-runbook.md
  - materials-discovery/developers-docs/configuration-reference.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/developers-docs/index.md
  - materials-discovery/Progress.md
autonomous: true
requirements: [LLM-31]

must_haves:
  truths:
    - "An operator can freeze a translated benchmark pack from a file-backed spec without writing custom Python."
    - "An operator can inspect the frozen pack and see both included and excluded translated cases plus the rule contract that produced them."
    - "The shipped docs and example config make the phase boundary explicit: benchmark-pack freezing is in scope now, while external target runtime and scorecards remain later-phase work."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator CLI commands for benchmark-pack freeze and inspect."
      exports:
        - "llm-translated-benchmark-freeze"
        - "llm-translated-benchmark-inspect"
    - path: "materials-discovery/configs/llm/al_cu_fe_translated_benchmark_freeze.yaml"
      provides: "A committed example spec that points at shipped translation-bundle manifests and explicit rules."
    - path: "materials-discovery/developers-docs/llm-translated-benchmark-runbook.md"
      provides: "Operator runbook for freeze, inspect, artifact layout, and scope boundaries."
    - path: "materials-discovery/tests/test_llm_translated_benchmark_cli.py"
      provides: "Focused CLI coverage for the new commands."
  key_links:
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/translated_benchmark.py"
      via: "freeze and inspect command entrypoints"
      pattern: "freeze_translated_benchmark_set|load_translated_benchmark_spec"
    - from: "materials-discovery/developers-docs/llm-translated-benchmark-runbook.md"
      to: "materials-discovery/configs/llm/al_cu_fe_translated_benchmark_freeze.yaml"
      via: "operator example workflow"
      pattern: "al_cu_fe_translated_benchmark_freeze.yaml"
---

<objective>
Expose the translated benchmark-pack workflow through CLI and operator docs.

Purpose: make Phase 34 executable and inspectable for operators while keeping
the milestone boundary narrow and additive to existing translation-bundle and
benchmark documentation patterns.

Output: CLI commands, committed example spec, docs updates, and discoverability
tests.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@AGENTS.md
@CLAUDE.md
@.planning/phases/34-benchmark-pack-and-freeze-contract/34-02-PLAN.md
@.planning/milestones/v1.5-phases/33-cli-benchmark-hooks-and-operator-docs/33-02-SUMMARY.md
@.planning/milestones/v1.5-phases/33-cli-benchmark-hooks-and-operator-docs/33-03-SUMMARY.md

@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/translated_benchmark.py
@materials-discovery/tests/test_llm_translation_cli.py
@materials-discovery/tests/test_cli.py
@materials-discovery/developers-docs/llm-translation-runbook.md
@materials-discovery/developers-docs/pipeline-stages.md
@materials-discovery/developers-docs/configuration-reference.md
@materials-discovery/configs/llm
@materials-discovery/Progress.md

<interfaces>
Follow the existing CLI pattern established in Phase 33:
```python
@app.command("llm-translate")
def llm_translate_command(...): ...

@app.command("llm-translate-inspect")
def llm_translate_inspect_command(...): ...
```

Reuse the Phase 34 core API from Plan 02:
```python
def load_translated_benchmark_spec(spec_path: Path) -> TranslatedBenchmarkSetSpec: ...
def freeze_translated_benchmark_set(spec_path: Path, root: Path | None = None) -> TranslatedBenchmarkSetSummary: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI freeze and inspect commands for translated benchmark packs</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/tests/test_llm_translated_benchmark_cli.py,
    materials-discovery/tests/test_cli.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/tests/test_llm_translation_cli.py,
    materials-discovery/tests/test_cli.py,
    .planning/phases/34-benchmark-pack-and-freeze-contract/34-02-PLAN.md,
    .planning/milestones/v1.5-phases/33-cli-benchmark-hooks-and-operator-docs/33-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-translated-benchmark-freeze --spec PATH` requires a spec path, calls the Phase 34 freeze core, and prints a JSON summary with manifest and inventory paths.
    - Test 2: missing spec or invalid contract exits with code 2 and a clear `llm-translated-benchmark-freeze failed:` error line.
    - Test 3: `mdisc llm-translated-benchmark-inspect --manifest PATH` prints the benchmark-set ID, target family, selected systems, included count, excluded count, and contract path.
    - Test 4: inspect supports `--show included|excluded|all` plus optional `--candidate-id`, and it fails clearly when the requested candidate is absent.
    - Test 5: the root CLI help lists both new commands so operators can discover the workflow without reading code.
  </behavior>
  <action>
Add two commands to `materials-discovery/src/materials_discovery/cli.py`:

1. `@app.command("llm-translated-benchmark-freeze")`
   - accepts `--spec PATH`
   - calls `freeze_translated_benchmark_set(spec_path, root=workspace_root())`
   - prints the returned `TranslatedBenchmarkSetSummary` as JSON

2. `@app.command("llm-translated-benchmark-inspect")`
   - accepts `--manifest PATH`
   - accepts `--show included|excluded|all` with default `all`
   - accepts optional `--candidate-id`
   - loads the benchmark-set manifest plus included/excluded inventories and prints a concise human-readable trace

Keep the command surface narrow:
- do not add external model registration
- do not add benchmark execution
- do not add scoring or recommendation lines yet

Create `materials-discovery/tests/test_llm_translated_benchmark_cli.py` for the new commands and update `materials-discovery/tests/test_cli.py` so CLI help discoverability is locked. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "@app.command\\(\"llm-translated-benchmark-freeze\"\\)" materials-discovery/src/materials_discovery/cli.py`
    - `rg -n "@app.command\\(\"llm-translated-benchmark-inspect\"\\)" materials-discovery/src/materials_discovery/cli.py`
    - `rg -n "llm-translated-benchmark-freeze failed|llm-translated-benchmark-inspect failed" materials-discovery/src/materials_discovery/cli.py`
    - `rg -n "--show included\\|excluded\\|all|candidate-id" materials-discovery/tests/test_llm_translated_benchmark_cli.py`
    - `rg -n "llm-translated-benchmark-freeze|llm-translated-benchmark-inspect" materials-discovery/tests/test_cli.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_cli.py tests/test_cli.py -x -v`
  </acceptance_criteria>
  <done>Operators can freeze and inspect translated benchmark packs from the CLI with the repo’s normal exit-code and discoverability conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Add an example freeze spec and operator documentation for the Phase 34 workflow</name>
  <files>
    materials-discovery/configs/llm/al_cu_fe_translated_benchmark_freeze.yaml,
    materials-discovery/developers-docs/llm-translated-benchmark-runbook.md,
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/developers-docs/index.md,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/developers-docs/llm-translation-runbook.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/index.md,
    materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml,
    .planning/research/SUMMARY.md
  </read_first>
  <action>
Create the operator-facing docs and example config that make Phase 34 executable without overreaching into later phases.

Add `materials-discovery/configs/llm/al_cu_fe_translated_benchmark_freeze.yaml` that points at shipped translation-bundle manifests and sets explicit:
- `benchmark_set_id`
- `bundle_manifest_paths`
- `systems`
- `target_family`
- `allowed_fidelity_tiers`
- `loss_posture`

Add `materials-discovery/developers-docs/llm-translated-benchmark-runbook.md` covering:
- when to freeze a translated benchmark pack
- required inputs from shipped translation bundles
- the new freeze and inspect commands
- artifact layout under `data/benchmarks/llm_external_sets/{benchmark_set_id}/`
- how to interpret included versus excluded rows
- an explicit scope note that external target registration and scorecards are deferred to Phases 35 and 36

Update `materials-discovery/developers-docs/configuration-reference.md`,
`materials-discovery/developers-docs/pipeline-stages.md`, and
`materials-discovery/developers-docs/index.md` so the new spec and commands are
discoverable beside the existing translation-bundle workflow. Update
`materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "benchmark_set_id|bundle_manifest_paths|allowed_fidelity_tiers|loss_posture" materials-discovery/configs/llm/al_cu_fe_translated_benchmark_freeze.yaml`
    - `rg -n "llm-translated-benchmark-freeze|llm-translated-benchmark-inspect" materials-discovery/developers-docs/llm-translated-benchmark-runbook.md`
    - `rg -n "data/benchmarks/llm_external_sets" materials-discovery/developers-docs/llm-translated-benchmark-runbook.md`
    - `rg -n "Phase 35|Phase 36|deferred" materials-discovery/developers-docs/llm-translated-benchmark-runbook.md`
    - `rg -n "translated benchmark|llm-translated-benchmark-freeze|llm-translated-benchmark-inspect" materials-discovery/developers-docs/configuration-reference.md materials-discovery/developers-docs/pipeline-stages.md materials-discovery/developers-docs/index.md`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_cli.py tests/test_cli.py -x -v`
  </acceptance_criteria>
  <done>The repo has one documented, example-backed operator workflow for freezing and inspecting translated benchmark packs without leaking into later-phase runtime work.</done>
</task>

</tasks>

<verification>
Run the focused CLI/discoverability slice:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translated_benchmark_cli.py tests/test_cli.py -x -v`
</verification>

<success_criteria>
Phase 34 is operator-usable end to end: a user can point the CLI at a
file-backed spec, freeze a translated benchmark pack, inspect what was included
or excluded, and find the workflow in the shipped docs.
</success_criteria>

<output>
After completion, create `.planning/phases/34-benchmark-pack-and-freeze-contract/34-03-SUMMARY.md`
</output>
