---
phase: 32-cif-and-material-string-exporters
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - materials-discovery/src/materials_discovery/llm/translation_export.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_translation_material_string.py
  - materials-discovery/tests/test_llm_translation_export.py
autonomous: true
requirements: [LLM-28, LLM-29]

must_haves:
  truths:
    - "The first material-string exporter must implement the shipped target format `crystaltextllm_material_string`; do not pivot this phase to a new CSLLM-specific format name."
    - "Material-string output must be derived from the same translated artifact identity and site ordering as the CIF path."
    - "Cross-target export must preserve source linkage, fidelity tier, and loss reasons in the emitted text instead of relying on sidecar state alone."
    - "Shared export validation from Plan 01 must remain the single gate for periodic export readiness."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      provides: "Material-string emission and cross-target dispatch"
      exports:
        [
          "emit_material_string_text",
          "emit_translated_structure",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/__init__.py"
      provides: "Public export surface for both Phase 32 serializers"
      exports:
        [
          "emit_material_string_text",
          "emit_translated_structure",
        ]
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      via: "the exporter must honor the committed target format name `crystaltextllm_material_string`"
      pattern: "material_string|crystaltextllm_material_string|requires_periodic_cell"
    - from: "materials-discovery/src/materials_discovery/llm/translation.py"
      to: "materials-discovery/src/materials_discovery/llm/translation_export.py"
      via: "material-string serialization must preserve the normalized artifact ordering and fidelity semantics Phase 31 already established"
      pattern: "prepare_translated_structure|loss_reasons|diagnostics|sites"
---

<objective>
Add the first concrete material-string exporter and finish the cross-target
dispatch surface.

Purpose: implement the shipped `crystaltextllm_material_string` target so the
same translated artifact can emit either CIF or material-string payloads
without diverging provenance or loss semantics.

Output: line-oriented material-string text, working dispatch for both target
families, and focused stability coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
@.planning/phases/32-cif-and-material-string-exporters/32-VALIDATION.md
@.planning/phases/32-cif-and-material-string-exporters/32-01-PLAN.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-VERIFICATION.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/translation.py
@materials-discovery/src/materials_discovery/llm/translation_export.py
@materials-discovery/developers-docs/llm-translation-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit deterministic CrystalTextLLM-style material strings from translated artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translation_material_string.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/developers-docs/llm-translation-contract.md,
    .planning/phases/32-cif-and-material-string-exporters/32-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: material-string output begins with deterministic line-oriented source/fidelity/loss headers
    - Test 2: material-string output includes one cell line and one composition line in fixed key order
    - Test 3: material-string output includes one `site` line per translated site in artifact order
    - Test 4: lossy QC-native exports keep explicit periodic-proxy metadata in the header lines
    - Test 5: repeated emission of the same artifact is byte-stable
  </behavior>
  <action>
Implement `emit_material_string_text(artifact: TranslatedStructureArtifact) -> str`
in `materials-discovery/src/materials_discovery/llm/translation_export.py`.

Use this exact line-oriented layout:
- `source_candidate_id <candidate_id>`
- `system <system-or-none>`
- `target_format crystaltextllm_material_string`
- `fidelity_tier <tier>`
- `loss_reasons <comma-separated reasons or none>`
- `cell a=<...> b=<...> c=<...> alpha=<...> beta=<...> gamma=<...>`
- `composition <element1>=<value> <element2>=<value> ...` using sorted element keys
- one `site` line per artifact site:
  `site label=<label> species=<species> occupancy=<occ> fractional=<x>,<y>,<z>`

Use the shared float formatter and validation helpers from Plan 01.
Do not include cartesian coordinates in this first target format.

Export `emit_material_string_text` from `materials-discovery/src/materials_discovery/llm/__init__.py`.

Add focused tests in `materials-discovery/tests/test_llm_translation_material_string.py`
for exact and lossy fixtures plus deterministic byte-stability.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_material_string.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `emit_material_string_text` exists and emits the fixed header, cell, composition, and site-line layout
    - material-string output preserves translated site order
    - lossy exports keep explicit periodic-proxy metadata in the text
    - repeated emission of the same artifact is byte-stable
    - the material-string regression command exits 0
  </acceptance_criteria>
  <done>The repo can now emit the first concrete material-string payload from the translated artifact contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish cross-target dispatch and preserve identical artifact identity across CIF and material-string export</name>
  <files>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/tests/test_llm_translation_export.py,
    materials-discovery/tests/test_llm_translation_material_string.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/translation_export.py,
    materials-discovery/src/materials_discovery/llm/translation.py,
    materials-discovery/tests/test_llm_translation_cif.py,
    materials-discovery/tests/test_llm_translation_material_string.py
  </read_first>
  <behavior>
    - Test 1: `emit_translated_structure(...)` dispatches to both `cif` and `material_string`
    - Test 2: the returned exported artifact preserves source linkage, fidelity tier, loss reasons, diagnostics, and site ordering for both target families
    - Test 3: the same translated candidate can produce stable CIF and material-string payloads without recomputing normalization differently between targets
    - Test 4: unsupported target families still fail clearly rather than silently falling back
  </behavior>
  <action>
Finish `emit_translated_structure(...)` so it dispatches working branches for
both target families:
- `cif` -> `emit_cif_text(...)`
- `material_string` -> `emit_material_string_text(...)`

Keep this function purely additive:
- reuse the validated translated artifact it receives
- return a copied artifact with `emitted_text` set
- preserve `source`, `target`, `fidelity_tier`, `loss_reasons`, `composition`,
  `cell`, `sites`, and `diagnostics`
- never reorder sites or rewrite fidelity semantics

Extend the export tests so the same normalized artifact can be emitted for both
target families and stays byte-stable across repeated runs.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_export.py tests/test_llm_translation_material_string.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `emit_translated_structure` dispatches both built-in target families
    - exported artifacts preserve source linkage, fidelity tier, loss reasons, and diagnostics
    - the same translated candidate yields stable text across repeated runs for both targets
    - unsupported target families still fail clearly
    - the cross-target export regression command exits 0
  </acceptance_criteria>
  <done>The exporter surface now supports both Phase 32 target families from the same translated artifact identity.</done>
</task>

</tasks>
