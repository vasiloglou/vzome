---
phase: 20-specialized-lane-integration-and-workflow-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/compare.py
  - materials-discovery/src/materials_discovery/diffraction/compare_patterns.py
  - materials-discovery/configs/systems/al_cu_fe_llm_local.yaml
  - materials-discovery/tests/test_llm_evaluate_cli.py
  - materials-discovery/tests/test_llm_compare_core.py
  - materials-discovery/tests/test_llm_compare_cli.py
  - materials-discovery/tests/test_llm_campaign_lineage.py
  - materials-discovery/tests/test_report.py
autonomous: true
requirements: [LLM-15, LLM-16, OPS-09]

must_haves:
  truths:
    - "The first specialized lane is operationally meaningful through evaluation-first workflow behavior, not by replacing the existing generator."
    - "Phase 20 must keep launch, replay, and compare compatible when a run carries specialized lane lineage."
    - "Comparison and report outputs must distinguish generation-lane lineage from evaluation-lane lineage rather than flattening them into one generic model label."
    - "One real proof is enough for this phase; the deeper proof system should be explicit rather than spread thinly across both systems."
    - "Core artifact paths remain stable even when compare/report gain richer specialized-lane fields."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator-facing lane selection for llm-evaluate"
    - path: "materials-discovery/configs/systems/al_cu_fe_llm_local.yaml"
      provides: "Real Phase 20 proof config with specialized evaluation lane enabled"
    - path: "materials-discovery/src/materials_discovery/llm/compare.py"
      provides: "Lane-aware outcome snapshot and comparison visibility for generation plus evaluation identity"
      exports:
        [
          "build_campaign_outcome_snapshot",
          "build_campaign_comparison_result",
        ]
    - path: "materials-discovery/tests/test_llm_compare_core.py"
      provides: "Comparison coverage for specialized evaluation lineage"
    - path: "materials-discovery/tests/test_report.py"
      provides: "Report visibility for specialized evaluation provenance"
  key_links:
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/evaluate.py"
      via: "CLI evaluation lane selection must feed the shared lane-aware evaluation core without bypassing config semantics"
      pattern: "llm_evaluate_command|evaluate_llm_candidates"
    - from: "materials-discovery/src/materials_discovery/llm/compare.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "compare artifacts need additive evaluation-lane lineage fields while keeping current snapshot/result contracts stable"
      pattern: "LlmCampaignOutcomeSnapshot|LlmCampaignComparisonResult"
    - from: "materials-discovery/src/materials_discovery/diffraction/compare_patterns.py"
      to: "materials-discovery/src/materials_discovery/common/stage_metrics.py"
      via: "report and calibration outputs must surface specialized evaluation context without changing the no-DFT downstream path"
      pattern: "llm_assessment|llm_synthesizability_mean"
---

<objective>
Make the first specialized lane real on one system and propagate its lineage
through the workflow surfaces operators already use.

Purpose: satisfy the substantive part of Phase 20 by letting operators run
lane-aware evaluation against a specialized endpoint, prove it on `Al-Cu-Fe`,
and keep compare/report/campaign outputs compatible and auditable.

Output: `llm-evaluate --model-lane`, a real proof config for `Al-Cu-Fe`, and
additive compare/report/campaign visibility for specialized evaluation lineage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-RESEARCH.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-VALIDATION.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-01-PLAN.md

@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/evaluate.py
@materials-discovery/src/materials_discovery/llm/compare.py
@materials-discovery/src/materials_discovery/diffraction/compare_patterns.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/configs/systems/al_cu_fe_llm_local.yaml
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire CLI lane selection for llm-evaluate and enable the real Al-Cu-Fe specialized proof config</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/configs/systems/al_cu_fe_llm_local.yaml,
    materials-discovery/tests/test_llm_evaluate_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/configs/systems/al_cu_fe_llm_local.yaml,
    materials-discovery/tests/test_llm_evaluate_cli.py,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-evaluate --model-lane specialized_materials` resolves a configured specialized lane and still emits the standard summary JSON shape
    - Test 2: when `--model-lane` is omitted, CLI evaluation respects `llm_evaluate.model_lane` if configured and otherwise preserves current config behavior
    - Test 3: unavailable specialized evaluation lanes fail clearly unless explicit fallback is configured
    - Test 4: the committed `Al-Cu-Fe` local config declares a real specialized evaluation lane through `llm_evaluate`
    - Test 5: the CLI tests stay fully offline via monkeypatched OpenAI-compatible responses
  </behavior>
  <action>
Add an additive `--model-lane` option to `materials-discovery/src/materials_discovery/cli.py`
for `llm-evaluate`.

`llm-evaluate` precedence must be:
1. CLI `--model-lane`
2. `config.llm_evaluate.model_lane`
3. existing shared default/fallback/backend-default resolution from Phase 19

The CLI must:
- route the requested lane into `evaluate_llm_candidates(...)`
- preserve the current summary JSON contract while exposing additive resolved
  lane information through the summary fields added in Plan 01
- fail early and clearly when a requested specialized lane is unavailable and
  no explicit fallback is configured

Update `materials-discovery/configs/systems/al_cu_fe_llm_local.yaml` to make
`Al-Cu-Fe` the real Phase 20 proof config.

Add an `llm_evaluate:` block with these concrete values:
- `prompt_template: materials_assess_v1`
- `temperature: 0.2`
- `max_tokens: 1024`
- `model_lane: specialized_materials`

Keep the existing generation-lane config intact. The config should express that
generation may still default to `general_purpose` while evaluation uses the
specialized lane.

Extend `materials-discovery/tests/test_llm_evaluate_cli.py` to cover the new
CLI and config behavior fully offline.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_evaluate_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains a `--model-lane` option for llm-evaluate
    - materials-discovery/configs/systems/al_cu_fe_llm_local.yaml contains "llm_evaluate:"
    - materials-discovery/configs/systems/al_cu_fe_llm_local.yaml contains "model_lane: specialized_materials"
    - evaluation CLI failures mention the requested lane before provider attempts begin
    - the evaluation CLI regression command exits 0
  </acceptance_criteria>
  <done>The first specialized lane is now operator-addressable on a real proof system through the shipped evaluation workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate specialized evaluation lineage into compare, report, and campaign compatibility surfaces</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/diffraction/compare_patterns.py,
    materials-discovery/tests/test_llm_compare_core.py,
    materials-discovery/tests/test_llm_compare_cli.py,
    materials-discovery/tests/test_llm_campaign_lineage.py,
    materials-discovery/tests/test_report.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/diffraction/compare_patterns.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/tests/test_llm_compare_core.py,
    materials-discovery/tests/test_llm_compare_cli.py,
    materials-discovery/tests/test_llm_campaign_lineage.py,
    materials-discovery/tests/test_report.py,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: compare outcome snapshots preserve generation lane identity and add distinct evaluation lane identity rather than replacing one with the other
    - Test 2: compare CLI output remains readable and machine-stable while exposing specialized evaluation lineage additively
    - Test 3: report entries surface specialized evaluation lane context through additive `llm_assessment` evidence
    - Test 4: campaign-lineage tests remain green when a launch or downstream report includes specialized evaluation lineage
    - Test 5: no new specialized-only artifact path is introduced
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/schema.py` and
`materials-discovery/src/materials_discovery/llm/compare.py` additively so
comparison artifacts can show both generation-lane and evaluation-lane lineage.

Concrete requirements:
- keep existing generation-lane fields intact
- add additive evaluation lineage fields such as:
  - `evaluation_requested_model_lanes`
  - `evaluation_resolved_model_lane`
  - `evaluation_resolved_model_lane_source`
  - `evaluation_serving_identity`
  - or a clearly named equivalent set that preserves the same distinction

Update `materials-discovery/src/materials_discovery/diffraction/compare_patterns.py`
so report entries and evidence keep specialized evaluation lineage inside the
additive `llm_assessment` block without changing the standard report artifact
family.

Extend tests:
- `materials-discovery/tests/test_llm_compare_core.py`
- `materials-discovery/tests/test_llm_compare_cli.py`
- `materials-discovery/tests/test_llm_campaign_lineage.py`
- `materials-discovery/tests/test_report.py`

The tests must prove:
- compare snapshots keep generation and evaluation lane identity distinct
- report evidence preserves specialized evaluation lineage
- existing compare/report CLI usage remains valid
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_compare_core.py tests/test_llm_compare_cli.py tests/test_llm_campaign_lineage.py tests/test_report.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/compare.py contains additive evaluation-lane lineage fields
    - compare outputs preserve generation-lane lineage alongside evaluation-lane lineage
    - report evidence surfaces specialized evaluation provenance additively
    - no new specialized-only artifact root is introduced
    - the compare/report regression command exits 0
  </acceptance_criteria>
  <done>The workflow can now carry a specialized evaluation lane through comparison and reporting without fracturing the existing artifact contracts.</done>
</task>

</tasks>

<verification>
  <check>`Al-Cu-Fe` is a real specialized-lane proof config for evaluation.</check>
  <check>Compare and report surfaces now expose specialized evaluation lineage without changing the core artifact family.</check>
  <check>Launch, replay, and compare compatibility remains intact when specialized lineage is present.</check>
</verification>
