---
phase: 20-specialized-lane-integration-and-workflow-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - materials-discovery/configs/systems/sc_zn_llm_local.yaml
  - materials-discovery/developers-docs/configuration-reference.md
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/tests/test_real_mode_pipeline.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-16, OPS-09]

must_haves:
  truths:
    - "Phase 20 uses one deep proof and one thin proof; it does not turn into the full benchmark milestone."
    - "The second system exists to prove compatibility, not to force equal-depth specialized evaluation on every workflow lane."
    - "Docs must describe the first specialized lane honestly as evaluation-primary and generation-compatible."
    - "Phase 20 documentation should stay additive; full benchmark/runbook scope belongs to Phase 21."
    - "Wave 3 docs must include a concrete operator recipe for reaching a real specialized OpenAI-compatible endpoint so the phase does not end as contract-only."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/configs/systems/sc_zn_llm_local.yaml"
      provides: "Thin second-system specialized evaluation compatibility config"
    - path: "materials-discovery/developers-docs/configuration-reference.md"
      provides: "Config semantics plus a concrete specialized-endpoint setup recipe"
    - path: "materials-discovery/tests/test_real_mode_pipeline.py"
      provides: "End-to-end compatibility proof for specialized-lane launch/replay/compare on the second system"
    - path: "materials-discovery/developers-docs/llm-integration.md"
      provides: "Milestone-level explanation of the evaluation-primary specialized lane"
    - path: "materials-discovery/developers-docs/pipeline-stages.md"
      provides: "Operator-facing command and artifact guidance for specialized-lane evaluation compatibility"
  key_links:
    - from: "materials-discovery/configs/systems/sc_zn_llm_local.yaml"
      to: "materials-discovery/tests/test_real_mode_pipeline.py"
      via: "the thin proof config must be exercised in an offline end-to-end compatibility regression"
      pattern: "llm_evaluate|specialized_materials"
    - from: "materials-discovery/developers-docs/configuration-reference.md"
      to: "materials-discovery/developers-docs/pipeline-stages.md"
      via: "config semantics and CLI workflow guidance must describe the same evaluation-primary specialized-lane contract"
      pattern: "llm_evaluate.model_lane|llm-evaluate --model-lane"
---

<objective>
Finish Phase 20 with the thinner second-system proof and the additive operator
docs that explain what the specialized lane now does.

Purpose: prove that specialized-lane compatibility is not limited to one system
and leave Phase 21 a clean benchmark surface with honest, operator-facing
documentation.

Output: `Sc-Zn` thin compatibility proof, end-to-end real-mode regression, and
additive docs for specialized-lane evaluation behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-RESEARCH.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-VALIDATION.md
@.planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-02-PLAN.md

@materials-discovery/configs/systems/sc_zn_llm_local.yaml
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/compare.py
@materials-discovery/developers-docs/configuration-reference.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the thin Sc-Zn specialized compatibility proof and end-to-end regression</name>
  <files>
    materials-discovery/configs/systems/sc_zn_llm_local.yaml,
    materials-discovery/tests/test_real_mode_pipeline.py
  </files>
  <read_first>
    materials-discovery/configs/systems/sc_zn_llm_local.yaml,
    materials-discovery/configs/systems/al_cu_fe_llm_local.yaml,
    materials-discovery/tests/test_real_mode_pipeline.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the `Sc-Zn` local config declares a specialized evaluation lane without disturbing the existing seeded generation path
    - Test 2: an offline real-mode pipeline regression can launch, replay, and compare when specialized evaluation lineage is present on the second system
    - Test 3: the thin proof remains clearly lighter than the `Al-Cu-Fe` real proof and does not require equal benchmark depth
  </behavior>
  <action>
Update `materials-discovery/configs/systems/sc_zn_llm_local.yaml` additively.

Add an `llm_evaluate:` block mirroring the Phase 20 specialized-evaluation
contract:
- `prompt_template: materials_assess_v1`
- `temperature: 0.2`
- `max_tokens: 1024`
- `model_lane: specialized_materials`

Preserve the existing `seed_zomic` generation path so this config remains the
thinner compatibility proof rather than a second full specialized workflow
redesign.

Extend `materials-discovery/tests/test_real_mode_pipeline.py` with an offline,
monkeypatched end-to-end regression that proves:
- a launch using the current lane-aware workflow still completes
- replay remains compatible
- compare remains compatible
- specialized evaluation lineage on `Sc-Zn` does not break the file-backed
  workflow

Keep the test fully offline by monkeypatching the OpenAI-compatible endpoint
rather than depending on a live server.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/configs/systems/sc_zn_llm_local.yaml contains "llm_evaluate:"
    - materials-discovery/configs/systems/sc_zn_llm_local.yaml contains "model_lane: specialized_materials"
    - materials-discovery/tests/test_real_mode_pipeline.py covers specialized evaluation compatibility on Sc-Zn
    - the real-mode pipeline regression command exits 0
  </acceptance_criteria>
  <done>The second system now proves specialized-lane compatibility without inflating Phase 20 into the benchmark milestone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update additive docs for the evaluation-primary specialized lane and compatibility boundaries</name>
  <files>
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_cli.py,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-CONTEXT.md,
    .planning/phases/20-specialized-lane-integration-and-workflow-compatibility/20-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: docs explain that the first specialized lane is evaluation-primary and not marketed as mature direct Zomic generation
    - Test 2: docs explain `llm_evaluate.model_lane` and `mdisc llm-evaluate --model-lane ...`
    - Test 3: docs explain that compare/report surfaces now show additive specialized evaluation lineage while preserving existing artifact families
    - Test 4: docs include a concrete operator recipe for connecting or standing up a real specialized OpenAI-compatible endpoint
    - Test 5: CLI regression coverage remains green after the documentation-driven command-surface updates
  </behavior>
  <action>
Update the following docs additively:

`materials-discovery/developers-docs/configuration-reference.md`
- document `llm_evaluate.model_lane`
- document that evaluation may intentionally use `specialized_materials` while
  generation still uses `general_purpose`
- add a short operator recipe for a real specialized OpenAI-compatible endpoint
  using placeholder-safe examples only
- make clear that the endpoint may be local or remote, but the example is
  operational guidance rather than a CI dependency

`materials-discovery/developers-docs/pipeline-stages.md`
- document `mdisc llm-evaluate --model-lane ...`
- explain that compare/report now preserve additive generation-lane and
  evaluation-lane lineage
- keep the existing artifact roots and closed-loop commands intact

`materials-discovery/developers-docs/llm-integration.md`
- add a Phase 20 section explaining the evaluation-primary specialized lane
- explicitly state that Phase 20 proves one real system and one thinner
  compatibility proof
- keep direct Zomic-native local generation as a later milestone, not a Phase
  20 claim

Update `materials-discovery/tests/test_cli.py` only as needed to keep the
documented CLI/help surface or summary-shape regressions green.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_real_mode_pipeline.py tests/test_cli.py tests/test_llm_compare_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/developers-docs/configuration-reference.md contains "llm_evaluate.model_lane"
    - materials-discovery/developers-docs/configuration-reference.md contains a concrete specialized endpoint recipe or equivalent operator setup section
    - materials-discovery/developers-docs/pipeline-stages.md contains "llm-evaluate --model-lane"
    - materials-discovery/developers-docs/llm-integration.md contains "evaluation-primary" or equivalent explicit wording
    - the CLI regression command exits 0
  </acceptance_criteria>
  <done>The specialized-lane workflow is documented honestly and handed off cleanly to the Phase 21 benchmark milestone.</done>
</task>

</tasks>

<verification>
  <check>Sc-Zn now serves as the thin compatibility proof without changing the role of the deeper Al-Cu-Fe proof.</check>
  <check>Operator docs describe the specialized lane honestly as evaluation-primary and generation-compatible.</check>
  <check>Phase 20 ends with a stable, additive workflow surface ready for Phase 21 benchmarking.</check>
</verification>
