---
phase: 31-translation-contracts-and-representation-loss-semantics
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_candidate.json
  - materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_candidate.json
  - materials-discovery/tests/test_llm_translation_fixtures.py
  - materials-discovery/developers-docs/llm-translation-contract.md
autonomous: true
requirements: [LLM-27, LLM-30]

must_haves:
  truths:
    - "Phase 31 must anchor the contract on repo fixtures, not notebook-only examples or screenshots."
    - "Fixture coverage needs both a periodic-safe example and a QC-native/lossy example so later exporters cannot collapse the fidelity boundary."
    - "The Phase 31 doc is an implementer contract for developers. Operator runbook and CLI workflow docs remain Phase 33 work."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_candidate.json"
      provides: "Periodic-safe candidate fixture for exact or anchored export coverage"
      exports:
        [
          "CandidateRecord fixture",
        ]
    - path: "materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_candidate.json"
      provides: "QC-native/lossy candidate fixture for explicit loss semantics"
      exports:
        [
          "CandidateRecord fixture",
        ]
    - path: "materials-discovery/developers-docs/llm-translation-contract.md"
      provides: "Developer-facing contract and fidelity semantics handoff for Phase 32"
      exports:
        [
          "translation contract note",
        ]
  key_links:
    - from: "materials-discovery/tests/fixtures/llm_translation/"
      to: "materials-discovery/src/materials_discovery/llm/translation.py"
      via: "real fixtures lock contract expectations for periodic-safe and lossy scenarios"
      pattern: "CandidateRecord|TranslationFidelityTier"
    - from: "materials-discovery/developers-docs/llm-translation-contract.md"
      to: "materials-discovery/developers-docs/llm-integration.md"
      via: "the new contract doc must inherit the existing Zomic-first/QC-native architectural boundary"
      pattern: "Zomic|CIF|QC-native|source of truth"
---

<objective>
Freeze the translation contract on real fixtures and a developer-facing handoff
note before serializer work begins.

Purpose: keep Phase 32 grounded in reproducible repo examples and documented
fidelity boundaries instead of implementation folklore.

Output: fixture-backed regression coverage and a concise developer note that
explains the contract, fidelity states, and source-of-truth boundary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-VALIDATION.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-01-PLAN.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-02-PLAN.md

@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/llm-quasicrystal-landscape.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixture-backed regression coverage for periodic-safe and QC-native/lossy translation paths</name>
  <files>
    materials-discovery/tests/fixtures/llm_translation/al_cu_fe_periodic_candidate.json,
    materials-discovery/tests/fixtures/llm_translation/sc_zn_qc_candidate.json,
    materials-discovery/tests/test_llm_translation_fixtures.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/translation.py,
    materials-discovery/tests/test_llm_record2zomic.py,
    materials-discovery/tests/test_structure_realization.py,
    .planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the periodic fixture round-trips through normalization with stable target and fidelity expectations
    - Test 2: the QC-native fixture cannot silently claim exact periodic export and surfaces explicit lossy diagnostics
    - Test 3: fixture-backed expectations remain deterministic across repeated runs
    - Test 4: the fixture suite proves the contract on repo files rather than synthetic inline test dicts alone
  </behavior>
  <action>
Add a small fixture set under materials-discovery/tests/fixtures/llm_translation/
that anchors the translation contract on realistic compiled candidates.

The fixture set must include:
- one periodic-safe or approximant-style candidate
- one QC-native or clearly lossy candidate

Use those fixtures in a dedicated regression test file so later Phase 32
serializer work inherits stable expectations for source linkage, normalized
structure payload, and fidelity/loss classification.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_fixtures.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - the repo contains at least one periodic-safe fixture and one QC-native/lossy fixture
    - fixture-backed tests lock fidelity expectations for both examples
    - fixture expectations are deterministic across repeated runs
    - the fixture regression command exits 0
  </acceptance_criteria>
  <done>Phase 32 will build serializers against stable repo fixtures instead of ad hoc examples.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the developer translation contract note for exporter implementers</name>
  <files>
    materials-discovery/developers-docs/llm-translation-contract.md
  </files>
  <read_first>
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/llm-quasicrystal-landscape.md,
    .planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md,
    .planning/phases/31-translation-contracts-and-representation-loss-semantics/31-02-PLAN.md
  </read_first>
  <behavior>
    - Test 1: the developer note names the source-of-truth boundary explicitly: Zomic stays authoritative and downstream exports are additive interop artifacts
    - Test 2: the developer note explains exact, anchored, approximate, and lossy semantics for future exporters
    - Test 3: the developer note distinguishes this contract handoff from the later operator/CLI docs in Phase 33
  </behavior>
  <action>
Write a concise developer-facing translation contract note at
materials-discovery/developers-docs/llm-translation-contract.md.

The note should cover:
- the source-of-truth boundary between compiled Zomic candidates and downstream
  export artifacts
- the built-in target families introduced in Phase 31
- what exact, anchored, approximate, and lossy mean for exporter authors
- why a QC-native source may still need a lossy CIF or material-string export
- what Phase 32 is expected to serialize from the normalized translated
  artifact

Keep this note implementation-facing. Operator workflow docs, CLI walkthroughs,
and benchmark reuse docs stay in Phase 33.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome && rg -n "exact|anchored|approximate|lossy|QC-native|source of truth" materials-discovery/developers-docs/llm-translation-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - the developer note states that Zomic remains the authoritative source representation
    - the developer note defines all four fidelity/loss states
    - the developer note names the Phase 32 serializer handoff clearly
    - the doc verification command exits 0
  </acceptance_criteria>
  <done>The exporter implementation phase now has a clear written contract instead of relying on planner context.</done>
</task>

</tasks>
