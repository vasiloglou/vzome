---
phase: 31-translation-contracts-and-representation-loss-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_translation_schema.py
autonomous: true
requirements: [LLM-27, LLM-30]

must_haves:
  truths:
    - "Phase 31 is an additive interoperability contract; it must not replace Zomic or alter shipped llm-generate, llm-evaluate, or checkpoint behavior."
    - "Translation fidelity relative to the source Zomic candidate needs an export-facing enum with `lossy`; do not overload the older corpus `FidelityTier` that uses `heuristic`."
    - "Every translated artifact must carry one auditable source reference back to the originating compiled `CandidateRecord` and its provenance."
    - "Target families and target formats must be typed and registry-backed so later exporters and CLI code cannot invent ad hoc names."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed translation contract, export-fidelity semantics, and target registry"
      exports:
        [
          "TranslationFidelityTier",
          "TranslationTargetFamily",
          "TranslationTargetDescriptor",
          "TranslatedStructureArtifact",
          "TranslatedStructureDiagnostic",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/__init__.py"
      provides: "Public exports for the translation contract"
      exports:
        [
          "TranslationFidelityTier",
          "TranslationTargetDescriptor",
          "TranslatedStructureArtifact",
        ]
  key_links:
    - from: "materials-discovery/src/materials_discovery/common/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "compiled candidates are the source-of-truth inputs to the new translation artifact"
      pattern: "CandidateRecord|SiteRecord|provenance"
    - from: "materials-discovery/src/materials_discovery/data_sources/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "existing representation metadata gives the repo precedent for typed representation inventories"
      pattern: "StructureRepresentation|representation_kind|payload_format"
---

<objective>
Define the additive translation artifact contract before concrete exporters land.

Purpose: make exact, anchored, approximate, and lossy semantics explicit in a
typed schema that later CIF and material-string exporters can share.

Output: export-facing fidelity models, target-family registration, source-linkage
contract, and schema-level regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md
@.planning/phases/31-translation-contracts-and-representation-loss-semantics/31-VALIDATION.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/data_sources/schema.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/__init__.py
@materials-discovery/tests/test_llm_corpus_schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a typed translated-structure artifact contract with export fidelity and source linkage</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translation_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/data_sources/schema.py,
    .planning/phases/31-translation-contracts-and-representation-loss-semantics/31-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a translated artifact records source candidate ID, source provenance hints, target family, target format, and schema version deterministically
    - Test 2: the export fidelity enum supports exact, anchored, approximate, and lossy without changing the existing corpus fidelity enum
    - Test 3: translated artifacts require explicit loss reasons when fidelity is lossy
    - Test 4: the translated artifact can preserve warnings and diagnostics without requiring final text payloads yet
    - Test 5: older LLM schema users still import successfully because the new contract is additive
  </behavior>
  <action>
Add a new additive translation contract family to
materials-discovery/src/materials_discovery/llm/schema.py.

The contract should define:
- a translation-specific fidelity enum with `exact`, `anchored`,
  `approximate`, and `lossy`
- a typed source-reference model that links a translated artifact back to the
  originating compiled `CandidateRecord`
- a typed target descriptor that separates target family from target format and
  carries enough metadata for later serializer selection
- a translated-structure artifact model that can exist before final CIF or
  material-string text is emitted
- typed diagnostics/warnings for coordinate derivation, dropped QC-native
  semantics, or unsupported exactness claims

Keep the separation explicit:
- corpus fidelity stays untouched for Phase 6/7/8 workflows
- export fidelity is a new additive contract for external-LLM interoperability
- translated artifacts describe normalized structure intent, not final emitted
  CIF or CrystalTextLLM/CSLLM text yet

Update materials-discovery/src/materials_discovery/llm/__init__.py so the new
contract is publicly importable.

Add focused schema tests in
materials-discovery/tests/test_llm_translation_schema.py that lock the additive
behavior and the lossy-reason requirement.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/schema.py defines a translation-specific fidelity enum with `lossy`
    - translated artifacts carry typed source linkage back to the compiled candidate contract
    - target family and target format are modeled separately and typed
    - older corpus fidelity behavior remains unchanged
    - the schema regression command exits 0
  </acceptance_criteria>
  <done>The repo has a stable additive contract for translated structure artifacts and export loss semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register supported target families for CIF and material-string workflows</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_translation_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/data_sources/schema.py,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/llm-quasicrystal-landscape.md
  </read_first>
  <behavior>
    - Test 1: the built-in registry exposes CIF as a first-class target family
    - Test 2: the built-in registry exposes at least one material-string target family for CrystalTextLLM- or CSLLM-style downstream use
    - Test 3: registry entries can declare whether the target requires a periodic proxy and whether qphi/QC-native semantics may be dropped
    - Test 4: unknown target-family lookups fail clearly instead of falling back silently
  </behavior>
  <action>
Add a built-in translation target registry to the same additive contract layer.

The registry must cover:
- CIF-oriented export targets
- at least one material-string family intended for CrystalTextLLM- or
  CSLLM-style workflows

Each registry entry should carry the semantics later exporters need, such as:
- whether the target expects a periodic cell and fractional coordinates
- whether the target can natively preserve QC-specific geometry semantics
- whether final text emission is line-oriented or file-oriented

Do not implement the final serializers here. The phase only needs the typed
registry and failure behavior so later plans can choose exporters cleanly.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_translation_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - the translation contract ships a built-in CIF target-family entry
    - the translation contract ships at least one material-string target-family entry
    - registry entries expose periodic-proxy and QC-semantics expectations explicitly
    - unknown target families fail clearly in tests
    - the schema regression command exits 0
  </acceptance_criteria>
  <done>Phase 32 will inherit stable target-family names and semantics instead of choosing formats ad hoc.</done>
</task>

</tasks>
