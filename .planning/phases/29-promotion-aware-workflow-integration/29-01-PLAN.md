---
phase: 29-promotion-aware-workflow-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/checkpoints.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/launch.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_checkpoint_registry.py
  - materials-discovery/tests/test_llm_launch_core.py
  - materials-discovery/tests/test_llm_generate_cli.py
autonomous: true
requirements: [LLM-24, LLM-26]
---

<objective>
Make new execution promotion-aware by resolving checkpoint families through the
Phase 28 lifecycle registry while preserving explicit pinning and legacy
checkpoint-only lanes.
</objective>

<context>
@.planning/phases/29-promotion-aware-workflow-integration/29-CONTEXT.md
@.planning/phases/29-promotion-aware-workflow-integration/29-RESEARCH.md
@materials-discovery/src/materials_discovery/llm/checkpoints.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/tests/test_llm_checkpoint_registry.py
@materials-discovery/tests/test_llm_launch_core.py
@materials-discovery/tests/test_llm_generate_cli.py
</context>

<tasks>

<task>
  <name>Task 1: Add promotion-aware checkpoint family resolution for new execution</name>
  <files>
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    materials-discovery/tests/test_llm_checkpoint_registry.py
  </files>
  <action>
Extend the checkpoint resolver so:
- `checkpoint_family` without `checkpoint_id` resolves the currently promoted
  family member for new execution
- `checkpoint_family` plus `checkpoint_id` remains an explicit pin inside that
  family
- legacy `checkpoint_id`-only lanes keep their current behavior
- new execution rejects retired checkpoints and fails clearly when a family has
  no promoted member yet

Keep replay-safe registration and fingerprint lineage intact.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_checkpoint_registry.py -x -v</automated>
  </verify>
</task>

<task>
  <name>Task 2: Record lifecycle selection metadata on serving identity</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_launch_core.py,
    materials-discovery/tests/test_llm_generate_cli.py
  </files>
  <action>
Add additive serving-identity fields that explain how a checkpoint was chosen,
including whether it came from a promoted family default, an explicit family
pin, or a legacy checkpoint lane. When a family lifecycle registry is involved,
record the lifecycle path and revision used for selection.

Thread the richer identity through the shared `build_serving_identity(...)`
path so `llm-generate`, launch resolution, evaluation, and benchmark smoke
surfaces all inherit the same behavior.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_launch_core.py tests/test_llm_generate_cli.py -x -v</automated>
  </verify>
</task>

</tasks>
