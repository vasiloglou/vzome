---
phase: 36-comparative-benchmark-workflow-and-fidelity-aware-scorecards
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - materials-discovery/src/materials_discovery/llm/external_benchmark.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_external_benchmark_core.py
  - materials-discovery/Progress.md
autonomous: true
requirements: [LLM-32, LLM-33]

must_haves:
  truths:
    - "One benchmark execution flow can load a frozen translated benchmark set, resolve registered external targets and internal controls, and write per-target run artifacts."
    - "Per-case benchmark results preserve eligibility, exclusions, response or parse status, and shared-slice metrics needed for later scorecards."
    - "The benchmark summary aggregates by target family and fidelity tier, reports shared eligible control deltas, and emits bounded recommendation lines."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/external_benchmark.py"
      provides: "Phase 36 spec loading, target resolution, prompt rendering, execution, case-result persistence, and summary building."
      exports:
        - "load_external_benchmark_spec"
        - "execute_external_benchmark"
        - "build_external_benchmark_summary"
    - path: "materials-discovery/tests/test_llm_external_benchmark_core.py"
      provides: "Fixture-backed regression coverage for benchmark execution, shared-slice deltas, and recommendation logic."
    - path: "materials-discovery/src/materials_discovery/llm/__init__.py"
      provides: "Public Phase 36 benchmark API exports."
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/external_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/translated_benchmark.py"
      via: "loads the frozen benchmark set manifest and included rows"
      pattern: "included_inventory_path|TranslatedBenchmarkIncludedRow"
    - from: "materials-discovery/src/materials_discovery/llm/external_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/external_targets.py"
      via: "resolves external registrations and environment lineage"
      pattern: "load_registered_external_target|capture_external_target_environment"
    - from: "materials-discovery/src/materials_discovery/llm/external_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/launch.py"
      via: "reuses internal serving-lane and serving-identity resolution for controls"
      pattern: "resolve_serving_lane|build_serving_identity"
---

<objective>
Implement the Phase 36 benchmark execution and scorecard core.

Purpose: make the comparative benchmark real and replayable before CLI polish
and docs are added.

Output: one execution module, public exports, and focused tests for per-target
artifact writing, fidelity-aware summaries, control deltas, and recommendation
logic.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-CONTEXT.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-RESEARCH.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-01-PLAN.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/translated_benchmark.py
@materials-discovery/src/materials_discovery/llm/external_targets.py
@materials-discovery/src/materials_discovery/llm/serving_benchmark.py
@materials-discovery/src/materials_discovery/llm/compare.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/Progress.md

<interfaces>
Follow the benchmark-entrypoint precedent:
```python
def load_serving_benchmark_spec(spec_path: Path) -> LlmServingBenchmarkSpec: ...
def execute_serving_benchmark(spec_path: Path, *, smoke_only: bool = False, out_path: Path | None = None) -> LlmServingBenchmarkSummary: ...
```

Follow the external-target and internal-control identity helpers:
```python
def load_registered_external_target(model_id: str, *, root: Path | None = None) -> tuple[LlmExternalTargetRegistration, Path]: ...
def build_serving_identity(... ) -> LlmServingIdentity: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared benchmark execution flow and per-target artifact writing</name>
  <files>
    materials-discovery/src/materials_discovery/llm/external_benchmark.py,
    materials-discovery/tests/test_llm_external_benchmark_core.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/src/materials_discovery/llm/external_targets.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-01-PLAN.md,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `execute_external_benchmark(...)` loads the frozen benchmark-set manifest and included rows from Phase 34 and resolves registered external targets plus internal controls from the typed spec.
    - Test 2: each target writes a run manifest, raw response inventory, and case-result inventory under the deterministic Phase 36 artifact layout.
    - Test 3: unsupported target families or systems are recorded as explicit exclusions instead of silently reducing denominators.
    - Test 4: benchmark execution reuses Phase 35 environment capture for external targets and existing serving-identity resolution for internal controls.
    - Test 5: tests can monkeypatch target-response generation paths so the benchmark core remains runnable without private model weights.
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/external_benchmark.py` and implement the benchmark execution core.

Add concrete functions for:
- `load_external_benchmark_spec(spec_path: Path) -> LlmExternalBenchmarkSpec`
- benchmark-set loading and included-row materialization
- external target resolution from registered `model_id`
- internal control resolution from system config and requested lane or pin
- prompt rendering from a small `prompt_contract_id` registry
- response parsing from a small `response_parser_key` registry
- per-target execution that writes run-manifest, raw-response, and case-result artifacts
- `execute_external_benchmark(spec_path: Path, *, root: Path | None = None, out_path: Path | None = None) -> LlmExternalBenchmarkSummary`

Required behavior:
- benchmark from the frozen translated benchmark-set manifest, not live bundles
- preserve target eligibility and explicit exclusion reasons
- capture prompt-contract and environment-lineage pointers in the per-target run manifest
- keep heavy external-runtime imports local to external target generation helpers
- keep the module benchmark-only and additive to the repo's existing LLM paths

Write focused core tests in `materials-discovery/tests/test_llm_external_benchmark_core.py` using fixture benchmark sets, tmp-path registrations, and monkeypatched target runners. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def load_external_benchmark_spec" materials-discovery/src/materials_discovery/llm/external_benchmark.py`
    - `rg -n "def execute_external_benchmark" materials-discovery/src/materials_discovery/llm/external_benchmark.py`
    - `rg -n "run_manifest.json|case_results.jsonl|raw_responses.jsonl" materials-discovery/src/materials_discovery/llm/external_benchmark.py`
    - `rg -n "unsupported_target_family|system_not_supported|control" materials-discovery/tests/test_llm_external_benchmark_core.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_core.py -x -v`
  </acceptance_criteria>
  <done>The repo can execute one shared translated benchmark across external targets and internal controls and persist replayable per-target artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build fidelity-aware scorecards, shared-slice deltas, and recommendation lines</name>
  <files>
    materials-discovery/src/materials_discovery/llm/external_benchmark.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_external_benchmark_core.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/external_benchmark.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-VALIDATION.md,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `build_external_benchmark_summary(...)` aggregates eligible counts, excluded counts, and metric rates overall plus by `target_family` and `fidelity_tier`.
    - Test 2: the summary computes control deltas only on the shared eligible slice between an external target and each control arm.
    - Test 3: recommendation lines privilege periodic-safe slices and avoid overclaiming when only lossy or tiny slices are available.
    - Test 4: failed or unsupported targets remain visible in the summary instead of disappearing from the benchmark output.
    - Test 5: the public API exports the benchmark summary builder for CLI and verification use.
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/external_benchmark.py` with the fidelity-aware scorecard layer.

Add concrete functions for:
- slice aggregation across overall, by-target-family, and by-fidelity sections
- shared eligible control-delta calculation
- recommendation-line generation bounded to the outcome classes locked in `36-CONTEXT.md`
- `build_external_benchmark_summary(...)`

Required behavior:
- keep exact and anchored slices visible as the main decision surface
- preserve approximate and lossy slices as explicit diagnostics
- report failed or unsupported targets clearly
- write the final `benchmark_summary.json`

Re-export the public Phase 36 benchmark API surface from `materials-discovery/src/materials_discovery/llm/__init__.py`.
Extend the core test file so summary and recommendation assertions operate on typed models, not raw dicts. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def build_external_benchmark_summary" materials-discovery/src/materials_discovery/llm/external_benchmark.py`
    - `rg -n "recommendation_lines|control_deltas|fidelity_tier" materials-discovery/src/materials_discovery/llm/external_benchmark.py`
    - `rg -n "shared eligible|periodic-safe|not competitive|follow-up|investment" materials-discovery/tests/test_llm_external_benchmark_core.py`
    - `rg -n "build_external_benchmark_summary|execute_external_benchmark" materials-discovery/src/materials_discovery/llm/__init__.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_core.py -x -v`
  </acceptance_criteria>
  <done>The benchmark now produces decision-grade, fidelity-aware scorecards with explicit shared-slice control deltas.</done>
</task>

</tasks>

<verification>
Run the focused benchmark-core slice after both tasks:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_core.py -x -v`
</verification>

<success_criteria>
Phase 36 can now execute one comparative benchmark from frozen translated cases
through typed per-target artifacts and a fidelity-aware summary.
</success_criteria>

<output>
After completion, create `.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-02-SUMMARY.md`
</output>
