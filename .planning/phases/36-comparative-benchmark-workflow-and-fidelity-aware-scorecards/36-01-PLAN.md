---
phase: 36-comparative-benchmark-workflow-and-fidelity-aware-scorecards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_external_benchmark_schema.py
  - materials-discovery/Progress.md
autonomous: true
requirements: [LLM-32, LLM-33]

must_haves:
  truths:
    - "One typed benchmark contract exists for Phase 36 instead of ad hoc YAML and loose JSON: the benchmark spec, target variants, per-target run manifest, case-result rows, control deltas, and fidelity-aware summary artifacts are explicit."
    - "The benchmark artifact family has a deterministic home under data/benchmarks/llm_external/ with benchmark-level and per-target paths resolved through storage helpers."
    - "The scorecard contract forces target-family-aware and fidelity-aware reporting with shared-eligible-slice deltas instead of one blended leaderboard."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed benchmark spec, target, case-result, run-manifest, control-delta, and summary models for Phase 36."
      contains: "class LlmExternalBenchmarkSpec"
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Deterministic storage helpers for benchmark-run and per-target artifacts."
      exports:
        - "llm_external_benchmark_dir"
        - "llm_external_benchmark_summary_path"
        - "llm_external_benchmark_target_run_manifest_path"
        - "llm_external_benchmark_target_case_results_path"
    - path: "materials-discovery/tests/test_llm_external_benchmark_schema.py"
      provides: "Validator coverage for the benchmark contract and storage layout."
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/translated_benchmark.py"
      via: "references the frozen benchmark manifest and included-row lineage as upstream input"
      pattern: "benchmark_set_manifest_path|candidate_id|fidelity_tier"
    - from: "materials-discovery/src/materials_discovery/llm/schema.py"
      to: "materials-discovery/src/materials_discovery/llm/external_targets.py"
      via: "benchmark targets point at registered external target identity and environment lineage"
      pattern: "model_id|registration_path|environment"
---

<objective>
Freeze the typed Phase 36 contract and artifact layout before benchmark
execution starts.

Purpose: lock the exact benchmark-spec, case-result, and scorecard shape that
the executor, CLI, and docs will rely on, so comparative benchmarking does not
drift into loosely structured experiment files.

Output: typed benchmark models, deterministic storage helpers, public exports,
and focused schema or storage tests.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-CONTEXT.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-RESEARCH.md
@.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-VALIDATION.md
@AGENTS.md
@CLAUDE.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/translated_benchmark.py
@materials-discovery/src/materials_discovery/llm/external_targets.py
@materials-discovery/src/materials_discovery/llm/serving_benchmark.py
@materials-discovery/Progress.md

<interfaces>
Follow the existing typed benchmark precedent:
```python
class LlmServingBenchmarkSpec(BaseModel): ...
class LlmServingBenchmarkTargetResult(BaseModel): ...
class LlmServingBenchmarkSummary(BaseModel): ...
```

Follow the Phase 34 and 35 artifact-family posture:
```python
def translated_benchmark_manifest_path(...): ...
def llm_external_target_registration_path(...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the typed comparative benchmark contract and fidelity-aware summary models</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_external_benchmark_schema.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/src/materials_discovery/llm/external_targets.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-CONTEXT.md,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `LlmExternalBenchmarkSpec` requires a benchmark ID, a translated benchmark-set manifest path, and at least one external target plus one internal control target.
    - Test 2: the contract models explicit target variants for registered external targets and internal controls instead of one ambiguous target blob.
    - Test 3: `LlmExternalBenchmarkCaseResult` preserves benchmark-row lineage plus `target_family`, `fidelity_tier`, exclusion posture, response status, parse status, and comparison metrics.
    - Test 4: `LlmExternalBenchmarkTargetRunManifest` captures target identity, prompt-contract lineage, benchmark-set lineage, environment pointers, and output file paths.
    - Test 5: `LlmExternalBenchmarkSummary` and related scorecard or delta models preserve eligible counts, excluded counts, by-family slices, by-fidelity slices, control deltas, and recommendation lines.
  </behavior>
  <action>
Add the Phase 36 benchmark contract models to `materials-discovery/src/materials_discovery/llm/schema.py`.

Create concrete types for:
- target variants for registered external targets and internal controls
- `LlmExternalBenchmarkSpec`
- `LlmExternalBenchmarkCaseResult`
- `LlmExternalBenchmarkTargetRunManifest`
- any typed scorecard section models needed for overall metrics, by-family slices, by-fidelity slices, and shared eligible control deltas
- `LlmExternalBenchmarkSummary`

Required behavior:
- treat translated benchmark-set manifests and external-target registrations as referenced upstream artifacts, not embedded payload copies
- preserve `target_family`, `fidelity_tier`, `loss_reasons`, and `diagnostic_codes` through the case-result and summary contract
- normalize recommendation lines as a first-class typed field
- keep the design additive; do not extend `SystemConfig`, checkpoint lifecycle state, or generic external serving abstractions

Re-export the new public models from `materials-discovery/src/materials_discovery/llm/__init__.py`.

Create `materials-discovery/tests/test_llm_external_benchmark_schema.py` with focused validator coverage. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class LlmExternalBenchmarkSpec" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class LlmExternalBenchmarkCaseResult" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class LlmExternalBenchmarkTargetRunManifest" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "class LlmExternalBenchmarkSummary" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "control_delta|fidelity_tier|recommendation_lines" materials-discovery/src/materials_discovery/llm/schema.py`
    - `rg -n "LlmExternalBenchmarkSpec|LlmExternalBenchmarkSummary" materials-discovery/src/materials_discovery/llm/__init__.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_schema.py -x -v`
  </acceptance_criteria>
  <done>The repo has one typed Phase 36 contract for benchmark specs, per-case results, and fidelity-aware scorecards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic storage helpers for benchmark-run and per-target artifacts</name>
  <files>
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/tests/test_llm_external_benchmark_schema.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/translated_benchmark.py,
    materials-discovery/src/materials_discovery/llm/external_targets.py,
    .planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: storage helpers write benchmark artifacts under `data/benchmarks/llm_external/{benchmark_id}/`.
    - Test 2: each target gets a deterministic subdirectory and paths for `run_manifest.json`, `case_results.jsonl`, and `raw_responses.jsonl`.
    - Test 3: benchmark-level helpers expose summary and optional smoke or scorecard sidecars without overloading Phase 34 or Phase 35 artifact directories.
    - Test 4: blank benchmark IDs or target IDs fail through the existing artifact-ID guard.
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/storage.py` with deterministic helpers for the Phase 36 benchmark artifact family, including:
- `llm_external_benchmark_dir`
- `llm_external_benchmark_summary_path`
- `llm_external_benchmark_smoke_path`
- `llm_external_benchmark_target_dir`
- `llm_external_benchmark_target_run_manifest_path`
- `llm_external_benchmark_target_case_results_path`
- `llm_external_benchmark_target_raw_responses_path`
- any helper needed for an inspectable scorecard-by-case sidecar

Use the artifact root `data/benchmarks/llm_external/{benchmark_id}/`.
Do not overload:
- `data/benchmarks/llm_external_sets/`
- `data/llm_external_models/`
- `data/llm_checkpoints/`
- existing serving-benchmark directories

Expand `materials-discovery/tests/test_llm_external_benchmark_schema.py` so the storage layout is locked together with the contract. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def llm_external_benchmark_dir" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def llm_external_benchmark_summary_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def llm_external_benchmark_target_run_manifest_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "def llm_external_benchmark_target_case_results_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "llm_external_benchmark_target_raw_responses_path" materials-discovery/src/materials_discovery/llm/storage.py`
    - `rg -n "data\" / \"benchmarks\" / \"llm_external\"" materials-discovery/src/materials_discovery/llm/storage.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_schema.py -x -v`
  </acceptance_criteria>
  <done>The Phase 36 benchmark artifact layout is deterministic, isolated, and ready for execution logic.</done>
</task>

</tasks>

<verification>
Run the focused schema or storage slice after both tasks:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_benchmark_schema.py -x -v`
</verification>

<success_criteria>
Phase 36 now has one typed, storage-backed benchmark contract that makes the
shared execution and scorecard workflow explicit before runtime code begins.
</success_criteria>

<output>
After completion, create `.planning/phases/36-comparative-benchmark-workflow-and-fidelity-aware-scorecards/36-01-SUMMARY.md`
</output>
