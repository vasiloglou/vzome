---
phase: 44-prose-enrichment-and-zomic-annotation
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - materials-discovery/developers-docs/guided-design-tutorial.md
  - materials-discovery/notebooks/guided_design_tutorial.ipynb
  - materials-discovery/Progress.md
autonomous: true
requirements: [NARR-03, NARR-04, NARR-05]
must_haves:
  truths:
    - "Section 5 explains what energy_proxy_ev_per_atom and min_distance_proxy measure physically, how the shortlist threshold works, and what passed_count vs shortlisted_count means"
    - "Section 5 has a 'What the numbers mean' callout box after the screening output"
    - "Section 6 frames the all-gates-False validation result as honest early-stage behavior with a gate checklist"
    - "Section 6 explains each gate: geometry_prefilter, phonon_imaginary_modes, md_stability_score, xrd_confidence"
    - "Sections 9.1-9.3 each have an explain-then-command-then-annotate block for the LLM commands with the same depth as the deterministic spine"
    - "The notebook has condensed versions of screening, validation, and LLM explanations with links to the markdown"
  artifacts:
    - path: "materials-discovery/developers-docs/guided-design-tutorial.md"
      provides: "Enriched screening, validation, and LLM sections"
      contains: "energy_proxy_ev_per_atom"
    - path: "materials-discovery/notebooks/guided_design_tutorial.ipynb"
      provides: "Condensed explanation cells for screening, validation, LLM"
      contains: "What the numbers mean"
  key_links:
    - from: "materials-discovery/developers-docs/guided-design-tutorial.md Section 5"
      to: "data/calibration/sc_zn_screen_calibration.json"
      via: "references calibration values in explanatory prose"
      pattern: "energy_proxy_ev_per_atom"
    - from: "materials-discovery/developers-docs/guided-design-tutorial.md Section 6"
      to: "data/hifi_validated/sc_zn_all_validated.jsonl"
      via: "references validation field values in gate checklist"
      pattern: "geometry_prefilter_pass"
    - from: "materials-discovery/notebooks/guided_design_tutorial.ipynb"
      to: "materials-discovery/developers-docs/guided-design-tutorial.md"
      via: "relative markdown links in condensed cells"
      pattern: "guided-design-tutorial\\.md"
---

<objective>
Enrich the screening, validation, and LLM sections of the guided design tutorial with plain-language explanations — one paragraph per metric, callout boxes, gate checklists, and explain-then-command-then-annotate blocks for each LLM command. Mirror condensed versions into notebook cells.

Purpose: NARR-03 (screening), NARR-04 (validation), and NARR-05 (LLM lane) transform the tutorial's output-display sections into educational prose that helps readers understand what each metric, gate, and LLM command actually means.

Output: Enriched Sections 5, 6, and 9.1-9.3 in the markdown tutorial, plus corresponding condensed notebook cells.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-prose-enrichment-and-zomic-annotation/44-CONTEXT.md
@.planning/phases/44-prose-enrichment-and-zomic-annotation/44-RESEARCH.md
@.planning/phases/44-prose-enrichment-and-zomic-annotation/44-02-SUMMARY.md

@materials-discovery/developers-docs/guided-design-tutorial.md
@materials-discovery/notebooks/guided_design_tutorial.ipynb

<interfaces>
<!-- Sections being enriched (from current tutorial structure) -->
Markdown Section 5 "Screen the Candidate Population" currently has:
- Command block, output table, inspection commands
- Brief bullet list: "passed_count tells you...", "shortlisted_count tells you..."
- Calibration snapshot: 30 input, 20 passing, 4 shortlisted

Markdown Section 6 "Run High-Fidelity Validation" currently has:
- Command block, output table, inspection commands
- Brief bullet list defining each field
- Calibration snapshot: md_000006 fails all gates

Markdown Section 9.1 "Same-system companion lane" currently has:
- Path table, representative commands
- Artifact families
- "What to inspect" and "What the signal means" blocks

Markdown Section 9.2 "Why this branch switches to Al-Cu-Fe" currently has:
- Two paragraphs explaining the chemistry switch

Markdown Section 9.3 "Translation and external benchmark branch" currently has:
- Four sub-steps (Export, Freeze, Register, Benchmark) with commands and inspect blocks

<!-- Notebook cell IDs for insertion points -->
- b1aada31: screening code cell (insert explanation cell AFTER this)
- 4509cd69: validation code cell (insert explanation cell AFTER this)
- 949545c4: LLM generate/evaluate code cell (insert explanation cell AFTER this)
- 1119a5de: existing "What the signal means" markdown cell (enrich or replace)
- 435cc36b: "Why this branch switches to Al-Cu-Fe" (enrich)
- 13a4b576: "Translation and external benchmark branch" heading cell
- 123812e6: first translation code cell (insert explanation AFTER)

<!-- Screening field values from RESEARCH.md (use these exact values in explanations) -->
Calibration snapshot: input_count=30, passed_count=20, shortlisted_count=4
First candidate: energy_proxy_ev_per_atom=-2.778674, min_distance_proxy=0.751937

<!-- Validation field values from RESEARCH.md (use these exact values in explanations) -->
Validated row md_000006: geometry_prefilter_pass=False, phonon_imaginary_modes=99, md_stability_score=0.0, xrd_confidence=0.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich screening and validation sections in markdown (NARR-03 + NARR-04)</name>
  <files>materials-discovery/developers-docs/guided-design-tutorial.md</files>
  <read_first>
    - materials-discovery/developers-docs/guided-design-tutorial.md
    - .planning/phases/44-prose-enrichment-and-zomic-annotation/44-RESEARCH.md
  </read_first>
  <action>
Enrich TWO existing sections in guided-design-tutorial.md. PRESERVE all existing content — add new paragraphs and callout boxes around the existing text. Do NOT delete existing bullet lists or calibration snapshots.

**Section 5 enrichment (NARR-03) — Insert AFTER the existing "How to interpret the screening artifacts:" bullet list and BEFORE the "As of 2026-04-15" calibration snapshot paragraph:**

Add one paragraph per metric with this structure:

Paragraph 1 — `energy_proxy_ev_per_atom`:
"**What `energy_proxy_ev_per_atom` measures.** This is the estimated cohesive energy per atom from a fast empirical potential, not a full DFT calculation. A more negative value means stronger atomic binding and greater thermodynamic plausibility. For Sc-Zn intermetallics, values below roughly -2.0 eV/atom are energetically plausible. The checked first candidate shows -2.78 eV/atom, which passes the threshold comfortably."

Paragraph 2 — `min_distance_proxy`:
"**What `min_distance_proxy` measures.** This is the minimum normalized intersite distance across all atom pairs in the candidate structure. Values below 0.7 indicate atoms that are unphysically close — a geometry error rather than a chemistry signal. The checked first candidate shows 0.75, clearing the crowding threshold."

Paragraph 3 — threshold and counts:
"**How the shortlist works.** Candidates that clear both the energy proxy and distance proxy thresholds enter the `passed` pool. From that pool, the pipeline ranks by a composite score and forwards the top slice to expensive validation. `passed_count` (20 out of 30 in this batch) tells you how much of the generated batch is geometrically and energetically plausible. `shortlisted_count` (4) tells you how many enter the validation queue — a deliberate bottleneck that controls validation compute cost."

Then add a blockquote callout:

```markdown
> **What the numbers mean**
>
> - `energy_proxy_ev_per_atom: -2.78` — below the threshold, so this candidate
>   is energetically plausible for a Sc-Zn intermetallic.
> - `min_distance_proxy: 0.75` — above the crowding threshold; atoms are not
>   unphysically close.
> - `shortlist_rank: 1` — this candidate is first in the queue for expensive
>   validation.
```

**Section 6 enrichment (NARR-04) — Insert AFTER the existing "How to interpret the validation fields:" bullet list and BEFORE the "As of 2026-04-15" calibration snapshot paragraph:**

Add one paragraph with the "honest early-stage batch" framing:

"**Why all gates are False in this batch.** An early-stage candidate batch run against a mock backend is expected to fail every gate. The geometry prefilter catches crowded structures before phonon work begins. The phonon, MD, and XRD signals come from the mock backend which returns sentinel values (99 imaginary modes, 0.0 stability, 0.0 confidence) rather than real physics. The pipeline is not broken — it is showing you the unfiltered result of a first-pass generation run."

Then add the gate checklist blockquote:

```markdown
> **Release gate checklist for this batch**
>
> - [ ] `geometry_prefilter_pass` — atom-crowding check (False = too crowded
>   for phonon work)
> - [ ] `phonon_imaginary_modes == 0` — no imaginary frequencies (99 = mock
>   sentinel, not a real count)
> - [ ] `md_stability_score > 0.8` — short-MD structural stability (0.0 = mock
>   placeholder)
> - [ ] `xrd_confidence > 0.7` — pattern match to reference (0.0 = mock
>   placeholder)
>
> All gates are False. This is the correct result for a mock-backend run on an
> early-stage candidate batch. The pipeline is not broken; it is showing you the
> unfiltered signal before any candidate has accumulated enough evidence to be
> promoted.
```

CRITICAL: Preserve ALL existing text in both sections. Insert new content at the specified positions, do not replace existing bullet lists.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && grep -c "What .energy_proxy_ev_per_atom. measures" developers-docs/guided-design-tutorial.md && grep -c "What .min_distance_proxy. measures" developers-docs/guided-design-tutorial.md && grep -c "What the numbers mean" developers-docs/guided-design-tutorial.md && grep -c "Release gate checklist" developers-docs/guided-design-tutorial.md && grep -c "Why all gates are False" developers-docs/guided-design-tutorial.md</automated>
  </verify>
  <acceptance_criteria>
    - guided-design-tutorial.md Section 5 contains "What `energy_proxy_ev_per_atom` measures"
    - guided-design-tutorial.md Section 5 contains "What `min_distance_proxy` measures"
    - guided-design-tutorial.md Section 5 contains "How the shortlist works"
    - guided-design-tutorial.md Section 5 contains blockquote "What the numbers mean" with energy_proxy and min_distance values
    - guided-design-tutorial.md Section 6 contains "Why all gates are False in this batch"
    - guided-design-tutorial.md Section 6 contains blockquote "Release gate checklist" with all 4 gates listed
    - All existing section headings remain intact (grep "## 5\\. Screen" and "## 6\\. Run High-Fidelity" still match)
    - All existing bullet points in sections 5 and 6 still present
  </acceptance_criteria>
  <done>Section 5 explains both screening metrics with physical meaning, threshold logic, and a "What the numbers mean" callout. Section 6 explains each validation gate and frames all-gates-False as honest early-stage behavior with a gate checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich LLM sections in markdown (NARR-05)</name>
  <files>materials-discovery/developers-docs/guided-design-tutorial.md</files>
  <read_first>
    - materials-discovery/developers-docs/guided-design-tutorial.md
    - .planning/phases/44-prose-enrichment-and-zomic-annotation/44-RESEARCH.md
  </read_first>
  <action>
Enrich Sections 9.1, 9.2, and 9.3 with explain-then-command-then-annotate depth matching the deterministic spine sections. PRESERVE all existing content — add explanatory paragraphs around the existing command blocks.

**Section 9.1 enrichment (llm-generate and llm-evaluate):**

Insert a new explanatory paragraph BEFORE the "Representative commands:" line:

"**What `llm-generate` does.** This command uses a language model as an alternate proposal source for candidate structures. Instead of expanding the Zomic-backed geometry deterministically, the LLM proposes new site arrangements given the Sc-Zn design constraints. The resulting candidates feed into the same screening and validation stages shown in Sections 5-6 — the LLM replaces only the proposal step, not the evidence chain."

Insert another paragraph AFTER the `llm-evaluate` command block and BEFORE the "Artifact families" list:

"**What `llm-evaluate` does.** This command adds an LLM assessment layer on top of ranked deterministic candidates. It does not replace the deterministic validation or report evidence; it provides additive signals about synthesis feasibility and precursor availability. The evaluated JSONL carries these signals alongside the original proxy and validation fields."

**Section 9.2 enrichment (chemistry switch explanation):**

Insert one paragraph AFTER the existing two paragraphs, BEFORE Section 9.3:

"**What the chemistry switch means for reading the tutorial.** When you see Al-Cu-Fe artifacts in the next section, you are looking at a different chemical system with its own fixture-backed benchmark data. The commands and artifact handoffs work identically to the Sc-Zn lane — only the composition and the backing fixtures change. This is deliberate: the tutorial demonstrates that the pipeline's artifact contracts are chemistry-agnostic."

**Section 9.3 enrichment (llm-translate and benchmark):**

Insert a new paragraph BEFORE the first sub-step heading ("#### Export and inspect translated bundles"):

"**What `llm-translate` does.** This command converts internal candidate records from the pipeline's native JSONL format into external interoperability formats (CIF files, material-string bundles). The translation preserves as much structural fidelity as possible and flags any information loss. Downstream benchmark commands consume these translated artifacts, not the raw pipeline JSONL."

Insert a new paragraph BEFORE the "#### Freeze and inspect one benchmark pack" sub-step:

"**What `llm-translated-benchmark-freeze` does.** This command creates an immutable snapshot of a translated candidate set for benchmarking. The freeze contract guarantees that the benchmark inputs will not change even if the upstream translation is re-run later. The `included.jsonl` contains the cases the benchmark is allowed to score; `excluded.jsonl` keeps rejected or mismatched rows visible."

Insert a new paragraph BEFORE the "#### Register, inspect, and smoke-test the external target" sub-step:

"**What `llm-register-external-target` does.** This command registers an external model (a downloaded checkpoint, a remote API endpoint, or a local inference server) as a named target that the benchmark can invoke. Registration captures the target identity and environment state. The smoke test verifies the target can process a single test case before committing to a full benchmark run."

Insert a new paragraph BEFORE the "#### Run and inspect the comparative benchmark" sub-step:

"**What `llm-external-benchmark` does.** This command runs the frozen benchmark set against all registered external targets and the internal pipeline baseline, producing a comparative scorecard. The summary separates exact/anchored evidence from approximate/lossy diagnostics. Recommendation lines are advisory milestone evidence — they do not trigger automatic promotion."

CRITICAL: Preserve ALL existing text. Insert new paragraphs at specified positions only.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && grep -c "What .llm-generate. does" developers-docs/guided-design-tutorial.md && grep -c "What .llm-evaluate. does" developers-docs/guided-design-tutorial.md && grep -c "What .llm-translate. does" developers-docs/guided-design-tutorial.md && grep -c "What .llm-translated-benchmark-freeze. does" developers-docs/guided-design-tutorial.md && grep -c "What .llm-external-benchmark. does" developers-docs/guided-design-tutorial.md</automated>
  </verify>
  <acceptance_criteria>
    - guided-design-tutorial.md Section 9.1 contains "What `llm-generate` does"
    - guided-design-tutorial.md Section 9.1 contains "What `llm-evaluate` does"
    - guided-design-tutorial.md Section 9.2 contains "What the chemistry switch means"
    - guided-design-tutorial.md Section 9.3 contains "What `llm-translate` does"
    - guided-design-tutorial.md Section 9.3 contains "What `llm-translated-benchmark-freeze` does"
    - guided-design-tutorial.md Section 9.3 contains "What `llm-register-external-target` does"
    - guided-design-tutorial.md Section 9.3 contains "What `llm-external-benchmark` does"
    - All existing section headings 9.1, 9.2, 9.3 remain intact
    - All existing command blocks in sections 9.1-9.3 remain intact
  </acceptance_criteria>
  <done>Sections 9.1-9.3 each have explain-then-command-then-annotate blocks for every LLM command with the same depth as the deterministic spine sections.</done>
</task>

<task type="auto">
  <name>Task 3: Add condensed explanation cells to notebook and update Progress.md</name>
  <files>materials-discovery/notebooks/guided_design_tutorial.ipynb, materials-discovery/Progress.md</files>
  <read_first>
    - materials-discovery/notebooks/guided_design_tutorial.ipynb
    - materials-discovery/developers-docs/guided-design-tutorial.md
  </read_first>
  <action>
Add FOUR new markdown cells to the notebook. Follow the v1.81 convention: condensed (1-3 sentences) + link to markdown for depth.

**Cell 1 — Screening explanation (insert AFTER cell id=b1aada31, the screening code cell)**

```markdown
### What the screening numbers mean

`energy_proxy_ev_per_atom` estimates cohesive energy per atom — more negative means more stable. `min_distance_proxy` catches unphysically close atoms. Of 30 input candidates, 20 pass both thresholds and 4 enter the shortlist for expensive validation.

See [Section 5](../developers-docs/guided-design-tutorial.md#5-screen-the-candidate-population) in the full tutorial for the metric-by-metric explanation.
```

**Cell 2 — Validation explanation (insert AFTER cell id=4509cd69, the validation code cell)**

```markdown
### What the validation gates mean

All gates are False in this batch — that is the expected result for a mock-backend early-stage run. `geometry_prefilter_pass` catches crowding, `phonon_imaginary_modes` checks dynamical stability, `md_stability_score` measures structural drift, and `xrd_confidence` compares diffraction patterns. The pipeline is showing you the unfiltered signal, not a failure.

See [Section 6](../developers-docs/guided-design-tutorial.md#6-run-high-fidelity-validation) in the full tutorial for the gate checklist and honest-batch framing.
```

**Cell 3 — LLM companion lane explanation (insert AFTER cell id=949545c4, the LLM generate/evaluate code cell, and BEFORE cell id=1119a5de)**

If cell 1119a5de ("What the signal means") already exists, REPLACE its content with:

```markdown
### What the LLM companion lane does

`llm-generate` uses a language model as an alternate proposal source — it replaces only the proposal step, not the validation evidence chain. `llm-evaluate` adds an assessment layer (synthesis feasibility, precursor availability) on top of ranked candidates without replacing deterministic validation.

See [Section 9.1](../developers-docs/guided-design-tutorial.md#91-same-system-companion-lane) in the full tutorial for the full explain-then-command-then-annotate walkthrough.
```

**Cell 4 — Translation/benchmark explanation (insert AFTER cell id=123812e6, the first translation code cell)**

```markdown
### What the translation and benchmark branch does

`llm-translate` converts internal candidates to external formats (CIF, material strings). `llm-translated-benchmark-freeze` creates an immutable snapshot for benchmarking. `llm-register-external-target` registers a model for comparison. `llm-external-benchmark` runs the comparative scorecard.

See [Section 9.3](../developers-docs/guided-design-tutorial.md#93-translation-and-external-benchmark-branch) in the full tutorial for the step-by-step explanation.
```

To insert/replace cells, parse the notebook JSON, find target cell indices by cell id, and insert or replace at the correct positions. Each new cell: `cell_type: "markdown"`, `metadata: {}`, `source: [...]`, `id: <unique 8-char hex>`.

Update materials-discovery/Progress.md:
- Add a changelog row: `| 2026-04-15 | Tutorial screening/validation/LLM prose | Added plain-language explanations for screening metrics, validation gates, and LLM commands in tutorial and notebook |`
- Append to diary section under `### 2026-04-15`: describe the NARR-03, NARR-04, NARR-05 additions.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && python3 -c "
import json
with open('notebooks/guided_design_tutorial.ipynb') as f:
    nb = json.load(f)
checks = {
    'screening': False,
    'validation': False,
    'llm_companion': False,
    'translation': False,
}
for cell in nb['cells']:
    src = ''.join(cell['source'])
    if 'What the screening numbers mean' in src:
        checks['screening'] = True
    if 'What the validation gates mean' in src:
        checks['validation'] = True
    if 'What the LLM companion lane does' in src:
        checks['llm_companion'] = True
    if 'What the translation and benchmark branch does' in src:
        checks['translation'] = True
for name, found in checks.items():
    assert found, f'Missing notebook cell: {name}'
print('OK: all 4 explanation cells found')
"</automated>
  </verify>
  <acceptance_criteria>
    - Notebook contains markdown cell with "What the screening numbers mean" after the screening code cell
    - Notebook contains markdown cell with "What the validation gates mean" after the validation code cell
    - Notebook contains markdown cell with "What the LLM companion lane does" after the LLM code cell
    - Notebook contains markdown cell with "What the translation and benchmark branch does" after the translation code cell
    - Each new cell contains a link to "../developers-docs/guided-design-tutorial.md"
    - The notebook JSON is valid (python3 JSON parse exits 0)
    - Progress.md contains "screening/validation/LLM" in the changelog
  </acceptance_criteria>
  <done>Notebook has 4 new condensed explanation cells covering screening, validation, LLM companion lane, and translation/benchmark — each linking to the full tutorial. Progress.md updated.</done>
</task>

</tasks>

<verification>
- `grep "What the numbers mean" materials-discovery/developers-docs/guided-design-tutorial.md` returns a match (NARR-03)
- `grep "Release gate checklist" materials-discovery/developers-docs/guided-design-tutorial.md` returns a match (NARR-04)
- `grep "What .llm-generate. does" materials-discovery/developers-docs/guided-design-tutorial.md` returns a match (NARR-05)
- Notebook verification script confirms all 4 condensed cells exist
- `cd materials-discovery && uv run pytest tests/ -x -q` — full suite still green (no code changes, but confirm no breakage)
</verification>

<success_criteria>
- Section 5 explains both screening metrics physically with a "What the numbers mean" callout (NARR-03)
- Section 6 explains each validation gate with a gate checklist and honest-batch framing (NARR-04)
- Sections 9.1-9.3 have explain-then-command-then-annotate blocks for every LLM command (NARR-05)
- Notebook has condensed versions of all three explanation types with links to the markdown
- No existing content deleted or section headings renumbered
</success_criteria>

<output>
After completion, create `.planning/phases/44-prose-enrichment-and-zomic-annotation/44-03-SUMMARY.md`
</output>
