---
phase: 07-llm-inference-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - materials-discovery/src/materials_discovery/llm/prompting.py
  - materials-discovery/src/materials_discovery/llm/compiler.py
  - materials-discovery/src/materials_discovery/llm/generate.py
  - materials-discovery/src/materials_discovery/generator/candidate_factory.py
  - materials-discovery/src/materials_discovery/common/stage_metrics.py
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml
  - materials-discovery/configs/systems/sc_zn_llm_mock.yaml
  - materials-discovery/tests/test_llm_generate_core.py
  - materials-discovery/tests/test_llm_generate_cli.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-02]

must_haves:
  truths:
    - "The llm-generate path remains config-driven and supports optional seed Zomic input without turning into a free-form chat interface."
    - "Failures use bounded retries and preserve every raw completion and validation outcome in run artifacts."
    - "Valid outputs become standard CandidateRecord JSONL that downstream screen can consume without a schema fork."
    - "llm-generate emits calibration JSON, a stage manifest, and a separate run manifest for heavy prompt/output lineage."
    - "Every Phase 7 execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/prompting.py"
      provides: "Config-driven prompt assembly for fresh generation and optional seed variation"
      exports: ["build_generation_prompt", "load_seed_zomic_text"]
    - path: "materials-discovery/src/materials_discovery/llm/generate.py"
      provides: "The bounded-retry llm-generate engine and run-artifact writer"
      exports: ["generate_llm_candidates"]
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "The `mdisc llm-generate` command"
    - path: "materials-discovery/src/materials_discovery/common/stage_metrics.py"
      provides: "llm-generate calibration metrics helper"
      exports: ["llm_generation_metrics"]
    - path: "materials-discovery/tests/test_llm_generate_core.py"
      provides: "Focused engine tests for retries, compile classification, and candidate conversion"
    - path: "materials-discovery/tests/test_llm_generate_cli.py"
      provides: "Typer command tests for llm-generate"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/generate.py"
      to: "materials-discovery/src/materials_discovery/llm/runtime.py"
      via: "adapter resolution and raw completion generation"
      pattern: "resolve_llm_adapter"
    - from: "materials-discovery/src/materials_discovery/llm/generate.py"
      to: "materials-discovery/src/materials_discovery/llm/compiler.py"
      via: "parse/compile classification for each attempt"
      pattern: "compile_zomic_script"
    - from: "materials-discovery/src/materials_discovery/llm/generate.py"
      to: "materials-discovery/src/materials_discovery/generator/candidate_factory.py"
      via: "candidate creation from an llm-produced orbit-library template"
      pattern: "CandidateRecord|prototype_library"
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/common/manifest.py"
      via: "llm_generate stage manifest emission"
      pattern: "build_manifest|write_manifest"
---

<objective>
Implement the core `mdisc llm-generate` path: prompt assembly, bounded retry
generation, compile validation, candidate conversion, CLI wiring, and example
configs.

Purpose: Turn the Phase 7 runtime seam into an actually usable generation path
that writes standard candidates plus the richer run-level audit trail required
by the context decisions.

Output: `llm/prompting.py`, `llm/generate.py`, CLI command wiring, calibration
metrics, mock configs for `Al-Cu-Fe` and `Sc-Zn`, and focused engine/CLI tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-llm-inference-mvp/07-CONTEXT.md
@.planning/phases/07-llm-inference-mvp/07-RESEARCH.md
@.planning/phases/07-llm-inference-mvp/07-VALIDATION.md
@.planning/phases/07-llm-inference-mvp/07-01-PLAN.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/common/stage_metrics.py
@materials-discovery/src/materials_discovery/common/manifest.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/src/materials_discovery/llm/compiler.py
@materials-discovery/src/materials_discovery/generator/candidate_factory.py
@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/developers-docs/pipeline-stages.md

<interfaces>
From common/schema.py:
```python
class LlmGenerateConfig(BaseModel): ...
class LlmGenerateSummary(BaseModel): ...
class CandidateRecord(BaseModel): ...
class SystemConfig(BaseModel): ...
```

From llm/runtime.py:
```python
def resolve_llm_adapter(mode: str, adapter: str | None = None) -> LlmAdapter: ...
```

From llm/compiler.py:
```python
def compile_zomic_script(
    zomic_text: str,
    *,
    prototype_key: str,
    system_name: str,
    template_family: str,
    source_qphi_bounds: list[QPhiCoord] | None = None,
) -> dict[str, Any]: ...
```

From candidate_factory.py:
```python
def generate_candidates(
    config: SystemConfig,
    output_path: Path,
    count: int,
    seed: int | None,
    config_path: Path | None = None,
) -> GenerateSummary: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the config-driven prompt, retry, compile, and candidate-conversion engine</name>
  <files>
    materials-discovery/src/materials_discovery/llm/prompting.py,
    materials-discovery/src/materials_discovery/llm/compiler.py,
    materials-discovery/src/materials_discovery/llm/generate.py,
    materials-discovery/src/materials_discovery/generator/candidate_factory.py,
    materials-discovery/tests/test_llm_generate_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/llm/compiler.py,
    materials-discovery/src/materials_discovery/generator/candidate_factory.py,
    materials-discovery/src/materials_discovery/generator/approximant_templates.py,
    materials-discovery/developers-docs/pipeline-stages.md,
    .planning/phases/07-llm-inference-mvp/07-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: build_generation_prompt includes system name, species bounds, template family, and optional seed Zomic text
    - Test 2: generate_llm_candidates writes a run directory containing `prompt.json`, `attempts.jsonl`, `compile_results.jsonl`, and `run_manifest.json`
    - Test 3: the retry loop stops when the requested number of valid candidates is produced or `max_attempts * requested_count` is exhausted
    - Test 4: invalid parse/compile attempts are preserved in attempts/compile result artifacts rather than silently dropped
    - Test 5: valid attempts are converted into standard CandidateRecord rows that include additive llm provenance and remain compatible with `screen`
    - Test 6: the engine supports an explicit seed-Zomic file path and a no-seed fresh-generation path
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/prompting.py` with:
- `load_seed_zomic_text(path: Path | None) -> str | None`
- `build_generation_prompt(config: SystemConfig, *, count: int, seed_zomic_text: str | None) -> str`

Make the prompt concrete and config-driven. It must include:
- `system_name`
- `template_family`
- species names and composition min/max bounds
- instruction to emit only Zomic text
- when a seed script exists, a clearly delimited seed block labeled `SEED_ZOMIC`

Extend `materials-discovery/src/materials_discovery/llm/compiler.py` so the return shape can distinguish:
- `parse_status`
- `compile_status`
- `error_kind`
- `error_message`
- `raw_export_path`
- `orbit_library_path`

Keep the current Java/vZome bridge as the authority. Do not add a second parser implementation.

Create `materials-discovery/src/materials_discovery/llm/generate.py` with:
- `generate_llm_candidates(config: SystemConfig, output_path: Path, count: int, config_path: Path | None = None, seed_zomic_path: Path | None = None, temperature_override: float | None = None) -> LlmGenerateSummary`
- run directory location:
  - `data/llm_runs/{system_slug}_{request_hash}/`
  - `request_hash` must be derived from config hash + requested count + effective temperature + seed path value
- committed run artifacts:
  - `prompt.json`
  - `attempts.jsonl`
  - `compile_results.jsonl`
  - `run_manifest.json`

Implement bounded retry behavior:
- maximum attempts = `requested_count * config.llm_generate.max_attempts`
- no model-repair loop
- every attempt records prompt path, raw completion path, parse/compile outcome, and error_kind

Refactor `materials-discovery/src/materials_discovery/generator/candidate_factory.py` by extracting a helper with a concrete name such as `build_candidate_from_prototype_library(...)` that:
- accepts `config`, `seed`, `candidate_index`, and `template_override_path`
- returns one `CandidateRecord`
- reuses the existing site/species assignment path instead of inventing a new candidate schema

Use that helper from `llm/generate.py` so each valid compiled orbit-library becomes a standard `CandidateRecord` with additive provenance fields:
- `source: "llm"`
- `llm_run_id`
- `llm_attempt_id`
- `llm_adapter`
- `llm_provider`
- `llm_model`
- `seed_zomic_path` when present

Create `materials-discovery/tests/test_llm_generate_core.py` with focused tests for prompt contents, run-artifact writing, retry stopping rules, compile classification, and standard CandidateRecord conversion.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/prompting.py contains "def build_generation_prompt("
    - materials-discovery/src/materials_discovery/llm/generate.py contains "def generate_llm_candidates("
    - materials-discovery/src/materials_discovery/llm/generate.py contains "data/llm_runs"
    - materials-discovery/src/materials_discovery/llm/generate.py contains "prompt.json"
    - materials-discovery/src/materials_discovery/llm/generate.py contains "attempts.jsonl"
    - materials-discovery/src/materials_discovery/llm/generate.py contains "compile_results.jsonl"
    - materials-discovery/src/materials_discovery/generator/candidate_factory.py contains "build_candidate_from_prototype_library"
    - materials-discovery/tests/test_llm_generate_core.py contains at least 5 test functions
    - The core generation test command exits 0
  </acceptance_criteria>
  <done>The repo can generate constrained Zomic attempts, validate them, preserve every attempt, and turn valid outputs into standard CandidateRecord rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the llm-generate CLI, metrics, manifests, and example configs</name>
  <files>
    materials-discovery/src/materials_discovery/common/stage_metrics.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml,
    materials-discovery/configs/systems/sc_zn_llm_mock.yaml,
    materials-discovery/tests/test_llm_generate_cli.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/common/stage_metrics.py,
    materials-discovery/src/materials_discovery/common/manifest.py,
    materials-discovery/src/materials_discovery/common/io.py,
    materials-discovery/tests/test_cli.py,
    materials-discovery/tests/test_llm_corpus_cli.py,
    materials-discovery/developers-docs/pipeline-stages.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-generate --config ... --count ...` prints a JSON summary with output, calibration, manifest, and run manifest paths
    - Test 2: default output path is `data/candidates/{slug}_llm_candidates.jsonl`
    - Test 3: a bad config or provider misconfiguration returns exit code `2`
    - Test 4: the two committed mock configs for `Al-Cu-Fe` and `Sc-Zn` validate as SystemConfig
    - Test 5: calibration records requested_count, attempt_count, parse_pass_count, compile_pass_count, generated_count, and pass rates
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/common/stage_metrics.py` with:
- `llm_generation_metrics(requested_count: int, attempt_count: int, parse_pass_count: int, compile_pass_count: int, generated_count: int) -> dict[str, float | int | bool]`

The returned calibration dict must include these exact keys:
- `requested_count`
- `attempt_count`
- `parse_pass_count`
- `compile_pass_count`
- `generated_count`
- `parse_pass_rate`
- `compile_pass_rate`
- `generation_success_rate`
- `passed`

Wire a new `@app.command("llm-generate")` into `materials-discovery/src/materials_discovery/cli.py` with this exact CLI signature:

```text
mdisc llm-generate --config PATH --count INT [--seed-zomic PATH] [--temperature FLOAT] [--out PATH]
```

The command must:
- load `SystemConfig`
- call `generate_llm_candidates(...)`
- write candidates JSONL to `data/candidates/{slug}_llm_candidates.jsonl` when `--out` is omitted
- write calibration JSON to `data/calibration/{slug}_llm_generation_metrics.json`
- write a stage manifest with `stage="llm_generate"`
- print the `LlmGenerateSummary` JSON to stdout
- return exit code `2` on `FileNotFoundError`, `ValidationError`, `ValueError`, or `RuntimeError`

Create example configs:
- `materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml`
- `materials-discovery/configs/systems/sc_zn_llm_mock.yaml`

Each config must:
- start from the corresponding committed system shape (`Al-Cu-Fe` or `Sc-Zn`)
- set `backend.mode: mock`
- set `backend.llm_adapter: llm_fixture_v1`
- set `backend.llm_provider: mock`
- set `backend.llm_model: fixture-zomic-v1`
- set `llm_generate.prompt_template: zomic_generate_v1`
- set `llm_generate.max_attempts: 3`
- set `llm_generate.fixture_outputs:` to at least two short `.zomic` examples

Create `materials-discovery/tests/test_llm_generate_cli.py` with focused Typer tests, and extend `materials-discovery/tests/test_cli.py` so the main CLI surface includes at least one `llm-generate` success case and one invalid-config or invalid-provider case.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_cli.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains "@app.command(\"llm-generate\")"
    - materials-discovery/src/materials_discovery/common/stage_metrics.py contains "def llm_generation_metrics("
    - materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml exists
    - materials-discovery/configs/systems/sc_zn_llm_mock.yaml exists
    - materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml contains "llm_adapter: llm_fixture_v1"
    - materials-discovery/configs/systems/sc_zn_llm_mock.yaml contains "llm_model: fixture-zomic-v1"
    - materials-discovery/tests/test_llm_generate_cli.py contains at least 3 test functions
    - materials-discovery/tests/test_cli.py contains "llm-generate"
    - The CLI test command exits 0
  </acceptance_criteria>
  <done>The llm-generate command exists, writes standard artifacts, and is exercisable through committed mock configs.</done>
</task>

</tasks>

## Verification

Verify this plan by checking:

1. Prompt assembly is config-driven and supports optional seed Zomic input.
2. Every attempt is persisted under a run directory with parse/compile outcomes.
3. Valid attempts become standard `CandidateRecord` JSONL.
4. The CLI writes candidates, calibration, manifest, and run manifest outputs.
5. Example mock configs for `Al-Cu-Fe` and `Sc-Zn` work through focused tests.

## Success Criteria

- [ ] `generate_llm_candidates(...)` exists and writes run artifacts.
- [ ] Candidate conversion reuses the existing generator path instead of forking the schema.
- [ ] `mdisc llm-generate` prints a standard JSON summary and returns exit code `2` on bad input.
- [ ] Calibration and manifest outputs exist for the new stage.
- [ ] Focused core and CLI tests pass.
