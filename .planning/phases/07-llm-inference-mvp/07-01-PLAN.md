---
phase: 07-llm-inference-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/common/schema.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/runtime.py
  - materials-discovery/developers-docs/configuration-reference.md
  - materials-discovery/tests/test_llm_generate_schema.py
  - materials-discovery/tests/test_llm_runtime.py
autonomous: true
requirements: [LLM-02]

must_haves:
  truths:
    - "Phase 7 starts with additive contracts and runtime seams before wiring the llm-generate CLI."
    - "Mock coverage remains deterministic and offline, and the first real hosted provider is added without forcing a vendor SDK onto the base install."
    - "Configuration remains config-driven: provider selection lives in BackendConfig while generation behavior lives in a dedicated llm_generate config block."
    - "Run-level provenance is richer than candidate-level provenance, and the contract must support prompt/raw-output audit trails without bloating CandidateRecord."
    - "Every Phase 7 execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/common/schema.py"
      provides: "SystemConfig/BackendConfig extension points and llm-generate summary contracts"
      exports: ["LlmGenerateConfig", "LlmGenerateSummary", "BackendConfig.llm_adapter", "BackendConfig.llm_provider", "BackendConfig.llm_model", "BackendConfig.llm_api_base"]
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed request, attempt, result, and run-manifest models for llm-generate"
      exports: ["LlmGenerationRequest", "LlmGenerationAttempt", "LlmGenerationResult", "LlmRunManifest"]
    - path: "materials-discovery/src/materials_discovery/llm/runtime.py"
      provides: "Provider-neutral runtime seam plus mock and hosted adapters"
      exports: ["LlmAdapter", "resolve_llm_adapter", "MockLlmAdapter", "AnthropicApiLlmAdapter"]
    - path: "materials-discovery/tests/test_llm_generate_schema.py"
      provides: "Schema/config validation coverage for Phase 7 contracts"
    - path: "materials-discovery/tests/test_llm_runtime.py"
      provides: "Adapter-resolution and lazy-import runtime coverage"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/runtime.py"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "backend.llm_* fields and nested llm_generate config"
      pattern: "llm_adapter|llm_provider|llm_model|llm_generate"
    - from: "materials-discovery/src/materials_discovery/llm/runtime.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "typed request and attempt records"
      pattern: "LlmGenerationRequest|LlmGenerationAttempt"
    - from: "materials-discovery/developers-docs/configuration-reference.md"
      to: "materials-discovery/src/materials_discovery/common/schema.py"
      via: "documents the new llm_generate config seam and backend fields"
      pattern: "llm_generate|llm_adapter|llm_provider"
---

<objective>
Define the Phase 7 runtime contracts, config extensions, and provider seam for
`llm-generate`.

Purpose: Land the lowest-blast-radius foundation first so later work can build
prompting, retry behavior, candidate conversion, and benchmarking on typed
contracts instead of ad hoc dicts. This is where the phase locks the split
between provider selection, generation behavior, and run-level provenance.

Output: additive config/schema models in `common/schema.py` and `llm/schema.py`,
a provider-neutral runtime in `llm/runtime.py`, updated config docs, and focused
tests for schema and adapter behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-llm-inference-mvp/07-CONTEXT.md
@.planning/phases/07-llm-inference-mvp/07-RESEARCH.md
@.planning/phases/07-llm-inference-mvp/07-VALIDATION.md

@materials-discovery/src/materials_discovery/common/schema.py
@materials-discovery/src/materials_discovery/llm/__init__.py
@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/developers-docs/configuration-reference.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/pyproject.toml

<interfaces>
From common/schema.py:
```python
class BackendConfig(BaseModel): ...
class SystemConfig(BaseModel): ...
class CandidateRecord(BaseModel): ...
```

From llm/schema.py:
```python
class CorpusBuildConfig(BaseModel): ...
class CorpusBuildSummary(BaseModel): ...
```

From pyproject.toml:
```toml
[project.optional-dependencies]
ingestion = [
  "httpx>=0.27.0",
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive llm-generate config and summary contracts</name>
  <files>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/tests/test_llm_generate_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    .planning/phases/07-llm-inference-mvp/07-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: BackendConfig validates `llm_adapter`, `llm_provider`, `llm_model`, and optional `llm_api_base`
    - Test 2: SystemConfig accepts a nested `llm_generate` block with prompt template, temperature, max tokens, max attempts, seed Zomic path, raw-artifact root, and fixture outputs
    - Test 3: Mock-mode configs auto-default `backend.llm_adapter` to `llm_fixture_v1` and `backend.llm_provider` to `mock` when omitted
    - Test 4: Real-provider configs do not silently auto-default a hosted provider; they require explicit `llm_provider` and `llm_model`
    - Test 5: LlmGenerateSummary validates requested/generated counts, parse/compile pass counts, output path, calibration path, manifest path, and run manifest path
    - Test 6: LLM runtime models validate non-empty prompt text, attempt IDs, raw completion text, parse/compile status, and workspace-relative run artifact paths
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/common/schema.py` with these exact additive contracts:

- Add BackendConfig fields:
  - `llm_adapter: str | None = None`
  - `llm_provider: str | None = None`
  - `llm_model: str | None = None`
  - `llm_api_base: str | None = None`
- Keep current ingest/validation defaults untouched.
- Add mock-only defaults inside BackendConfig validation:
  - when `mode == "mock"` and `llm_adapter` is unset, set it to `"llm_fixture_v1"`
  - when `mode == "mock"` and `llm_provider` is unset, set it to `"mock"`
- Do **not** auto-default real hosted-provider fields. Real configs must set `llm_provider` and `llm_model` explicitly.

Add a new `LlmGenerateConfig` model with these concrete fields:
- `prompt_template: str = "zomic_generate_v1"`
- `temperature: float = 0.7`
- `max_tokens: int = 2048`
- `max_attempts: int = 3`
- `seed_zomic: str | None = None`
- `artifact_root: str | None = None`
- `persist_raw_completions: bool = True`
- `fixture_outputs: list[str] = Field(default_factory=list)`

Add `llm_generate: LlmGenerateConfig | None = None` to `SystemConfig`.

Add `LlmGenerateSummary` to `common/schema.py` with fields:
- `requested_count: int`
- `generated_count: int`
- `attempt_count: int`
- `parse_pass_count: int`
- `compile_pass_count: int`
- `output_path: str`
- `calibration_path: str | None = None`
- `manifest_path: str | None = None`
- `run_manifest_path: str | None = None`

Extend `materials-discovery/src/materials_discovery/llm/schema.py` with these concrete Phase 7 models:
- `LlmGenerationRequest`
  - `system: str`
  - `template_family: str`
  - `composition_bounds: dict[str, dict[str, float]]`
  - `prompt_text: str`
  - `temperature: float`
  - `max_tokens: int`
  - `seed_zomic_path: str | None`
- `LlmGenerationAttempt`
  - `attempt_id: str`
  - `adapter_key: str`
  - `provider: str`
  - `model: str`
  - `temperature: float`
  - `prompt_path: str`
  - `raw_completion_path: str`
  - `parse_status: str`
  - `compile_status: str`
  - `error_kind: str | None`
  - `error_message: str | None`
- `LlmGenerationResult`
  - `attempt_id: str`
  - `candidate_id: str | None`
  - `orbit_library_path: str | None`
  - `raw_export_path: str | None`
  - `parse_status: str`
  - `compile_status: str`
  - `passed: bool`
- `LlmRunManifest`
  - `run_id: str`
  - `system: str`
  - `adapter_key: str`
  - `provider: str`
  - `model: str`
  - `prompt_template: str`
  - `attempt_count: int`
  - `requested_count: int`
  - `generated_count: int`
  - `prompt_path: str`
  - `attempts_path: str`
  - `compile_results_path: str`
  - `created_at_utc: str`

Add validators that reject blank IDs, blank prompt text, blank raw completion paths, non-positive `max_tokens`, and `max_attempts < 1`.

Update `materials-discovery/developers-docs/configuration-reference.md` so it documents:
- the new BackendConfig `llm_*` fields
- the new nested `llm_generate:` block under `SystemConfig`
- the mock-only defaulting rule for `llm_fixture_v1`
- the requirement that real hosted-provider configs set `llm_provider` and `llm_model`

Create `materials-discovery/tests/test_llm_generate_schema.py` with focused tests for the behaviors above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/common/schema.py contains "class LlmGenerateConfig(BaseModel)"
    - materials-discovery/src/materials_discovery/common/schema.py contains "class LlmGenerateSummary(BaseModel)"
    - materials-discovery/src/materials_discovery/common/schema.py contains "llm_adapter"
    - materials-discovery/src/materials_discovery/common/schema.py contains "llm_provider"
    - materials-discovery/src/materials_discovery/common/schema.py contains "llm_model"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmGenerationRequest(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmGenerationAttempt(BaseModel)"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmRunManifest(BaseModel)"
    - materials-discovery/developers-docs/configuration-reference.md contains "llm_generate"
    - materials-discovery/tests/test_llm_generate_schema.py contains at least 5 test functions
    - The schema test command exits 0
  </acceptance_criteria>
  <done>The repo has typed config and runtime contracts for llm-generate, and the config docs match the new additive seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the provider-neutral runtime seam with mock and Anthropic adapters</name>
  <files>
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/tests/test_llm_runtime.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/common/schema.py,
    materials-discovery/src/materials_discovery/backends/registry.py,
    materials-discovery/pyproject.toml,
    .planning/phases/07-llm-inference-mvp/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: resolve_llm_adapter returns a deterministic mock adapter for `mock:llm_fixture_v1`
    - Test 2: the mock adapter emits configured fixture outputs in deterministic order
    - Test 3: resolve_llm_adapter returns an Anthropic hosted adapter for `real:anthropic_api_v1`
    - Test 4: the Anthropic adapter raises a clear error when `ANTHROPIC_API_KEY` is missing
    - Test 5: `httpx` is imported lazily inside the Anthropic adapter call path rather than at module import time
    - Test 6: the Anthropic adapter respects `backend.llm_api_base` when set
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/runtime.py` with these exact runtime pieces:

- protocol/interface `LlmAdapter` with a method:
  - `generate(request: LlmGenerationRequest) -> str`
- adapter registry keys:
  - `("mock", "llm_fixture_v1")`
  - `("real", "anthropic_api_v1")`
- resolver:
  - `resolve_llm_adapter(mode: str, adapter: str | None) -> LlmAdapter`

Implement `MockLlmAdapter`:
- source fixture outputs from `SystemConfig.llm_generate.fixture_outputs`
- emit outputs deterministically in list order
- raise `ValueError` with the exact message fragment `"fixture_outputs must not be empty"` when no outputs are configured for mock generation

Implement `AnthropicApiLlmAdapter`:
- read API key from `ANTHROPIC_API_KEY`
- POST to `{llm_api_base or "https://api.anthropic.com"}/v1/messages`
- use model from `backend.llm_model`
- do not use the vendor SDK; use `httpx` via a lazy import inside `generate(...)`
- extract the first returned text block into a plain string result
- raise a clear `RuntimeError` when the HTTP status is non-2xx or no text block is present

Update `materials-discovery/src/materials_discovery/llm/__init__.py` to export:
- `LlmAdapter`
- `resolve_llm_adapter`
- `MockLlmAdapter`
- `AnthropicApiLlmAdapter`

Create `materials-discovery/tests/test_llm_runtime.py` with monkeypatched tests for adapter resolution, fixture behavior, missing API key, lazy imports, and base URL override behavior.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_runtime.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "class MockLlmAdapter"
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "class AnthropicApiLlmAdapter"
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "def resolve_llm_adapter("
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "ANTHROPIC_API_KEY"
    - materials-discovery/src/materials_discovery/llm/runtime.py contains "httpx"
    - materials-discovery/src/materials_discovery/llm/__init__.py exports resolve_llm_adapter
    - materials-discovery/tests/test_llm_runtime.py contains at least 5 test functions
    - The runtime test command exits 0
  </acceptance_criteria>
  <done>The repo has a stable adapter seam with deterministic mock coverage and one real hosted provider path, ready for the llm-generate engine.</done>
</task>

</tasks>

## Verification

Verify this plan by checking:

1. `SystemConfig` and `BackendConfig` have additive LLM generation fields.
2. Runtime request/attempt/run-manifest models exist and validate.
3. The runtime resolver supports deterministic `mock` plus one real hosted
   provider without forcing a vendor SDK.
4. Focused schema and runtime tests cover the new contracts.

## Success Criteria

- [ ] Additive config and summary contracts exist for llm-generate.
- [ ] Run-level request/attempt/provenance models are typed and validated.
- [ ] `resolve_llm_adapter` supports `llm_fixture_v1` and `anthropic_api_v1`.
- [ ] Lazy optional imports keep mock-only installs viable.
- [ ] Focused Phase 7 schema and runtime tests pass.
