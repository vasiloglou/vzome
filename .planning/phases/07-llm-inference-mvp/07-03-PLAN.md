---
phase: 07-llm-inference-mvp
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - materials-discovery/src/materials_discovery/llm/benchmark.py
  - materials-discovery/scripts/run_llm_generate_benchmarks.sh
  - materials-discovery/README.md
  - materials-discovery/developers-docs/index.md
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/pyproject.toml
  - materials-discovery/tests/test_llm_generate_benchmarks.py
  - materials-discovery/tests/test_cli.py
autonomous: true
requirements: [LLM-02]

must_haves:
  truths:
    - "Phase 7 only needs an offline benchmark proving the inference path is useful before full training; full hi-fi comparison belongs to Phase 8."
    - "The benchmark must compare llm-generate to the deterministic generator on Al-Cu-Fe and Sc-Zn."
    - "Comparison metrics must include parse, compile, conversion, and screen pass-through rather than only file existence."
    - "The benchmark harness should stay thin and reuse existing mdisc commands instead of inventing a new orchestration framework."
    - "Every Phase 7 execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/benchmark.py"
      provides: "Comparison helper for deterministic vs llm generation lanes"
      exports: ["build_llm_generate_comparison", "write_llm_generate_comparison"]
    - path: "materials-discovery/scripts/run_llm_generate_benchmarks.sh"
      provides: "Operator-facing benchmark wrapper for Phase 7"
    - path: "materials-discovery/tests/test_llm_generate_benchmarks.py"
      provides: "Offline two-system benchmark proof"
    - path: "materials-discovery/developers-docs/pipeline-stages.md"
      provides: "Updated llm-generate stage contract once implemented"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/benchmark.py"
      to: "materials-discovery/src/materials_discovery/common/stage_metrics.py"
      via: "consumes llm-generate and deterministic generation metric summaries"
      pattern: "parse_pass_rate|compile_pass_rate|generation_success_rate"
    - from: "materials-discovery/scripts/run_llm_generate_benchmarks.sh"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "reuses mdisc generate, mdisc llm-generate, and mdisc screen"
      pattern: "llm-generate|generate|screen"
    - from: "materials-discovery/tests/test_llm_generate_benchmarks.py"
      to: "materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml"
      via: "fixture-backed Al-Cu-Fe benchmark lane"
      pattern: "al_cu_fe_llm_mock"
    - from: "materials-discovery/tests/test_llm_generate_benchmarks.py"
      to: "materials-discovery/configs/systems/sc_zn_llm_mock.yaml"
      via: "fixture-backed Sc-Zn benchmark lane"
      pattern: "sc_zn_llm_mock"
---

<objective>
Ship the offline benchmark proof and operator-facing benchmark path for Phase 7.

Purpose: Prove that `llm-generate` is not just wired into the CLI, but can be
compared against the deterministic generator on the two required systems using
meaningful metrics and reproducible offline fixtures.

Output: benchmark comparison helper, benchmark runner script, docs updates, and
offline regression coverage for `Al-Cu-Fe` and `Sc-Zn`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-llm-inference-mvp/07-CONTEXT.md
@.planning/phases/07-llm-inference-mvp/07-RESEARCH.md
@.planning/phases/07-llm-inference-mvp/07-VALIDATION.md
@.planning/phases/07-llm-inference-mvp/07-02-PLAN.md

@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/common/stage_metrics.py
@materials-discovery/developers-docs/pipeline-stages.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/README.md
@materials-discovery/pyproject.toml

<interfaces>
From common/stage_metrics.py:
```python
def generation_metrics(...) -> dict[str, float | int | bool]: ...
def llm_generation_metrics(...) -> dict[str, float | int | bool]: ...
```

From cli.py:
```python
@app.command("generate")
@app.command("screen")
@app.command("llm-generate")
```

From the committed Phase 7 configs:
```yaml
backend:
  mode: mock
  llm_adapter: llm_fixture_v1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the comparison helper and benchmark runner script</name>
  <files>
    materials-discovery/src/materials_discovery/llm/benchmark.py,
    materials-discovery/scripts/run_llm_generate_benchmarks.sh
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/common/stage_metrics.py,
    materials-discovery/src/materials_discovery/common/io.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/scripts/run_reference_aware_benchmarks.sh,
    .planning/phases/07-llm-inference-mvp/07-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the comparison helper produces a JSON object that includes deterministic-lane and llm-lane metric blocks
    - Test 2: the comparison helper emits the required metric keys for parse, compile, conversion, and screen pass-through
    - Test 3: the benchmark runner script is config-driven and can target `Al-Cu-Fe`, `Sc-Zn`, or both
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/benchmark.py` with:
- `build_llm_generate_comparison(system: str, deterministic_generation: dict[str, Any], deterministic_screen: dict[str, Any], llm_generation: dict[str, Any], llm_screen: dict[str, Any]) -> dict[str, Any]`
- `write_llm_generate_comparison(payload: dict[str, Any], path: Path) -> Path`

The comparison payload must include these exact top-level keys:
- `system`
- `deterministic_generation`
- `deterministic_screen`
- `llm_generation`
- `llm_screen`
- `comparison`

The nested `comparison` block must include:
- `parse_pass_rate_delta`
- `compile_pass_rate_delta`
- `conversion_rate_delta`
- `screen_pass_rate_delta`

Create `materials-discovery/scripts/run_llm_generate_benchmarks.sh` as a thin wrapper around existing commands. It must:
- accept `--systems` with values `al_cu_fe`, `sc_zn`, or `all`
- accept `--count` with a numeric override
- run `mdisc generate`, `mdisc llm-generate`, and `mdisc screen` for each selected system/config pair
- write comparison JSON under `data/benchmarks/llm_generate/{system}_comparison.json`
- avoid inventing a new orchestration framework beyond shelling the committed `mdisc` commands
  </action>
  <verify>
    <automated>bash -n /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery/scripts/run_llm_generate_benchmarks.sh</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/benchmark.py contains "def build_llm_generate_comparison("
    - materials-discovery/src/materials_discovery/llm/benchmark.py contains "parse_pass_rate_delta"
    - materials-discovery/src/materials_discovery/llm/benchmark.py contains "screen_pass_rate_delta"
    - materials-discovery/scripts/run_llm_generate_benchmarks.sh exists
    - materials-discovery/scripts/run_llm_generate_benchmarks.sh contains "llm-generate"
    - materials-discovery/scripts/run_llm_generate_benchmarks.sh contains "--systems"
    - The benchmark runner passes `bash -n`
  </acceptance_criteria>
  <done>The repo has a thin operator-facing path to compare deterministic and LLM generation lanes offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add two-system offline benchmark regression coverage and docs</name>
  <files>
    materials-discovery/README.md,
    materials-discovery/developers-docs/index.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/pyproject.toml,
    materials-discovery/tests/test_llm_generate_benchmarks.py,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/README.md,
    materials-discovery/developers-docs/index.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/pyproject.toml,
    materials-discovery/tests/test_cli.py,
    materials-discovery/configs/systems/al_cu_fe_llm_mock.yaml,
    materials-discovery/configs/systems/sc_zn_llm_mock.yaml
  </read_first>
  <behavior>
    - Test 1: `test_llm_generate_benchmarks.py` runs offline comparison coverage for both `Al-Cu-Fe` and `Sc-Zn`
    - Test 2: the benchmark tests assert on parse, compile, conversion, and screen pass-through metrics rather than only file creation
    - Test 3: the slower benchmark lane is isolated behind a dedicated pytest marker
    - Test 4: docs describe the new llm-generate command, its config shape, first real-provider setup, and the benchmark runner
  </behavior>
  <action>
Create `materials-discovery/tests/test_llm_generate_benchmarks.py` with fixture-backed offline tests that:
- run deterministic `generate` + `screen` and LLM `llm-generate` + `screen` for `Al-Cu-Fe`
- run deterministic `generate` + `screen` and LLM `llm-generate` + `screen` for `Sc-Zn`
- assert that both systems produce a comparison payload with:
  - `parse_pass_rate_delta`
  - `compile_pass_rate_delta`
  - `conversion_rate_delta`
  - `screen_pass_rate_delta`
- avoid live network access by using mock configs and monkeypatched runtime calls

Update `materials-discovery/pyproject.toml` to add a new pytest marker:
- `llm_lane: Phase 7 llm-generate benchmark tests (slower lane)`

Update docs:
- `materials-discovery/README.md` with one short `llm-generate` example and a pointer to the benchmark runner
- `materials-discovery/developers-docs/index.md` with a row for the implemented `llm-generate` path
- `materials-discovery/developers-docs/llm-integration.md` to reflect that Phase 7 implements the first inference path, while `llm-evaluate` and fine-tuning remain future work
- `materials-discovery/developers-docs/pipeline-stages.md` so the `llm-generate` section matches the implemented summary/calibration/manifest behavior

Extend `materials-discovery/tests/test_cli.py` as needed so benchmark-related CLI helpers or script assumptions stay covered.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_generate_benchmarks.py tests/test_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/tests/test_llm_generate_benchmarks.py contains tests for both "Al-Cu-Fe" and "Sc-Zn"
    - materials-discovery/tests/test_llm_generate_benchmarks.py contains "parse_pass_rate_delta"
    - materials-discovery/tests/test_llm_generate_benchmarks.py contains "screen_pass_rate_delta"
    - materials-discovery/pyproject.toml contains "llm_lane"
    - materials-discovery/README.md contains "llm-generate"
    - materials-discovery/developers-docs/pipeline-stages.md contains "Stage manifest" in the llm-generate section
    - The benchmark pytest command exits 0
  </acceptance_criteria>
  <done>Phase 7 ends with an offline two-system benchmark proof and docs that tell operators how to run it.</done>
</task>

</tasks>

## Verification

Verify this plan by checking:

1. The repo has a benchmark helper and thin benchmark runner script.
2. Both required systems have offline comparison coverage.
3. The benchmark asserts on real metrics, not just files.
4. Docs reflect the implemented `llm-generate` path and its benchmark runner.

## Success Criteria

- [ ] An operator-facing benchmark runner exists for Phase 7.
- [ ] `Al-Cu-Fe` and `Sc-Zn` both have offline LLM-vs-deterministic comparison coverage.
- [ ] Comparison payloads include parse, compile, conversion, and screen pass-through deltas.
- [ ] A dedicated slower benchmark marker exists.
- [ ] Benchmark tests and docs updates pass focused verification.
