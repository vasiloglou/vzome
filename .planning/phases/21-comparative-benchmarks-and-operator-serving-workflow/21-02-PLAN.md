---
phase: 21-comparative-benchmarks-and-operator-serving-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - materials-discovery/src/materials_discovery/cli.py
  - materials-discovery/src/materials_discovery/llm/serving_benchmark.py
  - materials-discovery/src/materials_discovery/llm/launch.py
  - materials-discovery/tests/test_llm_serving_benchmark_cli.py
  - materials-discovery/tests/test_real_mode_pipeline.py
autonomous: true
requirements: [LLM-17, OPS-10]

must_haves:
  truths:
    - "Phase 21 must end with a dedicated operator benchmark command rather than a docs-only recipe."
    - "The benchmark command must run explicit smoke checks first and must not silently continue into a full benchmark when a strict target is unhealthy."
    - "Launch-style targets must reuse the existing campaign launch flow and artifact contracts rather than bypassing them."
    - "Evaluation-style targets must reuse the shipped llm-evaluate core and keep the specialized lane honest as evaluation-primary."
    - "The real proof should make hosted, local, and specialized targets visible under one benchmark summary without pretending they all play the same workflow role."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/cli.py"
      provides: "Operator-facing benchmark entrypoint and smoke-only mode"
    - path: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      provides: "Execution orchestration for launch-role and evaluate-role targets"
      exports:
        [
          "execute_serving_benchmark",
        ]
    - path: "materials-discovery/tests/test_llm_serving_benchmark_cli.py"
      provides: "CLI coverage for smoke-only, strict failure, and benchmark-summary output"
    - path: "materials-discovery/tests/test_real_mode_pipeline.py"
      provides: "Offline end-to-end real proof for one benchmark spec covering hosted, local, and specialized targets"
    - path: "materials-discovery/src/materials_discovery/llm/launch.py"
      provides: "Optional benchmark-safe launch override path without mutating source campaign specs"
  key_links:
    - from: "materials-discovery/src/materials_discovery/cli.py"
      to: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      via: "the operator-facing benchmark command should be a thin wrapper over the typed benchmark core"
      pattern: "llm_serving_benchmark_command|execute_serving_benchmark"
    - from: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/launch.py"
      via: "campaign-launch targets must be able to compare different generation lanes without mutating the source campaign spec on disk"
      pattern: "resolve_campaign_launch|requested_model_lane"
    - from: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/evaluate.py"
      via: "evaluation-role targets must reuse llm-evaluate instead of inventing a specialist-only benchmark path"
      pattern: "evaluate_llm_candidates"
---

<objective>
Build the dedicated operator benchmark entrypoint and prove it on one real
shared-context target matrix spanning hosted, local, and specialized serving.

Purpose: satisfy the core of Phase 21 by making comparative serving benchmarks
actually runnable, smoke-testable, and auditable from one command.

Output: `mdisc llm-serving-benchmark`, strict smoke-only mode, benchmark
execution over launch/evaluate subflows, and one offline real proof in tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-CONTEXT.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-VALIDATION.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-01-PLAN.md

@materials-discovery/src/materials_discovery/cli.py
@materials-discovery/src/materials_discovery/llm/serving_benchmark.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/src/materials_discovery/llm/evaluate.py
@materials-discovery/src/materials_discovery/llm/compare.py
@materials-discovery/tests/test_real_mode_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the operator benchmark command with strict smoke-only and failure behavior</name>
  <files>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/tests/test_llm_serving_benchmark_cli.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/tests/test_cli.py,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `mdisc llm-serving-benchmark --spec PATH --smoke-only` writes the typed smoke artifact and exits without executing targets
    - Test 2: a benchmark with a failed strict smoke check exits nonzero and reports the failing target before any target execution begins
    - Test 3: successful benchmarks print concise recommendation lines and the final benchmark summary path
    - Test 4: the command keeps benchmark artifacts under `data/benchmarks/llm_serving/{benchmark_id}/...`
    - Test 5: the CLI remains compatible with `_emit_error(...)` and existing JSON-oriented command conventions
  </behavior>
  <action>
Add a new Typer command in `materials-discovery/src/materials_discovery/cli.py`:
- `@app.command("llm-serving-benchmark")`

The command must accept:
- `--spec PATH` (required)
- `--smoke-only` (bool, default false)
- `--out PATH` (optional override for the final benchmark summary JSON)

Implement an orchestration entrypoint in
`materials-discovery/src/materials_discovery/llm/serving_benchmark.py`:
- `execute_serving_benchmark(spec_path: Path, *, smoke_only: bool = False, out_path: Path | None = None) -> LlmServingBenchmarkSummary`

Required behavior:
- load the typed benchmark spec
- run explicit smoke checks for every target first
- always write the smoke artifact before deciding whether to continue
- if `--smoke-only` is true, print per-target smoke lines plus the smoke artifact path and stop
- if any strict target smoke fails, emit an operator-facing error and exit with status code 2
- if the full benchmark proceeds, write the final benchmark summary JSON and print concise recommendation lines plus the benchmark summary path

CLI output should include:
- one line per smoke target in smoke-only mode
- recommendation lines from `LlmServingBenchmarkSummary.recommendation_lines`
- a final `Benchmark summary: ...` path line on success

Create `materials-discovery/tests/test_llm_serving_benchmark_cli.py` to cover:
- smoke-only success
- strict smoke failure
- successful benchmark-summary output
- output-path override behavior
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/cli.py contains `@app.command("llm-serving-benchmark")`
    - materials-discovery/src/materials_discovery/llm/serving_benchmark.py contains `def execute_serving_benchmark`
    - materials-discovery/src/materials_discovery/llm/serving_benchmark.py contains `smoke_only`
    - materials-discovery/tests/test_llm_serving_benchmark_cli.py covers smoke-only and strict failure behavior
    - the benchmark CLI regression command exits 0
  </acceptance_criteria>
  <done>The repo has a dedicated operator benchmark command with explicit smoke-only and strict failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse launch/evaluate subflows for one real offline benchmark proof across hosted, local, and specialized targets</name>
  <files>
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/tests/test_llm_serving_benchmark_cli.py,
    materials-discovery/tests/test_real_mode_pipeline.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/llm/replay.py,
    materials-discovery/tests/test_real_mode_pipeline.py,
    materials-discovery/tests/test_llm_replay_core.py,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a `campaign_launch` benchmark target can override the generation lane for comparison purposes without mutating the source campaign spec on disk
    - Test 2: launch-style targets write standard launch/comparison artifacts and record their paths inside the benchmark result
    - Test 3: evaluate-style targets reuse `evaluate_llm_candidates(...)`, keep their `batch` aligned with the shared acceptance-pack context, and write their evaluation summary path into the benchmark result
    - Test 4: misaligned evaluation batches fail clearly before benchmark execution rather than quietly comparing unrelated data
    - Test 5: one offline benchmark proof can compare a hosted launch target, a local launch target, and a specialized evaluation target under one shared acceptance-pack context
    - Test 6: benchmark summaries keep missing or role-specific quality fields explicit instead of fabricating parity between generation and evaluation roles
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/launch.py` additively with a
benchmark-only override seam.

Add an optional `requested_model_lane_override: str | None = None` parameter to
`resolve_campaign_launch(...)`.

Rules:
- existing `llm-launch` behavior stays unchanged when the override is omitted
- when provided, the override takes precedence over action-derived lane
  preferences for benchmark launch targets only
- the source campaign spec file on disk must remain unchanged

Update `materials-discovery/src/materials_discovery/llm/serving_benchmark.py`
to execute mixed benchmark targets honestly:

For `campaign_launch` targets:
- load the target system config
- resolve the benchmark launch using the optional generation-lane override
- run the existing generation path
- build the existing launch summary and comparison artifacts
- record `launch_summary_path`, `comparison_path`, and measured execution latency

For `llm_evaluate` targets:
- load the target system config
- verify the target `batch` or candidate source is aligned with the same shared acceptance-pack context used by the benchmark; fail clearly if the requested evaluation slice is unrelated
- call `evaluate_llm_candidates(...)` with `requested_model_lane=target.evaluation_model_lane`
- record `evaluate_summary_path`, measured execution latency, and any available
  quality metrics such as `assessment_success_rate`

For the benchmark summary:
- keep role-specific quality metrics explicit
- do not coerce missing launch metrics onto evaluation targets or missing
  evaluation metrics onto launch targets
- include at least the benchmark target label, role, latency, estimated cost,
  operator friction tier, and whatever quality metrics were actually observed

Extend `materials-discovery/tests/test_real_mode_pipeline.py` with one offline
real proof using a temporary benchmark spec that includes:
- one hosted `campaign_launch` target
- one local `campaign_launch` target
- one `llm_evaluate` target using `specialized_materials`

Keep the proof fully offline via monkeypatched provider calls and temporary
acceptance-pack/campaign-spec fixtures.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_cli.py tests/test_real_mode_pipeline.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/launch.py contains `requested_model_lane_override`
    - benchmark results record `launch_summary_path` for launch targets
    - benchmark results record `evaluate_summary_path` for evaluate targets
    - evaluation-role benchmark execution fails clearly for a batch that is not aligned to the shared benchmark context
    - materials-discovery/tests/test_real_mode_pipeline.py covers one offline benchmark run with hosted, local, and specialized targets
    - the integration regression command exits 0
  </acceptance_criteria>
  <done>Phase 21 now has a real comparative benchmark proof that exercises the honest hosted/local/specialized workflow mix.</done>
</task>

</tasks>

<verification>
  <check>The operator benchmark command can smoke-test and execute shared-context targets without hand-assembled artifacts.</check>
  <check>Launch-style and evaluation-style targets both reuse shipped workflow seams instead of introducing a second runtime.</check>
  <check>One real proof demonstrates hosted, local, and specialized benchmark visibility under one summary surface.</check>
</verification>
