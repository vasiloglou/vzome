---
phase: 21-comparative-benchmarks-and-operator-serving-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - materials-discovery/src/materials_discovery/llm/schema.py
  - materials-discovery/src/materials_discovery/llm/storage.py
  - materials-discovery/src/materials_discovery/llm/serving_benchmark.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_serving_benchmark_schema.py
  - materials-discovery/tests/test_llm_serving_benchmark_core.py
autonomous: true
requirements: [LLM-17, OPS-10]

must_haves:
  truths:
    - "Phase 21 must compare serving targets under one shared benchmark context rather than lane-specific ad hoc inputs."
    - "Smoke checks must be explicit, typed, and visible to operators before benchmark execution starts."
    - "The benchmark contract must preserve the honest role of the specialized lane instead of assuming it is already a Zomic-native generator."
    - "Benchmark artifacts must stay additive to the existing workflow and point to existing launch/evaluate/compare artifacts instead of creating a parallel artifact family."
    - "No target may silently downgrade to another lane during benchmarking unless the benchmark spec explicitly allows that fallback."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/schema.py"
      provides: "Typed benchmark spec, smoke artifact, and summary result contract"
      exports:
        [
          "LlmServingBenchmarkSpec",
          "LlmServingBenchmarkTarget",
          "LlmServingSmokeCheck",
          "LlmServingBenchmarkSummary",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/storage.py"
      provides: "Stable benchmark artifact roots under data/benchmarks/llm_serving"
      exports:
        [
          "llm_serving_benchmark_dir",
          "llm_serving_benchmark_smoke_path",
          "llm_serving_benchmark_summary_path",
        ]
    - path: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      provides: "Core smoke-check and benchmark-summary helpers"
      exports:
        [
          "load_serving_benchmark_spec",
          "run_serving_smoke_check",
          "build_serving_benchmark_summary",
        ]
    - path: "materials-discovery/tests/test_llm_serving_benchmark_schema.py"
      provides: "Focused schema coverage for the new benchmark contract"
    - path: "materials-discovery/tests/test_llm_serving_benchmark_core.py"
      provides: "Offline core coverage for smoke checks and recommendation summaries"
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/runtime.py"
      via: "smoke checks must reuse existing adapter readiness validation rather than inventing a second transport probe"
      pattern: "validate_llm_adapter_ready|resolve_llm_adapter"
    - from: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/launch.py"
      via: "benchmark launch-style targets must reuse the existing lane resolver and serving identity conventions"
      pattern: "resolve_serving_lane|build_serving_identity"
    - from: "materials-discovery/src/materials_discovery/llm/serving_benchmark.py"
      to: "materials-discovery/src/materials_discovery/llm/compare.py"
      via: "typed benchmark summaries should carry refs and metrics that remain compatible with existing compare artifacts"
      pattern: "build_campaign_outcome_snapshot|build_campaign_comparison"
---

<objective>
Define the typed serving-benchmark contract and its explicit smoke-test rules
before the operator-facing CLI and docs widen the regression surface.

Purpose: give Phase 21 one stable, file-backed benchmark spec and result model
that can compare hosted, local, and specialized targets honestly under one
shared context.

Output: benchmark spec models, smoke/result storage helpers, and a core
benchmark module with offline schema/core regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-CONTEXT.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-VALIDATION.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/src/materials_discovery/llm/launch.py
@materials-discovery/src/materials_discovery/llm/evaluate.py
@materials-discovery/src/materials_discovery/llm/compare.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed benchmark spec and artifact models for shared-context serving comparisons</name>
  <files>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_serving_benchmark_schema.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/tests/test_llm_compare_cli.py,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a benchmark spec requires a nonblank `benchmark_id`, a nonblank shared `acceptance_pack_path`, and at least two unique targets
    - Test 2: each target validates `workflow_role` as `campaign_launch` or `llm_evaluate`
    - Test 3: `campaign_launch` targets require `campaign_spec_path`, while `llm_evaluate` targets require `batch`
    - Test 4: target metadata records `estimated_cost_usd >= 0`, `operator_friction_tier` as `low|medium|high`, and `allow_fallback` as an explicit boolean defaulting to false
    - Test 5: smoke artifacts and target results can carry requested/resolved lane data plus nested `serving_identity`
    - Test 6: benchmark storage helpers write under `data/benchmarks/llm_serving/{benchmark_id}/...`
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/schema.py` additively with a
typed serving-benchmark contract.

Add these concrete models:
- `LlmServingBenchmarkTarget`
- `LlmServingSmokeCheck`
- `LlmServingBenchmarkTargetResult`
- `LlmServingBenchmarkSummary`
- `LlmServingBenchmarkSpec`

`LlmServingBenchmarkTarget` must include:
- `target_id: str`
- `label: str`
- `workflow_role: Literal["campaign_launch", "llm_evaluate"]`
- `system_config_path: str`
- `campaign_spec_path: str | None = None`
- `batch: str | None = None`
- `generation_model_lane: Literal["general_purpose", "specialized_materials"] | None = None`
- `evaluation_model_lane: Literal["general_purpose", "specialized_materials"] | None = None`
- `estimated_cost_usd: float`
- `operator_friction_tier: Literal["low", "medium", "high"]`
- `allow_fallback: bool = False`
- `notes: str | None = None`

Validation rules:
- `campaign_launch` targets require `campaign_spec_path` and may omit `batch`
- `llm_evaluate` targets require `batch`
- at least one of `generation_model_lane` or `evaluation_model_lane` must be set
- string fields must be normalized and nonblank where required
- `estimated_cost_usd` must be `>= 0`

`LlmServingSmokeCheck` must carry:
- `target_id`
- `role: Literal["generation", "evaluation"]`
- `status: Literal["passed", "failed"]`
- `requested_model_lane`
- `resolved_model_lane`
- `resolved_model_lane_source`
- `serving_identity: LlmServingIdentity | None = None`
- `latency_s: float`
- `detail: str | None = None`

`LlmServingBenchmarkTargetResult` must carry:
- `target_id`
- `label`
- `workflow_role`
- `estimated_cost_usd`
- `operator_friction_tier`
- `smoke_checks: list[LlmServingSmokeCheck]`
- `quality_metrics: dict[str, float | bool | None]`
- `execution_latency_s: float | None = None`
- `launch_summary_path: str | None = None`
- `comparison_path: str | None = None`
- `evaluate_summary_path: str | None = None`
- `summary_lines: list[str] = Field(default_factory=list)`

`LlmServingBenchmarkSummary` must carry:
- `benchmark_id`
- `acceptance_pack_path`
- `generated_at_utc`
- `targets: list[LlmServingBenchmarkTargetResult]`
- `recommendation_lines: list[str] = Field(default_factory=list)`
- `failed_targets: list[str] = Field(default_factory=list)`

`LlmServingBenchmarkSpec` must carry:
- `benchmark_id`
- `acceptance_pack_path`
- `targets: list[LlmServingBenchmarkTarget]`

Extend `materials-discovery/src/materials_discovery/llm/storage.py` additively with:
- `llm_serving_benchmark_dir(benchmark_id, root=None)`
- `llm_serving_benchmark_smoke_path(benchmark_id, root=None)`
- `llm_serving_benchmark_summary_path(benchmark_id, root=None)`

Artifact paths must live under:
- `data/benchmarks/llm_serving/{benchmark_id}/smoke_checks.json`
- `data/benchmarks/llm_serving/{benchmark_id}/benchmark_summary.json`

Update `materials-discovery/src/materials_discovery/llm/__init__.py` exports for the
new schema/storage symbols.

Create `materials-discovery/tests/test_llm_serving_benchmark_schema.py` to cover
the model validation and storage-path behavior above.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_schema.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmServingBenchmarkSpec"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "class LlmServingSmokeCheck"
    - materials-discovery/src/materials_discovery/llm/schema.py contains "estimated_cost_usd"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "llm_serving_benchmark_dir"
    - materials-discovery/src/materials_discovery/llm/storage.py contains "data/benchmarks/llm_serving"
    - materials-discovery/tests/test_llm_serving_benchmark_schema.py covers both target-role validation paths
    - the schema regression command exits 0
  </acceptance_criteria>
  <done>The repo has a stable typed contract for serving benchmarks and smoke artifacts under one shared context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the core smoke-check and benchmark-summary helpers using existing serving seams</name>
  <files>
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_serving_benchmark_core.py
  </files>
  <read_first>
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/llm/launch.py,
    materials-discovery/src/materials_discovery/llm/evaluate.py,
    materials-discovery/src/materials_discovery/llm/compare.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/tests/test_llm_runtime.py,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: launch-role smoke checks resolve the requested generation lane through the shared lane resolver and run `validate_llm_adapter_ready(...)`
    - Test 2: evaluate-role smoke checks use the requested evaluation lane and the same readiness helper before provider execution
    - Test 3: if a target resolves through fallback while `allow_fallback` is false, the smoke check fails and records the resolved source explicitly
    - Test 4: benchmark summary helpers preserve per-target role, latency, cost, and operator-friction metadata
    - Test 5: benchmark recommendation lines can identify fastest, cheapest, and lowest-friction targets while leaving missing quality keys explicit rather than inventing zeros
    - Test 6: all smoke/core tests remain offline through monkeypatched readiness calls and synthetic result payloads
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/serving_benchmark.py`.

Add these concrete functions:
- `load_serving_benchmark_spec(path: Path) -> LlmServingBenchmarkSpec`
- `run_serving_smoke_check(target: LlmServingBenchmarkTarget, *, root: Path | None = None) -> list[LlmServingSmokeCheck]`
- `build_serving_benchmark_summary(spec: LlmServingBenchmarkSpec, target_results: list[LlmServingBenchmarkTargetResult]) -> LlmServingBenchmarkSummary`

`run_serving_smoke_check(...)` must:
- load the target `SystemConfig`
- for `campaign_launch` targets, resolve `generation_model_lane` through `resolve_serving_lane(...)`
- for `llm_evaluate` targets, resolve `evaluation_model_lane` using the same lane authority and backend config
- build a typed `LlmServingIdentity` using the shared serving-identity logic where possible
- measure probe latency with `time.perf_counter()`
- call `validate_llm_adapter_ready(...)`
- return one smoke check per exercised role (`generation` and/or `evaluation`)
- mark the smoke check `failed` if the resolved lane differs from the requested lane and `allow_fallback` is false
- include failure detail instead of raising away the operator-facing context

`build_serving_benchmark_summary(...)` must:
- preserve the per-target `quality_metrics`, `execution_latency_s`, `estimated_cost_usd`, and `operator_friction_tier`
- produce recommendation lines for fastest target, cheapest target, and lowest-friction target
- prefer concrete quality metrics when present and leave missing metrics explicit
- carry `failed_targets` rather than hiding them

Update `materials-discovery/src/materials_discovery/llm/__init__.py` exports for the
new benchmark helper functions.

Create `materials-discovery/tests/test_llm_serving_benchmark_core.py` with fully
offline monkeypatched coverage for:
- launch-role smoke checks
- evaluate-role smoke checks
- explicit fallback failure when `allow_fallback` is false
- summary-line generation from mixed target results
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_core.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/src/materials_discovery/llm/serving_benchmark.py contains "def run_serving_smoke_check"
    - materials-discovery/src/materials_discovery/llm/serving_benchmark.py contains "validate_llm_adapter_ready"
    - materials-discovery/src/materials_discovery/llm/serving_benchmark.py contains "allow_fallback"
    - materials-discovery/src/materials_discovery/llm/serving_benchmark.py contains "recommendation_lines"
    - materials-discovery/tests/test_llm_serving_benchmark_core.py covers both launch-role and evaluate-role smoke checks
    - the core regression command exits 0
  </acceptance_criteria>
  <done>Phase 21 now has an offline-testable benchmark core and explicit smoke-test behavior built on the shipped serving seams.</done>
</task>

</tasks>

<verification>
  <check>The benchmark contract is file-backed, typed, and anchored to one shared acceptance-pack context.</check>
  <check>Smoke checks are explicit and reuse the existing serving-lane readiness rules rather than inventing a second transport contract.</check>
  <check>Phase 21 can now build a CLI and runbook on one stable benchmark core instead of ad hoc scripts.</check>
</verification>

