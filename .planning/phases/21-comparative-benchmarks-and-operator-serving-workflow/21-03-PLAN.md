---
phase: 21-comparative-benchmarks-and-operator-serving-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - materials-discovery/configs/systems/al_cu_fe_llm_hosted.yaml
  - materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml
  - materials-discovery/configs/llm/sc_zn_serving_benchmark.yaml
  - materials-discovery/RUNBOOK.md
  - materials-discovery/developers-docs/configuration-reference.md
  - materials-discovery/developers-docs/llm-integration.md
  - materials-discovery/developers-docs/pipeline-stages.md
  - materials-discovery/tests/test_cli.py
  - materials-discovery/tests/test_real_mode_pipeline.py
autonomous: true
requirements: [LLM-17, OPS-10]

must_haves:
  truths:
    - "Phase 21 must leave behind a runnable operator recipe, not just hidden benchmark plumbing."
    - "Committed example specs must demonstrate hosted, local, and specialized targets without requiring live endpoints in CI."
    - "The runbook must describe smoke-only mode, strict failure behavior, and when to prefer hosted, local, or specialized targets."
    - "Docs should explain the specialized target honestly as evaluation-primary within the comparative benchmark workflow."
    - "Wave 3 should keep the benchmark workflow additive to the existing closed-loop CLI family."
    - "Every execution task that changes materials-discovery/ must update materials-discovery/Progress.md per AGENTS.md."
  artifacts:
    - path: "materials-discovery/configs/systems/al_cu_fe_llm_hosted.yaml"
      provides: "Hosted benchmark example config for the real Phase 21 comparison surface"
    - path: "materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml"
      provides: "Committed real benchmark spec showing hosted, local, and specialized targets on one system"
    - path: "materials-discovery/configs/llm/sc_zn_serving_benchmark.yaml"
      provides: "Thin second-system compatibility benchmark spec"
    - path: "materials-discovery/RUNBOOK.md"
      provides: "Operator-facing smoke and benchmark workflow guidance"
    - path: "materials-discovery/developers-docs/pipeline-stages.md"
      provides: "Formal command and artifact documentation for the benchmark workflow"
    - path: "materials-discovery/tests/test_cli.py"
      provides: "CLI/help-surface regression coverage after the new benchmark command and docs land"
  key_links:
    - from: "materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml"
      to: "materials-discovery/src/materials_discovery/cli.py"
      via: "the committed example spec must be executable by the shipped benchmark command without extra hidden fields"
      pattern: "llm-serving-benchmark|LlmServingBenchmarkSpec"
    - from: "materials-discovery/RUNBOOK.md"
      to: "materials-discovery/developers-docs/pipeline-stages.md"
      via: "operator workflow guidance and formal command docs must describe the same smoke-only, failure, and comparison behavior"
      pattern: "llm-serving-benchmark|--smoke-only"
---

<objective>
Finish Phase 21 with committed example specs and the runbook/docs that make the
serving benchmark workflow usable by operators.

Purpose: turn the benchmark command into a documented serving workflow with
clear setup, smoke testing, fallback expectations, and recommendation guidance.

Output: example hosted/benchmark configs, additive docs, and final CLI/docs
regressions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-CONTEXT.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-VALIDATION.md
@.planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-02-PLAN.md

@materials-discovery/RUNBOOK.md
@materials-discovery/developers-docs/configuration-reference.md
@materials-discovery/developers-docs/llm-integration.md
@materials-discovery/developers-docs/pipeline-stages.md
@materials-discovery/configs/systems/al_cu_fe_llm_local.yaml
@materials-discovery/configs/systems/sc_zn_llm_local.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add committed hosted and benchmark example configs for the operator workflow</name>
  <files>
    materials-discovery/configs/systems/al_cu_fe_llm_hosted.yaml,
    materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml,
    materials-discovery/configs/llm/sc_zn_serving_benchmark.yaml,
    materials-discovery/tests/test_real_mode_pipeline.py
  </files>
  <read_first>
    materials-discovery/configs/systems/al_cu_fe_llm_local.yaml,
    materials-discovery/configs/systems/sc_zn_llm_local.yaml,
    materials-discovery/tests/test_real_mode_pipeline.py,
    materials-discovery/src/materials_discovery/cli.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the committed hosted example config validates as a `SystemConfig` and keeps placeholder-safe operator values
    - Test 2: the committed Al-Cu-Fe benchmark spec includes hosted, local, and specialized targets under one shared acceptance-pack context
    - Test 3: the committed specialized benchmark target uses a batch or candidate source that is explicitly aligned with the same shared benchmark context rather than a generic unrelated evaluation slice
    - Test 4: the committed Sc-Zn benchmark spec provides a thinner compatibility example rather than a second full benchmark proof
    - Test 5: real-mode pipeline regression stays green with the example-spec surface available in the repo
  </behavior>
  <action>
Add `materials-discovery/configs/systems/al_cu_fe_llm_hosted.yaml`.

Use the same system/composition/template settings as the local `Al-Cu-Fe`
config, but define a hosted baseline with these placeholder-safe serving values:
- `backend.mode: real`
- `backend.llm_adapter: anthropic_api_v1`
- `backend.llm_provider: anthropic`
- `backend.llm_model: hosted-general-placeholder`

Keep `llm_generate` concrete with:
- `prompt_template: zomic_generate_v1`
- `temperature: 0.2`
- `max_tokens: 1024`
- `max_attempts: 2`

Add `materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml` with:
- `benchmark_id: al_cu_fe_serving_v1`
- `acceptance_pack_path: data/benchmarks/llm_acceptance/pack_v1/acceptance_pack.json`
- three targets:
  - `hosted_generation` using `materials-discovery/configs/systems/al_cu_fe_llm_hosted.yaml`, `workflow_role: campaign_launch`, `generation_model_lane: general_purpose`, `estimated_cost_usd: 0.35`, `operator_friction_tier: low`
  - `local_generation` using `materials-discovery/configs/systems/al_cu_fe_llm_local.yaml`, `workflow_role: campaign_launch`, `generation_model_lane: general_purpose`, `estimated_cost_usd: 0.03`, `operator_friction_tier: medium`
  - `specialized_assessment` using `materials-discovery/configs/systems/al_cu_fe_llm_local.yaml`, `workflow_role: llm_evaluate`, `batch:` set to the acceptance-pack-aligned evaluation slice defined by the shipped benchmark contract rather than a generic unconstrained `all`, `evaluation_model_lane: specialized_materials`, `estimated_cost_usd: 0.05`, `operator_friction_tier: high`

Add `materials-discovery/configs/llm/sc_zn_serving_benchmark.yaml` as a thinner
compatibility example with the same benchmark contract but lighter explanatory
notes and the `Sc-Zn` configs.

Extend `materials-discovery/tests/test_real_mode_pipeline.py` or adjacent fixture
helpers only as needed to assert the example-spec shape stays compatible with
the shipped benchmark command and schema.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_cli.py tests/test_real_mode_pipeline.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/configs/systems/al_cu_fe_llm_hosted.yaml contains "llm_adapter: anthropic_api_v1"
    - materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml contains "benchmark_id: al_cu_fe_serving_v1"
    - materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml contains "hosted_generation"
    - materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml contains "specialized_assessment"
    - materials-discovery/configs/llm/sc_zn_serving_benchmark.yaml contains "benchmark_id:"
    - the example-spec regression command exits 0
  </acceptance_criteria>
  <done>Operators now have committed benchmark specs and a hosted config example that match the shipped benchmark contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document the smoke and benchmark workflow in the runbook and CLI docs</name>
  <files>
    materials-discovery/RUNBOOK.md,
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_cli.py
  </files>
  <read_first>
    materials-discovery/RUNBOOK.md,
    materials-discovery/developers-docs/configuration-reference.md,
    materials-discovery/developers-docs/llm-integration.md,
    materials-discovery/developers-docs/pipeline-stages.md,
    materials-discovery/tests/test_cli.py,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-CONTEXT.md,
    .planning/phases/21-comparative-benchmarks-and-operator-serving-workflow/21-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the runbook includes a smoke-only benchmark recipe and a full benchmark recipe
    - Test 2: docs explain strict failure behavior and the no-silent-fallback rule during benchmarking
    - Test 3: docs explain that the specialized target is evaluation-primary inside the benchmark examples
    - Test 4: docs explain how to interpret quality, cost, latency, and operator-friction fields together
    - Test 5: CLI regression coverage stays green after the new command and docs surface land
  </behavior>
  <action>
Update the following docs additively:

`materials-discovery/RUNBOOK.md`
- add a new serving benchmark section immediately after the closed-loop LLM workflow section
- include:
  - a smoke-only example:
    `uv run mdisc llm-serving-benchmark --spec materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml --smoke-only`
  - a full benchmark example:
    `uv run mdisc llm-serving-benchmark --spec materials-discovery/configs/llm/al_cu_fe_serving_benchmark.yaml`
  - interpretation bullets for quality, cost, latency, and operator friction
  - explicit wording that smoke failure stops the benchmark unless the target explicitly allows fallback
  - a short cleanup note that benchmark artifacts accumulate under `data/benchmarks/llm_serving/`

`materials-discovery/developers-docs/configuration-reference.md`
- document the benchmark spec fields:
  - `workflow_role`
  - `generation_model_lane`
  - `evaluation_model_lane`
  - `estimated_cost_usd`
  - `operator_friction_tier`
  - `allow_fallback`

`materials-discovery/developers-docs/pipeline-stages.md`
- add a formal section for `mdisc llm-serving-benchmark`
- document `--spec`, `--smoke-only`, and the benchmark artifact paths under `data/benchmarks/llm_serving/{benchmark_id}/...`

`materials-discovery/developers-docs/llm-integration.md`
- describe Phase 21 as the operator-serving benchmark layer
- state explicitly that the specialized benchmark target is evaluation-primary in the example workflow
- keep Zomic-native specialized generation deferred to a later milestone

Update `materials-discovery/tests/test_cli.py` only as needed to keep the CLI
help and command-surface regressions green after the new benchmark command lands.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_serving_benchmark_cli.py tests/test_cli.py tests/test_real_mode_pipeline.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - materials-discovery/RUNBOOK.md contains "llm-serving-benchmark"
    - materials-discovery/RUNBOOK.md contains "--smoke-only"
    - materials-discovery/developers-docs/configuration-reference.md contains "estimated_cost_usd"
    - materials-discovery/developers-docs/pipeline-stages.md contains "data/benchmarks/llm_serving"
    - materials-discovery/developers-docs/llm-integration.md contains "evaluation-primary"
    - the docs/CLI regression command exits 0
  </acceptance_criteria>
  <done>The serving benchmark workflow is documented, reproducible, and operator-usable at the end of Phase 21.</done>
</task>

</tasks>

<verification>
  <check>Operators can run a smoke-only serving benchmark and a full serving benchmark from committed example specs.</check>
  <check>Docs explain the tradeoffs across quality, cost, latency, and operator friction without overstating the specialized lane.</check>
  <check>Phase 21 ends with a benchmarkable, operator-usable serving surface rather than hidden test-only behavior.</check>
</verification>
