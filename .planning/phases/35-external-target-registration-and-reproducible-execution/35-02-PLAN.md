---
phase: 35-external-target-registration-and-reproducible-execution
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - materials-discovery/src/materials_discovery/llm/external_targets.py
  - materials-discovery/src/materials_discovery/llm/__init__.py
  - materials-discovery/tests/test_llm_external_target_registry.py
  - materials-discovery/Progress.md
autonomous: true
requirements: [OPS-17]

must_haves:
  truths:
    - "An operator can register one curated external target from a typed spec and get one immutable registration artifact with a conflict-detecting fingerprint."
    - "The same registered target can be resolved back to the same local snapshot and runtime contract on repeated runs."
    - "Each registered target can emit inspectable environment lineage and a typed smoke result before benchmark execution begins."
  artifacts:
    - path: "materials-discovery/src/materials_discovery/llm/external_targets.py"
      provides: "Spec loader, registration core, environment capture, and smoke execution for external targets."
      exports:
        - "load_external_target_spec"
        - "register_external_target"
        - "load_registered_external_target"
        - "capture_external_target_environment"
        - "smoke_external_target"
    - path: "materials-discovery/tests/test_llm_external_target_registry.py"
      provides: "Registry-core regression coverage for normalization, fingerprinting, reload, environment capture, and smoke persistence."
    - path: "materials-discovery/src/materials_discovery/llm/__init__.py"
      provides: "Public Phase 35 registry and smoke API exports."
  key_links:
    - from: "materials-discovery/src/materials_discovery/llm/external_targets.py"
      to: "materials-discovery/src/materials_discovery/llm/schema.py"
      via: "loads the typed spec and writes registration, environment, and smoke artifacts"
      pattern: "LlmExternalTargetRegistrationSpec|LlmExternalTargetEnvironmentManifest|LlmExternalTargetSmokeCheck"
    - from: "materials-discovery/src/materials_discovery/llm/external_targets.py"
      to: "materials-discovery/src/materials_discovery/llm/storage.py"
      via: "persists external target registration and reproducibility artifacts"
      pattern: "llm_external_target_registration_path|llm_external_target_environment_path|llm_external_target_smoke_path"
---

<objective>
Implement the registration, environment-capture, and smoke core for external
benchmark targets.

Purpose: make curated external targets reproducible and smoke-testable before
later phases add comparative benchmark execution and scorecards.

Output: one core external-target module, public exports, and focused tests for
registration, reload, environment capture, and smoke behavior.
</objective>

<execution_context>
@/Users/nikolaosvasiloglou/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nikolaosvasiloglou/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-CONTEXT.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-RESEARCH.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@.planning/phases/35-external-target-registration-and-reproducible-execution/35-01-PLAN.md

@materials-discovery/src/materials_discovery/llm/schema.py
@materials-discovery/src/materials_discovery/llm/storage.py
@materials-discovery/src/materials_discovery/llm/checkpoints.py
@materials-discovery/src/materials_discovery/llm/serving_benchmark.py
@materials-discovery/src/materials_discovery/llm/runtime.py
@materials-discovery/Progress.md

<interfaces>
Follow the existing registration-core pattern:
```python
def register_llm_checkpoint(spec_path: Path, *, root: Path | None = None) -> LlmCheckpointRegistrationSummary: ...
def load_registered_checkpoint(checkpoint_id: str, *, root: Path | None = None) -> tuple[LlmCheckpointRegistration, Path]: ...
```

Reuse the smoke-check posture:
```python
def run_serving_smoke_check(target: LlmServingBenchmarkTarget, *, root: Path | None = None) -> list[LlmServingSmokeCheck]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the immutable external-target registration core and conflict checks</name>
  <files>
    materials-discovery/src/materials_discovery/llm/external_targets.py,
    materials-discovery/tests/test_llm_external_target_registry.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/schema.py,
    materials-discovery/src/materials_discovery/llm/storage.py,
    materials-discovery/src/materials_discovery/llm/checkpoints.py,
    .planning/phases/35-external-target-registration-and-reproducible-execution/35-01-PLAN.md,
    .planning/phases/35-external-target-registration-and-reproducible-execution/35-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `register_external_target(...)` loads a typed spec, normalizes relative snapshot paths, and writes `registration.json` under the dedicated target directory.
    - Test 2: registering the same target ID with the same immutable facts is idempotent, but reusing the target ID with a different fingerprint fails closed with a clear error.
    - Test 3: `load_registered_external_target(...)` returns the typed registration and persisted path for later CLI or benchmark consumers.
    - Test 4: the registration summary returns the target ID, fingerprint, and registration path for CLI output.
    - Test 5: the registry core does not extend `SystemConfig`, checkpoint lifecycle state, or comparative benchmark summary logic.
  </behavior>
  <action>
Create `materials-discovery/src/materials_discovery/llm/external_targets.py` and implement the in-memory registration core.

Add these concrete functions:
- `load_external_target_spec(spec_path: Path) -> LlmExternalTargetRegistrationSpec`
- `_fingerprint_external_target(spec: LlmExternalTargetRegistrationSpec) -> str`
- `register_external_target(spec_path: Path, *, root: Path | None = None) -> LlmExternalTargetRegistrationSummary`
- `load_registered_external_target(model_id: str, *, root: Path | None = None) -> tuple[LlmExternalTargetRegistration, Path]`

Required behavior:
- normalize spec-relative paths into workspace-relative storage values when possible
- fingerprint immutable target facts only
- persist `registration.json` through the Plan 01 storage helpers
- reject conflicting reuse of the same `model_id`
- keep the module benchmark-only and additive to the repo's existing LLM seams

Write focused tests in `materials-discovery/tests/test_llm_external_target_registry.py` using tmp-path specs and fixture snapshot directories. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_registry.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def load_external_target_spec" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "def register_external_target" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "def load_registered_external_target" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "fingerprint" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "already registered with a different fingerprint|model_id" materials-discovery/tests/test_llm_external_target_registry.py`
    - `rg -c "^def test_" materials-discovery/tests/test_llm_external_target_registry.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_registry.py -x -v`
  </acceptance_criteria>
  <done>The repo can register and reload immutable external benchmark targets without silent drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture reproducibility lineage and persist typed smoke results for registered targets</name>
  <files>
    materials-discovery/src/materials_discovery/llm/external_targets.py,
    materials-discovery/src/materials_discovery/llm/__init__.py,
    materials-discovery/tests/test_llm_external_target_registry.py,
    materials-discovery/Progress.md
  </files>
  <read_first>
    AGENTS.md,
    CLAUDE.md,
    materials-discovery/Progress.md,
    materials-discovery/src/materials_discovery/llm/external_targets.py,
    materials-discovery/src/materials_discovery/llm/runtime.py,
    materials-discovery/src/materials_discovery/llm/serving_benchmark.py,
    materials-discovery/src/materials_discovery/llm/schema.py,
    .planning/phases/35-external-target-registration-and-reproducible-execution/35-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `capture_external_target_environment(...)` writes `environment.json` with Python version, package-version map, hardware visibility fields, snapshot lineage, and the registration fingerprint it was captured against.
    - Test 2: `smoke_external_target(...)` writes `smoke_check.json` with typed pass or fail status, latency, detail, and the environment artifact path.
    - Test 3: smoke fails closed when the registration cannot reconstruct its snapshot path or required environment fields.
    - Test 4: runtime-heavy imports stay isolated so registry tests can monkeypatch the smoke path without requiring real Hugging Face weights or package installs.
    - Test 5: the public API exports the environment and smoke helpers for later CLI use.
  </behavior>
  <action>
Extend `materials-discovery/src/materials_discovery/llm/external_targets.py` so registered targets can persist reproducibility lineage and typed smoke artifacts.

Add these concrete functions:
- `capture_external_target_environment(registration: LlmExternalTargetRegistration, *, root: Path | None = None) -> LlmExternalTargetEnvironmentManifest`
- `smoke_external_target(model_id: str, *, root: Path | None = None) -> LlmExternalTargetSmokeCheck`

Required behavior:
- collect Python version, platform fields, and an explicit package-version map for packages that are importable in the current environment
- record snapshot and tokenizer lineage fields from the registration
- persist the environment manifest to `environment.json`
- persist the smoke result to `smoke_check.json`
- keep heavy runtime imports local to the smoke path and make the test surface monkeypatchable
- fail closed when the registration or captured environment is incomplete for reproducible execution

Re-export the public Phase 35 API surface from `materials-discovery/src/materials_discovery/llm/__init__.py`.
Extend the test file so environment and smoke artifacts are validated through the typed schema models, not raw dict assertions. Update `materials-discovery/Progress.md` in the same task.
  </action>
  <verify>
    <automated>cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_registry.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def capture_external_target_environment" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "def smoke_external_target" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "environment.json|smoke_check.json" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "python_version|package_versions|registration_fingerprint" materials-discovery/src/materials_discovery/llm/external_targets.py`
    - `rg -n "smoke_external_target|capture_external_target_environment" materials-discovery/src/materials_discovery/llm/__init__.py`
    - `rg -n "environment.json|smoke_check.json|failed" materials-discovery/tests/test_llm_external_target_registry.py`
    - `cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_registry.py -x -v`
  </acceptance_criteria>
  <done>The repo can prove an external target's environment lineage and smoke readiness as first-class files before benchmark execution.</done>
</task>

</tasks>

<verification>
Run the focused registry or smoke suite after both tasks:

`cd /Users/nikolaosvasiloglou/github-repos/vzome/materials-discovery && uv run pytest tests/test_llm_external_target_registry.py -x -v`
</verification>

<success_criteria>
Phase 35 can now register external benchmark targets immutably, re-resolve them
to the same runtime contract, and persist inspectable environment and smoke
artifacts ahead of benchmark execution.
</success_criteria>

<output>
After completion, create `.planning/phases/35-external-target-registration-and-reproducible-execution/35-02-SUMMARY.md`
</output>
