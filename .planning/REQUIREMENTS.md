# Requirements

No active milestone requirements are defined yet.

The shipped `v1.0` requirement set is archived at
`.planning/milestones/v1.0-REQUIREMENTS.md`.

Start the next cycle with `$gsd-new-milestone` when you are ready to define the
next milestone.
